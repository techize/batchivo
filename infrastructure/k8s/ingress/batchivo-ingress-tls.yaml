apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: batchivo-ingress
  namespace: batchivo
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: batchivo-body-size@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - batchivo.app
    - api.batchivo.app
    secretName: batchivo-tls
  rules:
  - host: batchivo.app
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 8000
      - path: /uploads
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 8080
  - host: api.batchivo.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 8000
