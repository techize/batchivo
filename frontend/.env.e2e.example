# E2E Test Configuration
# Copy this file to .env.e2e and fill in with actual test credentials

# Test user credentials (should be a dedicated test account)
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword

# Playwright base URL (defaults to http://localhost:5173 if not set)
PLAYWRIGHT_BASE_URL=http://localhost:5173

# Backend API URL (if different from default)
# VITE_API_BASE_URL=http://localhost:8000
