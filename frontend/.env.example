# Batchivo Frontend - Environment Variables

# Backend API URL
VITE_API_URL=http://localhost:8000

# Feature Flags
VITE_ENABLE_QR_SCANNER=true
VITE_ENABLE_INTEGRATIONS=false

# OpenTelemetry Configuration
# Enable OTEL in development (automatically enabled in production)
VITE_OTEL_ENABLED=false
# OTLP endpoint for traces (defaults to /v1/traces, proxied to backend)
VITE_OTEL_ENDPOINT=/v1/traces
# Service name for traces
VITE_SERVICE_NAME=batchivo-frontend
# Service version (defaults to package.json version)
VITE_SERVICE_VERSION=1.0.0

# Sentry Error Monitoring
# Get DSN from: https://sentry.io/ -> Settings -> Projects -> Client Keys
VITE_SENTRY_DSN=
# Enable Sentry in development (automatically enabled in production if DSN set)
VITE_SENTRY_DEV_ENABLED=false

# Sentry Build Configuration (for source map uploads)
# These are only needed in CI/CD for production builds
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
# SENTRY_ORG=batchivo
# SENTRY_PROJECT=batchivo-frontend
