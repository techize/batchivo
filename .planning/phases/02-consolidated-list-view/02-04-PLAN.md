---
phase: 02-consolidated-list-view
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - frontend/src/types/filament-type.ts
  - frontend/src/lib/api/filament-types.ts
  - frontend/src/hooks/useFilamentTypes.ts
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "FilamentTypeListItem, FilamentTypeAggregatedListResponse, FilamentTypeListParams, SpoolInSheet, and FilamentTypeUpdate TypeScript interfaces are exported from src/types/filament-type.ts"
    - "filamentTypesApi.list(), filamentTypesApi.getSpools(), and filamentTypesApi.update() are exported from src/lib/api/filament-types.ts"
    - "useFilamentTypes, useFilamentTypeSpools, and useToggleHasSample hooks are exported from src/hooks/useFilamentTypes.ts"
    - "TypeScript compiles with no errors across all three files"
  artifacts:
    - path: "frontend/src/types/filament-type.ts"
      provides: "TypeScript types mirroring backend schemas from Plan 02"
    - path: "frontend/src/lib/api/filament-types.ts"
      provides: "API client for /api/v1/filament-types/aggregated and /{id}/spools"
    - path: "frontend/src/hooks/useFilamentTypes.ts"
      provides: "TanStack Query hooks for data fetching and has_sample optimistic toggle"
  key_links:
    - from: "frontend/src/types/filament-type.ts"
      to: "frontend/src/lib/api/filament-types.ts"
      via: "imported type annotations"
    - from: "frontend/src/lib/api/filament-types.ts"
      to: "frontend/src/hooks/useFilamentTypes.ts"
      via: "filamentTypesApi imported into hooks"
    - from: "frontend/src/hooks/useFilamentTypes.ts"
      to: "frontend/src/pages/FilamentLibrary.tsx (Plan 05)"
      via: "useFilamentTypes and useToggleHasSample imported by page component"
---

<objective>
Create the frontend data layer: TypeScript type definitions matching the backend schemas, an API client for the two new endpoints, and TanStack Query hooks for the list, sheet, and has_sample toggle.

Purpose: These three files are consumed by the UI components in Plans 05 and 06. Building them in Wave 2 (parallel with the backend endpoint work in Plan 03) means the frontend components in Wave 3 can start immediately after Wave 2 completes.
Output: Three new files created; no existing files modified.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md

<interfaces>
From frontend/src/types/spool.ts (exact pattern to replicate for naming and style):
  Module docstring at top, grouped interface exports, snake_case field names matching backend.

From frontend/src/lib/api/spools.ts (exact pattern to replicate for apiClient usage):
  import { apiClient } from '../api'
  URLSearchParams built conditionally before appending to URL
  Named export: `export const spoolsApi = { list, get, create, update, delete }`

From frontend/src/lib/api/api.ts or src/lib/api.ts (base client — verify import path):
  apiClient.get<T>(url): Promise<T>
  apiClient.put<T>(url, data): Promise<T>

From frontend/src/hooks/useModules.ts (hook pattern to replicate):
  useQuery with explicit queryKey array, queryFn arrow function, optional enabled flag
  useMutation with onMutate (optimistic), onError (rollback), onSettled (invalidate)

Backend schema shapes (from Plan 02 PATTERNS.md):
  FilamentTypeAggregatedResponse: id, brand, color, color_hex?, material_type_name, material_type_code, has_sample, spool_count, labeled_count
  SpoolInSheetResponse: id, spool_id, current_weight, initial_weight, is_labeled, is_active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types file for filament-type domain</name>
  <files>frontend/src/types/filament-type.ts</files>
  <read_first>
    - frontend/src/types/spool.ts (read for naming convention, module docstring pattern, interface structure)
    - frontend/src/lib/api/spools.ts (read lines 1-15 for import pattern — confirms apiClient import path)
  </read_first>
  <action>
    Create frontend/src/types/filament-type.ts as a new file. Do not modify any existing file.

    Include a module docstring comment: "TypeScript types for FilamentType API — matches Pydantic schemas in backend/app/schemas/filament_type.py"

    Export these interfaces in order:

    FilamentTypeListItem:
      id: string; brand: string; color: string; color_hex?: string | null;
      material_type_name: string; material_type_code: string;
      has_sample: boolean; spool_count: number; labeled_count: number

    FilamentTypeAggregatedListResponse:
      total: number; filament_types: FilamentTypeListItem[]; page: number; page_size: number

    FilamentTypeListParams:
      page?: number; page_size?: number; brand?: string; color?: string;
      material_type_id?: string; needs_labels?: boolean; needs_sample?: boolean

    SpoolInSheet:
      id: string; spool_id: string; current_weight: number; initial_weight: number;
      is_labeled: boolean; is_active: boolean

    FilamentTypeUpdate:
      has_sample?: boolean; brand?: string; color?: string; color_hex?: string | null;
      material_type_id?: string; finish?: string | null; diameter?: number;
      extruder_temp?: number | null; bed_temp?: number | null;
      translucent?: boolean; glow?: boolean; notes?: string | null
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm tsc --noEmit 2>&1 | grep "filament-type" || echo "no errors in filament-type.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/types/filament-type.ts
    - `pnpm tsc --noEmit` exits 0 (or produces no errors referencing filament-type.ts)
    - All five interface names appear in the file: FilamentTypeListItem, FilamentTypeAggregatedListResponse, FilamentTypeListParams, SpoolInSheet, FilamentTypeUpdate
    - color_hex field typed as `string | null | undefined` (optional nullable)
  </acceptance_criteria>
  <done>Five TypeScript interfaces exported from filament-type.ts; compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Create filament-types API client and TanStack Query hooks</name>
  <files>frontend/src/lib/api/filament-types.ts, frontend/src/hooks/useFilamentTypes.ts</files>
  <read_first>
    - frontend/src/lib/api/spools.ts (full file — exact structure: import pattern, URLSearchParams conditional build, named export object with typed methods)
    - frontend/src/hooks/useModules.ts (full file — useQuery options object pattern, useMutation with optimistic update)
    - frontend/src/lib/api.ts (verify apiClient export and generic method signatures: get<T>, put<T>)
  </read_first>
  <action>
    Create frontend/src/lib/api/filament-types.ts:
    - Import apiClient from '../api' (verify exact relative path from the spools.ts import)
    - Import types from '@/types/filament-type': FilamentTypeAggregatedListResponse, FilamentTypeListParams, FilamentTypeListItem, SpoolInSheet, FilamentTypeUpdate
    - Export const filamentTypesApi with three methods:
      list(params?: FilamentTypeListParams): builds URLSearchParams conditionally for all 7 possible params (page, page_size, brand, color, material_type_id, needs_labels, needs_sample), calls apiClient.get<FilamentTypeAggregatedListResponse>(`/api/v1/filament-types/aggregated${queryString ? '?' + queryString : ''}`)
      getSpools(filamentTypeId: string): calls apiClient.get<SpoolInSheet[]>(`/api/v1/filament-types/${filamentTypeId}/spools`)
      update(id: string, data: FilamentTypeUpdate): calls apiClient.put<FilamentTypeListItem>(`/api/v1/filament-types/${id}`, data)

    Create frontend/src/hooks/useFilamentTypes.ts:
    - Imports: useQuery, useMutation, useQueryClient from '@tanstack/react-query'; filamentTypesApi from '@/lib/api/filament-types'; FilamentTypeAggregatedListResponse, FilamentTypeListParams, FilamentTypeUpdate from '@/types/filament-type'
    - Export useFilamentTypes(params: FilamentTypeListParams): useQuery<FilamentTypeAggregatedListResponse> with queryKey ['filament-types', params] and queryFn calling filamentTypesApi.list(params)
    - Export useFilamentTypeSpools(filamentTypeId: string | null): useQuery with queryKey ['filament-type-spools', filamentTypeId], queryFn calling filamentTypesApi.getSpools(filamentTypeId!), and enabled: filamentTypeId !== null
    - Export useToggleHasSample(params: FilamentTypeListParams): useMutation with:
        mutationFn: ({ id, hasSample }) => filamentTypesApi.update(id, { has_sample: hasSample })
        onMutate: cancelQueries(['filament-types']), snapshot previous, optimistically setQueryData to flip has_sample for matching id, return { previous }
        onError: restore previous snapshot on error
        onSettled: invalidateQueries({ queryKey: ['filament-types'] })
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm tsc --noEmit 2>&1 | grep -E "filament-types|useFilamentTypes" || echo "no errors"</automated>
  </verify>
  <acceptance_criteria>
    - Both files created at correct paths
    - `pnpm tsc --noEmit` exits 0 (no TypeScript errors)
    - filament-types.ts exports filamentTypesApi with list, getSpools, update methods
    - useFilamentTypes.ts exports useFilamentTypes, useFilamentTypeSpools, useToggleHasSample
    - useToggleHasSample onMutate contains cancelQueries and setQueryData calls
    - useFilamentTypeSpools has `enabled: filamentTypeId !== null`
  </acceptance_criteria>
  <done>API client and three hooks created and TypeScript-clean; ready for UI component consumption in Plans 05 and 06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API client | API client receives server responses — types enforce shape at compile time only |
| Optimistic update → query cache | onMutate writes to cache before server confirms — onError must restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | filamentTypesApi query params | mitigate | URLSearchParams.append() escapes values; no raw string interpolation; server validates UUID and boolean types |
| T-02-11 | Tampering | useToggleHasSample optimistic update | accept | Client-side cache manipulation affects only the current user's view; onError rollback restores truth; server is source of truth after onSettled invalidation |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd frontend && pnpm tsc --noEmit` — exits 0
2. `grep -l "filamentTypesApi" frontend/src/lib/api/filament-types.ts frontend/src/hooks/useFilamentTypes.ts` — both files appear
3. `grep -c "useFilamentTypes\|useFilamentTypeSpools\|useToggleHasSample" frontend/src/hooks/useFilamentTypes.ts` — returns 3
</verification>

<success_criteria>
- Three new frontend files created; zero TypeScript errors
- API client builds correct URL with conditional query string
- useFilamentTypeSpools only fetches when filamentTypeId is non-null (sheet-open guard)
- Optimistic toggle has cancel/snapshot/setQueryData/rollback/invalidate pattern complete
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-04-SUMMARY.md` when done
</output>
