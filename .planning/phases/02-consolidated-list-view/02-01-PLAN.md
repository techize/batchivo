---
phase: 02-consolidated-list-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - backend/app/api/v1/modules.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02
must_haves:
  truths:
    - "Navigating to /inventory in a browser redirects to /filaments without a 404"
    - "The sidebar shows exactly one nav entry linking to /filaments labelled 'Filaments'; no 'Inventory' entry appears"
    - "Dynamic nav (from GET /api/v1/modules) returns exactly one 'spools' entry pointing to /filaments"
    - "No internal source file contains a routing path literal '/inventory' in a navigation context"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "inventoryRoute redirects to /filaments; filamentsRoute renders FilamentLibrary placeholder"
    - path: "frontend/src/components/layout/AppLayout.tsx"
      provides: "fallbackNavItems omits /inventory entry"
    - path: "backend/app/api/v1/modules.py"
      provides: "MODULE_NAVIGATION[spools] has single /filaments entry"
  key_links:
    - from: "frontend/src/App.tsx inventoryRoute"
      to: "/filaments"
      via: "Navigate component from @tanstack/react-router"
    - from: "AppLayout.tsx fallbackNavItems"
      to: "backend MODULE_NAVIGATION"
      via: "must stay in sync — both updated in same plan"
---

<objective>
Route and navigation surgery: convert /inventory from a duplicate page to a redirect, update the sidebar nav to show a single "Filaments" entry, and align the backend module nav dict.

Purpose: Satisfies NAV-01 (route deduplication) and NAV-02 (no internal /inventory links), and is production-safe (route stays registered in the router tree — only its component changes, per D-02).
Output: Three files modified; no new files created.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md

<interfaces>
From frontend/src/App.tsx (lines 219-242, existing route definitions to modify):

inventoryRoute defined at path '/inventory', currently wraps Dashboard in ProtectedRoute+ModuleGuard.
filamentsRoute defined at path '/filaments', currently wraps Dashboard in ProtectedRoute+ModuleGuard.

Navigate is already imported from '@tanstack/react-router' (line 11).
Both routes are registered in routeTree.addChildren([...]) — inventoryRoute MUST remain in that array.

From frontend/src/components/layout/AppLayout.tsx (lines 115-128, fallbackNavItems array):
  { path: '/inventory', label: 'Inventory', icon: 'box', exact: true }  ← REMOVE this entry
  (no /filaments entry exists in fallbackNavItems — it comes from MODULE_NAVIGATION dynamically)

From backend/app/api/v1/modules.py (lines 78-80, MODULE_NAVIGATION dict):
  "spools": [
      {"path": "/inventory", "label": "Inventory", "icon": "box", "exact": True},
      {"path": "/filaments", "label": "Filaments", "icon": "box", "exact": False},
  ]
  ← Replace with single entry pointing to /filaments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert inventoryRoute to redirect and filamentsRoute to FilamentLibrary placeholder</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (read full file — need current route definitions, addChildren array, import list, Navigate import location)
  </read_first>
  <action>
    Read the full App.tsx. Make two targeted changes:

    1. Change inventoryRoute's component to render only `() => <Navigate to="/filaments" />`. Remove the ProtectedRoute+ModuleGuard wrapper from this route — Navigate fires immediately and needs no auth gating. Keep inventoryRoute registered in routeTree.addChildren([...]) unchanged.

    2. Change filamentsRoute's component to render `FilamentLibrary` in place of `Dashboard`. The wrapper stays: `ProtectedRoute > ModuleGuard > FilamentLibrary`. Add a temporary stub import: `const FilamentLibrary = () => <div>Filament Library — Phase 2 WIP</div>` at the top of the route section as a placeholder (the real component is created in Plan 05). This placeholder keeps the route functional so tests can assert the redirect.

    Do not change any other routes, imports, or the addChildren array ordering.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && grep -n "Navigate to=\"/filaments\"" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `component: () => <Navigate to="/filaments" />` inside inventoryRoute
    - App.tsx contains `FilamentLibrary` as the component inside filamentsRoute (stub or real)
    - inventoryRoute remains in the routeTree.addChildren([...]) array (grep confirms it's still there)
    - No compile errors: `cd frontend && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>inventoryRoute redirects to /filaments; filamentsRoute uses FilamentLibrary component; both routes in addChildren array</done>
</task>

<task type="auto">
  <name>Task 2: Remove /inventory from fallbackNavItems and fix MODULE_NAVIGATION</name>
  <files>frontend/src/components/layout/AppLayout.tsx, backend/app/api/v1/modules.py</files>
  <read_first>
    - frontend/src/components/layout/AppLayout.tsx (full file — locate fallbackNavItems array, lines ~115-128)
    - backend/app/api/v1/modules.py (full file — locate MODULE_NAVIGATION dict, lines ~78-80)
  </read_first>
  <action>
    In AppLayout.tsx: Remove exactly the object `{ path: '/inventory', label: 'Inventory', icon: 'box', exact: true }` from the fallbackNavItems array. Do not touch any other entries. The dynamic nav from MODULE_NAVIGATION already serves the /filaments entry — fallbackNavItems is only a loading-state fallback.

    In modules.py: Replace the "spools" key's list value with a single entry: `[{"path": "/filaments", "label": "Filaments", "icon": "box", "exact": False}]`. Remove the /inventory entry entirely. The path, label, icon, and exact values must match exactly as specified.

    Both files must be modified in this task since they form a coupled "nav source of truth" — they must stay in sync (pitfall documented in RESEARCH.md: stale nav cache when only one is updated).
  </action>
  <verify>
    <automated>grep -n "inventory" /Users/jonathan/Repos/batchivo/frontend/src/components/layout/AppLayout.tsx | grep -v "^.*#" | grep -v "//"; grep -n "inventory" /Users/jonathan/Repos/batchivo/backend/app/api/v1/modules.py</automated>
  </verify>
  <acceptance_criteria>
    - AppLayout.tsx has zero occurrences of `/inventory` as a nav path (grep exits non-zero or returns no matching nav object lines)
    - modules.py "spools" value is a list with exactly one dict: path="/filaments", label="Filaments"
    - `cd frontend && pnpm tsc --noEmit` exits 0
    - `cd backend && poetry run ruff check app/api/v1/modules.py` exits 0
  </acceptance_criteria>
  <done>/inventory removed from both nav sources; /filaments is the single entry in both fallbackNavItems-equivalent and MODULE_NAVIGATION</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → TanStack Router | Route path is controlled by browser URL — external input |
| Browser → GET /api/v1/modules | Dynamic nav is fetched; manipulated response could inject nav paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | inventoryRoute component | accept | Route renders `<Navigate>` only — no auth check removed because redirect does not expose protected content; /filaments is itself protected by ProtectedRoute |
| T-02-02 | Information Disclosure | MODULE_NAVIGATION dict | accept | Dict is read-only server config; no user input involved; nav paths are not sensitive |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `grep -rn '"/inventory"' frontend/src/ --include="*.tsx" --include="*.ts"` — result must show only the inventoryRoute definition (path: '/inventory') and zero navigation-context uses
2. `grep -n "inventory" backend/app/api/v1/modules.py` — must return no nav path matches
3. `cd frontend && pnpm tsc --noEmit` — exits 0 (TypeScript clean)
4. `cd backend && poetry run ruff check app/api/v1/modules.py` — exits 0
</verification>

<success_criteria>
- /inventory route remains registered in TanStack Router (no 404), renders Navigate to /filaments
- /filaments route renders FilamentLibrary (stub or real)
- Sidebar nav has zero "Inventory" entries in both static fallback and dynamic API source
- TypeScript and ruff both pass
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-01-SUMMARY.md` when done
</output>
