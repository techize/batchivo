---
phase: 02-consolidated-list-view
plan: 06
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx
  - frontend/src/components/filaments/FilamentTypeFilterSheet.tsx
  - frontend/src/pages/FilamentLibrary.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "Clicking a FilamentType row opens a Sheet from the right showing child Spool records (spool_id, weight, is_labeled badge, is_active badge)"
    - "The spool sheet fetches from GET /api/v1/filament-types/{id}/spools only when the sheet opens (enabled guard)"
    - "The spool sheet shows Skeleton rows while loading and an error/empty state when appropriate"
    - "The Filters button opens a second Sheet with 5 filter dimensions: brand text, color text, material type select, Needs labels toggle, No sample toggle"
    - "The two sheets are independent (not nested) — each has its own state variable"
    - "Spool labeled status shows 'Labeled' (success Badge) / 'Needs label' (warning Badge); active status shows 'Active' (success) / 'Inactive' (secondary)"
    - "Filter sheet title is 'Filter Filaments'; filter sheet has Apply filters and Clear all buttons"
  artifacts:
    - path: "frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx"
      provides: "Read-only spool drill-down Sheet with skeleton loading and error states"
    - path: "frontend/src/components/filaments/FilamentTypeFilterSheet.tsx"
      provides: "Filter Sheet with 5 dimensions, toggle buttons, and material type select"
  key_links:
    - from: "frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx"
      to: "frontend/src/hooks/useFilamentTypes.ts useFilamentTypeSpools"
      via: "useFilamentTypeSpools(filamentTypeId) hook with enabled guard"
    - from: "frontend/src/components/filaments/FilamentTypeFilterSheet.tsx"
      to: "frontend/src/lib/api/spools.ts materialTypesApi.list()"
      via: "existing materialTypesApi for material type select options"
    - from: "frontend/src/pages/FilamentLibrary.tsx"
      to: "both sheet components"
      via: "replacing stubs from Plan 05 with real imports"
---

<objective>
Build the two Sheet components (spool drill-down and filter panel) and wire them into FilamentLibrary.tsx by replacing the Plan 05 stubs with real imports.

Purpose: Completes the interactive layer of LIST-01/LIST-02 (spool drill-down sheet) and LIST-03 (filter sheet with 5 dimensions). These run parallel to FilamentLibrary page creation (Plan 05) since they share no files with Plan 05 except FilamentLibrary.tsx which is updated as a final wiring step.
Output: Two new component files; FilamentLibrary.tsx updated to remove stubs.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-UI-SPEC.md

<interfaces>
From frontend/src/components/ui/sheet.tsx (verify Sheet component API):
  Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription — controlled via open + onOpenChange props

From frontend/src/components/ui/skeleton.tsx (Skeleton component):
  Skeleton className pattern: used for loading placeholder rows

From frontend/src/lib/api/spools.ts (for materialTypesApi):
  materialTypesApi.list() — already exists, returns material types for select dropdown

From frontend/src/hooks/useFilamentTypes.ts (Plan 04 output):
  useFilamentTypeSpools(filamentTypeId: string | null) — enabled only when filamentTypeId !== null

From frontend/src/types/filament-type.ts (Plan 04 output):
  SpoolInSheet: { id, spool_id, current_weight, initial_weight, is_labeled, is_active }
  FilamentTypeListParams: { brand?, color?, material_type_id?, needs_labels?, needs_sample? }

UI-SPEC contracts:
  Sheet title (spool): "{brand} {color} {material_type_name}" e.g. "JAYO Black PETG"
  Sheet description (spool): "{spool_count} spool{s}" e.g. "3 spools"
  Sheet empty: "No spools found for this filament type."
  Sheet error: "Could not load spools. Try again."
  Spool labeled badge: "Labeled" (success) / "Needs label" (warning)
  Spool active badge: "Active" (success) / "Inactive" (secondary)
  Sheet loading: Skeleton rows — no text
  Filter sheet title: "Filter Filaments"
  Filter sheet description: "Narrow the list by any combination of properties."
  Filter apply button: "Apply filters"
  Filter clear button: "Clear all"
  Two sheets must be SIBLINGS (not nested) — shadcn Sheet uses Radix Dialog; nesting breaks focus trap

Spool sheet MUST NOT contain Edit/Delete/Update Weight — read-only per D-09.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilamentTypeSpoolSheet (read-only spool drill-down)</name>
  <files>frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx</files>
  <read_first>
    - frontend/src/components/ui/sheet.tsx (verify Sheet props: open, onOpenChange, SheetContent, SheetHeader, SheetTitle, SheetDescription)
    - frontend/src/components/ui/skeleton.tsx (verify Skeleton className usage)
    - frontend/src/components/ui/table.tsx (Table, TableHeader, TableBody, TableHead, TableRow, TableCell)
    - frontend/src/hooks/useFilamentTypes.ts (useFilamentTypeSpools return shape)
  </read_first>
  <action>
    Create frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx.

    Props interface FilamentTypeSpoolSheetProps:
      filamentTypeId: string | null
      filamentTypeName?: string  (format: "{brand} {color} {material_type_name}" — passed from parent)
      spoolCount?: number        (for Sheet description pluralization)
      onClose: () => void

    Import useFilamentTypeSpools from hooks. Fetch only when filamentTypeId !== null (hook's enabled guard handles this).

    Sheet: `open={filamentTypeId !== null}`, `onOpenChange={(open) => { if (!open) onClose() }}`
    SheetContent: side="right", className="w-full sm:max-w-[480px]"
    SheetHeader: SheetTitle={filamentTypeName ?? 'Spools'}, SheetDescription="{spoolCount} spool{spoolCount !== 1 ? 's' : ''}" when spoolCount defined, else omit description

    Loading state (isLoading):
      Render 3 Skeleton rows inside a div — each row is `<div className="flex gap-4 py-2"><Skeleton className="h-4 w-24" /><Skeleton className="h-4 w-16" /><Skeleton className="h-4 w-16" /></div>`

    Error state (isError):
      `<p className="text-destructive text-sm">Could not load spools. Try again.</p>`

    Empty state (data && data.length === 0):
      `<p className="text-muted-foreground text-sm">No spools found for this filament type.</p>`

    Data state (data && data.length > 0):
      Table with TableHeader (Spool ID / Weight / Labeled / Active columns)
      TableBody mapping spools:
        TableCell 1: spool.spool_id with className="font-mono text-sm"
        TableCell 2: `${spool.current_weight.toFixed(0)}g / ${spool.initial_weight.toFixed(0)}g`
        TableCell 3: Badge variant="success" "Labeled" when is_labeled, else Badge variant="warning" "Needs label"
        TableCell 4: Badge variant="success" "Active" when is_active, else Badge variant="secondary" "Inactive"

    No Edit / Delete / Update Weight actions — sheet is read-only per D-09.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm tsc --noEmit 2>&1 | grep "FilamentTypeSpoolSheet" || echo "no errors"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx
    - `pnpm tsc --noEmit` exits 0
    - Sheet has `open={filamentTypeId !== null}` and `onOpenChange` that calls onClose when closed
    - isLoading renders Skeleton elements
    - Labeled badge uses "Needs label" (not "Unlabeled") per UI-SPEC copywriting contract
    - No edit/delete/update-weight actions present in the component
    - `font-mono` class applied to spool_id cell
  </acceptance_criteria>
  <done>Read-only SpoolSheet created with loading/error/empty/data states and correct badge copywriting</done>
</task>

<task type="auto">
  <name>Task 2: Create FilamentTypeFilterSheet and wire both sheets into FilamentLibrary.tsx</name>
  <files>frontend/src/components/filaments/FilamentTypeFilterSheet.tsx, frontend/src/pages/FilamentLibrary.tsx</files>
  <read_first>
    - frontend/src/components/inventory/SpoolList.tsx (lines 280-363 — filter panel pattern: Input fields, toggle buttons, Select for material type, clear all logic)
    - frontend/src/lib/api/spools.ts (confirm materialTypesApi export name and list() method signature)
    - frontend/src/pages/FilamentLibrary.tsx (current state from Plan 05 — locate the two stub component declarations to replace)
  </read_first>
  <action>
    Create frontend/src/components/filaments/FilamentTypeFilterSheet.tsx:

    Props interface FilamentTypeFilterSheetProps:
      open: boolean
      onOpenChange: (open: boolean) => void
      params: FilamentTypeListParams
      onParamsChange: (params: FilamentTypeListParams) => void

    Import: Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription; Input, Button, Label; Select, SelectContent, SelectItem, SelectTrigger, SelectValue; useQuery from @tanstack/react-query; materialTypesApi from '@/lib/api/spools'

    Inside component:
      const { data: materialTypes } = useQuery({ queryKey: ['material-types'], queryFn: () => materialTypesApi.list() })
      Local draft state mirroring params: const [draft, setDraft] = useState(params). Reset draft when sheet opens.

    Sheet: open={open}, onOpenChange={onOpenChange}
    SheetContent: side="right", className="w-full sm:max-w-[420px]"
    SheetHeader: SheetTitle="Filter Filaments", SheetDescription="Narrow the list by any combination of properties."

    Filter dimensions inside SheetContent:
      1. Brand text Input: Label "Brand", Input value={draft.brand ?? ''} onChange updating draft.brand (empty string → undefined)
      2. Colour text Input: Label "Colour", same pattern for draft.color
      3. Material type Select: Label "Material type", SelectTrigger+SelectValue+SelectContent with SelectItem per material type (value=material.id, text=material.name); "All materials" option with value="" to clear
      4. "Needs labels" Button: variant={draft.needs_labels ? 'default' : 'outline'}, aria-pressed={!!draft.needs_labels}, onClick toggles draft.needs_labels (undefined ↔ true)
      5. "No sample" Button: same pattern for draft.needs_sample

    Footer row with two buttons:
      "Clear all" Button variant="outline": resets draft to {} and calls onParamsChange({})
      "Apply filters" Button variant="default": calls onParamsChange(draft) then onOpenChange(false)

    Wire into FilamentLibrary.tsx:
      Remove the two stub declarations (`const FilamentTypeSpoolSheet = (_props: any) => null` and `const FilamentTypeFilterSheet = (_props: any) => null`).
      Add real imports: `import { FilamentTypeSpoolSheet } from '@/components/filaments/FilamentTypeSpoolSheet'` and `import { FilamentTypeFilterSheet } from '@/components/filaments/FilamentTypeFilterSheet'`.
      Verify the JSX usage in FilamentLibrary.tsx passes the correct props to each sheet (spoolSheetFilamentTypeId, spoolSheetFilamentTypeName, spoolCount from data?.total, onClose for spool sheet; open, onOpenChange, params, onParamsChange for filter sheet).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm tsc --noEmit 2>&1 | grep -E "FilterSheet|SpoolSheet|FilamentLibrary" || echo "no errors"</automated>
  </verify>
  <acceptance_criteria>
    - FilamentTypeFilterSheet.tsx created at correct path
    - `pnpm tsc --noEmit` exits 0 across all new and modified files
    - FilamentLibrary.tsx no longer contains `const FilamentTypeSpoolSheet = (_props: any) => null` stub
    - FilamentLibrary.tsx imports FilamentTypeSpoolSheet from '@/components/filaments/FilamentTypeSpoolSheet'
    - FilamentLibrary.tsx imports FilamentTypeFilterSheet from '@/components/filaments/FilamentTypeFilterSheet'
    - FilamentTypeFilterSheet.tsx contains 5 filter controls: brand Input, color Input, material_type_id Select, needs_labels Button, needs_sample Button
    - "Apply filters" and "Clear all" buttons present with correct variant attributes
    - Draft state pattern present (local state synced with params, applied on button click)
    - `pnpm lint` exits 0 (or `pnpm eslint src/components/filaments/ --max-warnings 0`)
  </acceptance_criteria>
  <done>Both sheets created and wired into FilamentLibrary; stubs removed; full Phase 2 UI chain complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filter sheet inputs → API params | User text input flows to backend query params |
| Spool sheet API response → DOM | Spool data rendered as text nodes in React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | FilamentTypeFilterSheet text inputs | mitigate | React JSX renders as text nodes; server uses parameterized ilike() — no SQL injection path; brand/color are display-only strings |
| T-02-16 | Information Disclosure | SpoolSheet renders spool data | accept | Data is tenant-scoped at API layer; client renders tenant's own spool records |
| T-02-17 | Denial of Service | Sheet focus trap conflict | mitigate | Sheets are siblings (not nested) — each has independent open state; Radix Dialog focus trap is designed for non-nested usage; tested by visual inspection checkpoint in verify-work |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd frontend && pnpm tsc --noEmit` — exits 0
2. `grep -n "FilamentTypeSpoolSheet\|FilamentTypeFilterSheet" frontend/src/pages/FilamentLibrary.tsx` — both real imports present, no stub declarations
3. `grep -n "materialTypesApi" frontend/src/components/filaments/FilamentTypeFilterSheet.tsx` — present
4. `grep -c "filter" frontend/src/components/filaments/FilamentTypeFilterSheet.tsx` — at least 5 filter controls
5. `grep -n "Needs label" frontend/src/components/filaments/FilamentTypeSpoolSheet.tsx` — copywriting matches UI-SPEC
</verification>

<success_criteria>
- FilamentTypeSpoolSheet: read-only spool table, loading skeleton, error/empty states, correct badge labels
- FilamentTypeFilterSheet: 5 filter dimensions, draft state, apply/clear buttons, material type select populated from API
- FilamentLibrary.tsx: real imports in place, no stubs
- Full TypeScript compilation clean
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-06-SUMMARY.md` when done
</output>
