---
phase: 02-consolidated-list-view
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/integration/test_filament_types_api.py
  - frontend/src/App.test.tsx
  - frontend/src/components/filaments/FilamentTypeCard.test.tsx
  - .planning/phases/02-consolidated-list-view/02-VALIDATION.md
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "backend/tests/integration/test_filament_types_api.py is importable and contains a TestFilamentTypeAggregatedEndpoints class with 8 skipped test stubs"
    - "frontend/src/App.test.tsx is importable by Vitest and contains a describe block for inventory redirect coverage"
    - "frontend/src/components/filaments/FilamentTypeCard.test.tsx is importable by Vitest and contains a describe block for card rendering coverage"
    - "pytest --collect-only discovers all 8 stubs without errors"
    - "vitest --run discovers both frontend stub files without errors"
    - "VALIDATION.md frontmatter has wave_0_complete: true and nyquist_compliant: true after this plan"
  artifacts:
    - path: "backend/tests/integration/test_filament_types_api.py"
      provides: "TestFilamentTypeAggregatedEndpoints class with @pytest.mark.skip stubs for Plan 07 to fill in"
      contains: "class TestFilamentTypeAggregatedEndpoints"
    - path: "frontend/src/App.test.tsx"
      provides: "Skeleton describe block for route redirect coverage; Plan 08 fills in real assertions"
    - path: "frontend/src/components/filaments/FilamentTypeCard.test.tsx"
      provides: "Skeleton describe block for card unit tests; Plan 08 fills in real assertions"
    - path: ".planning/phases/02-consolidated-list-view/02-VALIDATION.md"
      provides: "Updated frontmatter confirming Wave 0 complete and Nyquist compliance"
  key_links:
    - from: "backend/tests/integration/test_filament_types_api.py"
      to: "backend/app/api/v1/filament_types.py"
      via: "pytest collects stubs now; Plan 07 fills in real test bodies against new endpoints"
    - from: "frontend/src/App.test.tsx"
      to: "frontend/src/App.tsx inventoryRoute"
      via: "Vitest collects stub now; Plan 08 fills in redirect assertion"
    - from: "frontend/src/components/filaments/FilamentTypeCard.test.tsx"
      to: "frontend/src/components/filaments/FilamentTypeCard.tsx"
      via: "Vitest collects stub now; Plan 08 fills in rendering assertions"
---

<objective>
Create skeleton test stubs for all three test files that Phase 2 requires BEFORE any implementation begins. This satisfies the Nyquist rule: every task that produces code must have a test file ready to receive assertions.

Purpose: Plan 07 will extend test_filament_types_api.py; Plan 08 will extend App.test.tsx and FilamentTypeCard.test.tsx. Creating the stubs in Wave 0 ensures the test runner can discover and collect them throughout execution, giving continuous feedback from Wave 1 onward.
Output: Three test stub files; VALIDATION.md updated to mark Wave 0 complete.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-VALIDATION.md

<interfaces>
From backend/tests/integration/test_filament_types_api.py (existing file — if present):
  Check whether this file exists before writing. If it exists, APPEND the new class; do NOT overwrite the file.
  If it does not exist, create it from scratch with required imports.

Existing backend test pattern (from test_filament_types_api.py or nearest analog):
  - Imports: pytest, pytest.mark.asyncio, httpx.AsyncClient
  - Class-based: class TestXxx, docstring, @pytest.mark.asyncio on each async def test_...
  - Fixtures: client, auth_headers, test_filament_type, test_spool, unauthenticated_client

Frontend test pattern (Vitest + @testing-library/react):
  - Imports: import { describe, it, expect } from 'vitest'
  - Structure: describe('Component') { it('behavior', () => {}) }
  - For stubs: it.skip('behavior', () => {}) — skip so suite passes while stub is unfilled

VALIDATION.md frontmatter fields to update:
  - nyquist_compliant: false → true
  - wave_0_complete: false → true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend test stubs — TestFilamentTypeAggregatedEndpoints</name>
  <files>backend/tests/integration/test_filament_types_api.py</files>
  <read_first>
    - backend/tests/integration/test_filament_types_api.py (full file — check if it exists; read imports and existing class structure to match style; if file does not exist, note which imports are needed)
    - backend/tests/conftest.py (lines 370-430 — confirm fixture names: test_filament_type, test_spool, auth_headers, unauthenticated_client)
  </read_first>
  <action>
    Check whether backend/tests/integration/test_filament_types_api.py exists.

    If the file exists: APPEND the following block at the end of the file. Do NOT remove or reorder any existing content.

    If the file does not exist: Create it with the following content (including required imports).

    Content to append (or use as full file if new):

    When creating a new file, include these imports at the top:
      import pytest
      from httpx import AsyncClient

    Then add the class (whether appending or creating):

    Add a section divider: `# ============================================`

    Add the class:

    ```
    class TestFilamentTypeAggregatedEndpoints:
        """Integration tests for aggregated FilamentType list and spool sub-resource endpoints.

        NOTE: Stubs created in Wave 0 (Plan 00). Real test bodies filled in by Plan 07.
        Phase 1 prerequisite: verify Spool.filament_type_id FK is non-null for migrated spools
        before running these tests against a production-migrated database.
        """

        # ============================================
        # GET /api/v1/filament-types/aggregated
        # ============================================

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_aggregated_list_returns_200_with_counts(
            self, client: AsyncClient, auth_headers: dict, test_filament_type, test_spool
        ):
            """Aggregated list returns 200 with spool_count and labeled_count per row."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_aggregated_list_labeled_count_accuracy(
            self, client: AsyncClient, auth_headers: dict, test_filament_type, test_spool
        ):
            """labeled_count == 0 when test_spool.is_labeled is False."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_aggregated_filter_needs_labels(
            self, client: AsyncClient, auth_headers: dict, test_filament_type, test_spool
        ):
            """needs_labels=true filter returns only types with unlabeled spools."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_aggregated_filter_needs_sample(
            self, client: AsyncClient, auth_headers: dict, test_filament_type, test_spool
        ):
            """needs_sample=true filter returns only types without a sample."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_aggregated_requires_auth(self, unauthenticated_client: AsyncClient):
            """Aggregated list without auth returns 401 or 403."""
            ...

        # ============================================
        # GET /api/v1/filament-types/{id}/spools
        # ============================================

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_spools_sub_resource_returns_child_spools(
            self, client: AsyncClient, auth_headers: dict, test_filament_type, test_spool
        ):
            """Spools sub-resource returns list of spools with correct fields."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_spools_sub_resource_requires_auth(
            self, unauthenticated_client: AsyncClient, test_filament_type
        ):
            """Spools sub-resource without auth returns 401 or 403."""
            ...

        @pytest.mark.skip(reason="Wave 0 stub — filled in by Plan 07")
        @pytest.mark.asyncio
        async def test_spools_sub_resource_404_for_nonexistent(
            self, client: AsyncClient, auth_headers: dict
        ):
            """Spools sub-resource returns 404 for a random UUID not in this tenant."""
            ...
    ```

    Do NOT insert fenced code blocks in the actual file — write the Python source directly.
    Each stub method body is `...` (ellipsis).
    The `@pytest.mark.skip` decorator must appear BEFORE `@pytest.mark.asyncio` on each stub method.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_filament_types_api.py --collect-only -q 2>&1 | grep -E "TestFilamentTypeAggregated|error|Error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/tests/integration/test_filament_types_api.py
    - `poetry run pytest tests/integration/test_filament_types_api.py --collect-only -q` exits 0 (no collection errors)
    - Output shows 8 tests under TestFilamentTypeAggregatedEndpoints (listed as skip/deselected)
    - `poetry run ruff check tests/integration/test_filament_types_api.py` exits 0
    - Existing tests in the file (if any were present before) are NOT removed or broken
  </acceptance_criteria>
  <done>TestFilamentTypeAggregatedEndpoints class with 8 skip stubs present and collectible by pytest; ruff clean</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stubs — App.test.tsx and FilamentTypeCard.test.tsx; update VALIDATION.md</name>
  <files>frontend/src/App.test.tsx, frontend/src/components/filaments/FilamentTypeCard.test.tsx, .planning/phases/02-consolidated-list-view/02-VALIDATION.md</files>
  <read_first>
    - frontend/vitest.config.ts (full file — confirm test environment: jsdom, globals flag, setup file path)
    - frontend/src/App.test.tsx (check if file already exists — if so, read before modifying)
    - frontend/src/components/filaments/FilamentTypeCard.test.tsx (check if file exists)
    - .planning/phases/02-consolidated-list-view/02-VALIDATION.md (read frontmatter — need to update wave_0_complete and nyquist_compliant)
  </read_first>
  <action>
    Create frontend/src/App.test.tsx (or verify it does not exist first):

    ```typescript
    /**
     * App route configuration tests.
     * Wave 0 stubs — real assertions filled in by Plan 08.
     */
    import { describe, it } from 'vitest'

    describe('App routing — inventory redirect', () => {
      it.skip('navigating to /inventory redirects to /filaments', () => {
        // Wave 0 stub: Plan 08 Task 1 fills in redirect assertion using TanStack Router test utilities
      })

      it.skip('filamentsRoute renders FilamentLibrary component', () => {
        // Wave 0 stub: Plan 08 Task 1 fills in component render assertion
      })
    })
    ```

    Create frontend/src/components/filaments/FilamentTypeCard.test.tsx:
    First ensure the directory frontend/src/components/filaments/ exists (it may not yet — create the test file directly; Vitest does not require the component file to exist for stubs to collect).

    ```typescript
    /**
     * FilamentTypeCard component tests.
     * Wave 0 stubs — real assertions filled in by Plan 08.
     */
    import { describe, it } from 'vitest'

    describe('FilamentTypeCard', () => {
      it.skip('renders brand and color name', () => {
        // Wave 0 stub: Plan 08 fills in render assertion
      })

      it.skip('renders material type name', () => {
        // Wave 0 stub
      })

      it.skip('renders spool count badge', () => {
        // Wave 0 stub
      })

      it.skip('renders labeled count badge with partial status', () => {
        // Wave 0 stub
      })

      it.skip('renders No sample badge when has_sample is false', () => {
        // Wave 0 stub
      })

      it.skip('renders Sample checkmark badge when has_sample is true', () => {
        // Wave 0 stub
      })

      it.skip('clicking card calls onRowClick with filamentType id', () => {
        // Wave 0 stub
      })

      it.skip('clicking has_sample toggle calls onToggleSample but not onRowClick (stopPropagation)', () => {
        // Wave 0 stub
      })

      it.skip('renders color swatch when color_hex is present', () => {
        // Wave 0 stub
      })

      it.skip('does not render color swatch when color_hex is absent', () => {
        // Wave 0 stub
      })
    })
    ```

    Update .planning/phases/02-consolidated-list-view/02-VALIDATION.md:
    In the YAML frontmatter at the top of the file, change:
      `nyquist_compliant: false` → `nyquist_compliant: true`
      `wave_0_complete: false` → `wave_0_complete: true`
    Do not change any other content in the file.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm vitest run --reporter=verbose src/App.test.tsx src/components/filaments/FilamentTypeCard.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/App.test.tsx exists with a describe block containing 2 it.skip stubs
    - frontend/src/components/filaments/FilamentTypeCard.test.tsx exists with a describe block containing 10 it.skip stubs
    - `pnpm vitest run src/App.test.tsx src/components/filaments/FilamentTypeCard.test.tsx` exits 0 (skipped tests do not fail)
    - VALIDATION.md frontmatter shows `nyquist_compliant: true` and `wave_0_complete: true`
    - `pnpm tsc --noEmit` exits 0 (stub files are valid TypeScript)
  </acceptance_criteria>
  <done>Both frontend stub files collectible by Vitest; VALIDATION.md frontmatter updated to mark Wave 0 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → CI pipeline | Skipped tests must not be treated as passing tests for coverage purposes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00 | Tampering | Stub test class appended to existing test file | mitigate | Read existing file before appending — preserve all existing tests; grep confirms existing class count unchanged |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd backend && poetry run pytest tests/integration/test_filament_types_api.py --collect-only -q` — exits 0, no collection errors
2. `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -q` — exits 0 (stubs are skipped, not failed)
3. `cd frontend && pnpm vitest run src/App.test.tsx src/components/filaments/FilamentTypeCard.test.tsx` — exits 0
4. `grep "wave_0_complete: true" .planning/phases/02-consolidated-list-view/02-VALIDATION.md` — present
5. `grep "nyquist_compliant: true" .planning/phases/02-consolidated-list-view/02-VALIDATION.md` — present
</verification>

<success_criteria>
- All three stub test files exist and are discoverable by their respective test runners
- pytest collects 8 stubs under TestFilamentTypeAggregatedEndpoints with zero errors
- Vitest collects 2 stubs in App.test.tsx and 10 stubs in FilamentTypeCard.test.tsx with zero errors
- VALIDATION.md frontmatter updated
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-00-SUMMARY.md` when done
</output>
