---
phase: 02-consolidated-list-view
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - backend/app/api/v1/filament_types.py
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "GET /api/v1/filament-types/aggregated returns HTTP 200 with spool_count and labeled_count per FilamentType row"
    - "GET /api/v1/filament-types/aggregated requires authentication (returns 401/403 without token)"
    - "GET /api/v1/filament-types/aggregated filters correctly by brand, color, material_type_id, needs_labels, needs_sample params"
    - "GET /api/v1/filament-types/{id}/spools returns HTTP 200 with list of SpoolInSheetResponse objects"
    - "GET /api/v1/filament-types/{id}/spools returns HTTP 404 when filament_type_id does not belong to tenant"
    - "Both new endpoints are tenant-scoped — no cross-tenant data leakage"
  artifacts:
    - path: "backend/app/api/v1/filament_types.py"
      provides: "Two new endpoints: GET /aggregated and GET /{id}/spools"
      exports:
        - "list_filament_types_aggregated"
        - "list_spools_for_filament_type"
  key_links:
    - from: "GET /api/v1/filament-types/aggregated"
      to: "FilamentType + Spool tables"
      via: "outerjoin + func.count(case(...)) aggregation"
    - from: "GET /api/v1/filament-types/{id}/spools"
      to: "Spool table"
      via: "WHERE filament_type_id AND tenant_id"
---

<objective>
Add two new FastAPI endpoints to the existing filament_types router: an aggregated list endpoint that returns FilamentType rows with pre-computed spool counts, and a spools sub-resource endpoint that returns child Spool records for the drill-down sheet.

Purpose: Provides the backend data layer for LIST-01 (grouped aggregation), LIST-02 (labeled/sample status), LIST-03 (filters), and D-10/D-12 (spool sheet fetch). No frontend work — this plan is purely server-side.
Output: filament_types.py extended with two new route handlers and updated imports.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-RESEARCH.md

<interfaces>
From backend/app/api/v1/filament_types.py (read before editing):

Router prefix registered in main.py at /api/v1/filament-types (line 243).
Existing imports: select, func from sqlalchemy; CurrentUser, CurrentTenant, TenantDB from auth.dependencies;
  FilamentType model; FilamentTypeCreate/Update/Response/ListResponse from schemas.filament_type.
Existing endpoints: GET / (list), POST / (create), GET /{id} (get), PUT /{id} (update), DELETE /{id} (delete).

CRITICAL ordering rule: /aggregated (static path) MUST be placed in the file BEFORE /{filament_type_id} (dynamic path).
/{filament_type_id}/spools MUST be placed BEFORE /{filament_type_id} as well (static sub-path takes priority).
FastAPI matches routes in registration order — static routes first avoids the dynamic param matching "/aggregated" as an id.

From backend/app/models/ (for imports in new endpoints):
  FilamentType model: fields include id (UUID), brand (str), color (str), color_hex (Optional[str]),
    has_sample (bool), material_type_id (UUID FK), tenant_id (UUID)
  Spool model (backend/app/models/spool.py): fields include id (UUID), spool_id (str),
    filament_type_id (UUID FK), current_weight (float), initial_weight (float),
    is_labeled (bool), is_active (bool), tenant_id (UUID)
  MaterialType model (backend/app/models/material.py — verify exact path): fields include id, name, code

Auth dependencies (CurrentUser, CurrentTenant, TenantDB) already imported — use same pattern as existing endpoints.

New schema imports needed from app.schemas.filament_type (added in Plan 02):
  FilamentTypeAggregatedListResponse, FilamentTypeAggregatedResponse, SpoolInSheetResponse
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aggregated list endpoint GET /aggregated</name>
  <files>backend/app/api/v1/filament_types.py</files>
  <read_first>
    - backend/app/api/v1/filament_types.py (full file — understand current imports, endpoint order, existing list endpoint pattern on lines ~68-116)
    - backend/app/models/spool.py (check exact field names: filament_type_id, is_labeled, is_active, current_weight, initial_weight, spool_id)
    - backend/app/models/filament_type.py (confirm has_sample, color_hex, material_type_id field names)
    - backend/app/models/material.py or backend/app/models/ (locate MaterialType model — check exact module path for import)
  </read_first>
  <action>
    Add the following to filament_types.py. Insert the new endpoint BEFORE the existing `GET /{filament_type_id}` route handler (around line 119).

    1. Update imports at top of file:
       - Add `case` to the existing SQLAlchemy import (if not present): `from sqlalchemy import case, func, select`
       - Add `Spool` import from the correct models module path
       - Add `FilamentTypeAggregatedListResponse, FilamentTypeAggregatedResponse, SpoolInSheetResponse` to the schema import
       - Add `Optional, UUID` to typing imports if not already present
       - Add `Query` from fastapi if not present

    2. Add the aggregated list endpoint function `list_filament_types_aggregated`:
       - Route: `@router.get("/aggregated", response_model=FilamentTypeAggregatedListResponse)`
       - Parameters: user (CurrentUser), tenant (CurrentTenant), db (TenantDB), page (int = Query(1, ge=1)), page_size (int = Query(20, ge=1, le=100)), brand (Optional[str] = Query(None)), color (Optional[str] = Query(None)), material_type_id (Optional[UUID] = Query(None)), needs_labels (Optional[bool] = Query(None)), needs_sample (Optional[bool] = Query(None))
       - Docstring: "Aggregated FilamentType list with spool/labeled counts for the list view."

       Build the base query using select() with these columns:
         FilamentType.id, FilamentType.brand, FilamentType.color, FilamentType.color_hex,
         FilamentType.has_sample, MaterialType.name.label("material_type_name"),
         MaterialType.code.label("material_type_code"),
         func.count(Spool.id).label("spool_count"),
         func.count(case((Spool.is_labeled == True, Spool.id))).label("labeled_count")

       Joins: .outerjoin(Spool, (Spool.filament_type_id == FilamentType.id) & (Spool.tenant_id == tenant.id))
              .outerjoin(MaterialType, MaterialType.id == FilamentType.material_type_id)

       Where: FilamentType.tenant_id == tenant.id

       Group by: FilamentType.id, FilamentType.brand, FilamentType.color, FilamentType.color_hex,
                 FilamentType.has_sample, MaterialType.name, MaterialType.code

       Apply optional filters after base query construction:
         - brand: query = query.where(FilamentType.brand.ilike(f"%{brand}%"))
         - color: query = query.where(FilamentType.color.ilike(f"%{color}%"))
         - material_type_id: query = query.where(FilamentType.material_type_id == material_type_id)
         - needs_sample: query = query.where(FilamentType.has_sample == False)
         - needs_labels: use a subquery — add a HAVING clause: query = query.having(func.count(case((Spool.is_labeled == False, Spool.id))) > 0)

       Count total before pagination: wrap query in subquery, select func.count() from it.
       Paginate: offset = (page - 1) * page_size, add .offset(offset).limit(page_size).order_by(FilamentType.brand, FilamentType.color)

       Execute and build response:
         rows = result.mappings().all()
         return FilamentTypeAggregatedListResponse(
             total=total,
             filament_types=[FilamentTypeAggregatedResponse(**dict(row)) for row in rows],
             page=page,
             page_size=page_size,
         )

       Use `logger.info(f"Listed {total} filament types aggregated for tenant {tenant.id}")`.

       Import MaterialType locally inside the function if needed to avoid circular imports — check whether it is already imported at module level. Pattern: `from app.models.material import MaterialType` (verify exact path from models directory).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run python -c "from app.api.v1.filament_types import list_filament_types_aggregated; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import check above prints "OK"
    - `poetry run ruff check app/api/v1/filament_types.py` exits 0
    - The string "/aggregated" appears in filament_types.py before the string "/{filament_type_id}" (route ordering preserved)
    - func.count and case are present in the aggregation query
    - All five filter conditions (brand, color, material_type_id, needs_sample, needs_labels) are applied conditionally
  </acceptance_criteria>
  <done>GET /api/v1/filament-types/aggregated endpoint defined with correct aggregation query, filters, pagination, and auth dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Add spools sub-resource endpoint GET /{id}/spools</name>
  <files>backend/app/api/v1/filament_types.py</files>
  <read_first>
    - backend/app/api/v1/filament_types.py (current state after Task 1 — read to find correct insertion point)
  </read_first>
  <action>
    Insert the `list_spools_for_filament_type` endpoint AFTER the `/aggregated` endpoint but BEFORE the `/{filament_type_id}` GET route.

    Route: `@router.get("/{filament_type_id}/spools", response_model=list[SpoolInSheetResponse])`
    Parameters: filament_type_id (UUID, path param), user (CurrentUser), tenant (CurrentTenant), db (TenantDB)
    Docstring: "List child spools for a FilamentType. Used by the read-only spool drill-down sheet."

    Implementation:
    1. Authorization check — verify FilamentType exists and belongs to tenant:
       ft_result = await db.execute(select(FilamentType).where(FilamentType.id == filament_type_id, FilamentType.tenant_id == tenant.id))
       ft = ft_result.scalar_one_or_none()
       if not ft: raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=f"FilamentType {filament_type_id} not found")

    2. Fetch spools:
       result = await db.execute(select(Spool).where(Spool.filament_type_id == filament_type_id, Spool.tenant_id == tenant.id).order_by(Spool.spool_id))
       spools = result.scalars().all()

    3. Log and return:
       logger.info(f"Listed {len(spools)} spools for FilamentType {filament_type_id}")
       return [SpoolInSheetResponse.model_validate(s) for s in spools]

    Both tenant_id conditions are explicit (defense-in-depth against RLS misconfiguration).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run ruff check app/api/v1/filament_types.py && poetry run python -c "from app.api.v1.filament_types import list_spools_for_filament_type; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both imports print "OK"
    - `ruff check` exits 0
    - "/{filament_type_id}/spools" appears in the file before "/{filament_type_id}" route with `@router.get` (ordering correct)
    - HTTPException with 404 is present in the function body
    - Both `Spool.filament_type_id == filament_type_id` and `Spool.tenant_id == tenant.id` appear in the Spool query (both conditions explicit)
    - `SpoolInSheetResponse.model_validate` is used for the return mapping
  </acceptance_criteria>
  <done>GET /api/v1/filament-types/{id}/spools endpoint defined with 404 guard, tenant isolation, and ordered-by spool_id result</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI router | Query params (brand, color, UUIDs, booleans) are external input |
| FastAPI handler → PostgreSQL | SQL query must scope to tenant — trust boundary for multi-tenant isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | GET /aggregated | mitigate | FilamentType.tenant_id == tenant.id in WHERE clause; TenantDB sets RLS SET LOCAL; both layers enforce isolation |
| T-02-06 | Information Disclosure | GET /{id}/spools | mitigate | FilamentType ownership checked first (404 if not owner); Spool.tenant_id == tenant.id explicit in Spool query — both conditions required |
| T-02-07 | Tampering | brand/color filter params | mitigate | ilike() with parameterized values — SQLAlchemy prevents SQL injection; no raw string interpolation into SQL |
| T-02-08 | Tampering | material_type_id, filament_type_id path param | mitigate | UUID type annotation causes FastAPI to return 422 for non-UUID values before handler runs |
| T-02-09 | Elevation of Privilege | both new endpoints | mitigate | CurrentUser dependency validates JWT; CurrentTenant resolves tenant from token — same auth chain as all existing endpoints |
| T-02-SC | Tampering | pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd backend && poetry run ruff check app/api/v1/filament_types.py` — exits 0
2. `cd backend && poetry run python -c "from app.api.v1.filament_types import list_filament_types_aggregated, list_spools_for_filament_type; print('OK')"` — prints OK
3. `grep -n "router.get" backend/app/api/v1/filament_types.py` — /aggregated line appears before /{filament_type_id} lines
4. `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -x -q` — existing tests still pass (new endpoints not yet tested by Plan 03)
</verification>

<success_criteria>
- GET /api/v1/filament-types/aggregated endpoint registered and importable
- GET /api/v1/filament-types/{id}/spools endpoint registered and importable
- Route ordering: /aggregated before /{filament_type_id}; /{id}/spools before /{filament_type_id}
- Both endpoints require auth (CurrentUser + CurrentTenant)
- Both endpoints scope queries to tenant.id explicitly
- ruff passes; existing filament_types tests still green
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-03-SUMMARY.md` when done
</output>
