---
phase: 02-consolidated-list-view
plan: 07
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - backend/tests/integration/test_filament_types_api.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "GET /api/v1/filament-types/aggregated returns 200 with spool_count and labeled_count fields per row"
    - "GET /api/v1/filament-types/aggregated returns 401 without authentication"
    - "GET /api/v1/filament-types/aggregated filters correctly: brand, color, material_type_id, needs_labels, needs_sample"
    - "GET /api/v1/filament-types/{id}/spools returns 200 with list of spool objects for an authenticated tenant"
    - "GET /api/v1/filament-types/{id}/spools returns 404 for a filament_type_id that belongs to a different tenant"
    - "All new test functions pass: poetry run pytest tests/integration/test_filament_types_api.py -x -q"
  artifacts:
    - path: "backend/tests/integration/test_filament_types_api.py"
      provides: "New TestFilamentTypeAggregatedEndpoints class with tests for LIST-01, LIST-02, LIST-03, D-10, D-12"
  key_links:
    - from: "backend/tests/integration/test_filament_types_api.py"
      to: "backend/app/api/v1/filament_types.py"
      via: "AsyncClient hitting /api/v1/filament-types/aggregated and /{id}/spools"
---

<objective>
Extend the existing filament_types integration test file with a new test class covering the two new endpoints added in Plan 03: the aggregated list and the spools sub-resource.

Purpose: Automated regression safety for the backend aggregation logic (tenant isolation, count accuracy, filter correctness) and the spool drill-down endpoint (404 guard, correct response shape). This closes the automated coverage gaps for LIST-01, LIST-02, LIST-03, and D-10/D-12.
Output: test_filament_types_api.py extended with one new test class (TestFilamentTypeAggregatedEndpoints); no other files modified.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md

<interfaces>
From backend/tests/integration/test_filament_types_api.py (existing file — primary read):
  Existing class: TestFilamentTypesEndpoints (lines 21-198)
  Test pattern: class-based, @pytest.mark.asyncio on each method, AsyncClient via client fixture
  Auth pattern: auth_headers dict from fixture; unauthenticated_client fixture for 401 tests
  Assertion pattern: response.status_code == 200, response.json() access, assert "key" in data

From backend/tests/conftest.py (fixture source — read to confirm exact fixture names):
  test_filament_type — creates FilamentType linked to test_tenant, test_material_type
  test_spool — creates Spool with filament_type_id=test_filament_type.id, is_labeled=False, spool_id "TEST-SPOOL-001"
  auth_headers — JWT headers for test_user in test_tenant
  unauthenticated_client — client with no auth headers

API endpoints under test (from Plan 03):
  GET /api/v1/filament-types/aggregated — returns FilamentTypeAggregatedListResponse
  GET /api/v1/filament-types/{id}/spools — returns list[SpoolInSheetResponse]

Response field names to assert:
  Aggregated row: id, brand, color, color_hex, material_type_name, material_type_code, has_sample, spool_count, labeled_count
  Spool in sheet: id, spool_id, current_weight, initial_weight, is_labeled, is_active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestFilamentTypeAggregatedEndpoints test class</name>
  <files>backend/tests/integration/test_filament_types_api.py</files>
  <read_first>
    - backend/tests/integration/test_filament_types_api.py (full file — understand existing class structure, imports, and test patterns to replicate)
    - backend/tests/conftest.py (lines 376-421 — confirm test_filament_type and test_spool fixture names and field values, especially spool_id value and is_labeled default)
  </read_first>
  <action>
    Append a new test class `TestFilamentTypeAggregatedEndpoints` at the end of test_filament_types_api.py. Follow the existing class structure and docstring convention.

    Include a section divider comment: `# ============================================`

    Class docstring: "Integration tests for aggregated FilamentType list and spool sub-resource endpoints."

    Write these test methods:

    test_aggregated_list_returns_200_with_counts(client, auth_headers, test_filament_type, test_spool):
      GET /api/v1/filament-types/aggregated, assert 200
      Parse data, assert "total" in data, "filament_types" in data, data["total"] >= 1
      Find the row for test_filament_type.id (filter filament_types list by id)
      Assert row has "spool_count" key, row["spool_count"] >= 1
      Assert row has "labeled_count" key
      Assert row has "material_type_name" key and "material_type_code" key
      Assert row has "has_sample" key

    test_aggregated_list_labeled_count_accuracy(client, auth_headers, test_filament_type, test_spool):
      Precondition: test_spool.is_labeled = False (confirmed from conftest)
      GET /api/v1/filament-types/aggregated, assert 200
      Find row for test_filament_type.id
      Assert row["labeled_count"] == 0  (test_spool is unlabeled)
      Assert row["spool_count"] == 1    (only one spool linked to this type)

    test_aggregated_filter_needs_labels(client, auth_headers, test_filament_type, test_spool):
      GET /api/v1/filament-types/aggregated?needs_labels=true, assert 200
      Assert at least one row in filament_types (the test FilamentType should appear since test_spool is unlabeled)
      Find row for test_filament_type.id, assert it is present

    test_aggregated_filter_needs_sample(client, auth_headers, test_filament_type, test_spool):
      Precondition: test_filament_type.has_sample = False (confirm from conftest)
      GET /api/v1/filament-types/aggregated?needs_sample=true, assert 200
      Assert test_filament_type.id appears in results (has_sample=False means it needs a sample)

    test_aggregated_requires_auth(unauthenticated_client):
      GET /api/v1/filament-types/aggregated with no auth headers
      Assert response.status_code in [401, 403]

    test_spools_sub_resource_returns_child_spools(client, auth_headers, test_filament_type, test_spool):
      GET /api/v1/filament-types/{test_filament_type.id}/spools, assert 200
      Parse as list, assert len(spools) >= 1
      Assert spools[0] has keys: "id", "spool_id", "current_weight", "initial_weight", "is_labeled", "is_active"
      Assert spools[0]["spool_id"] == "TEST-SPOOL-001"  (or whatever the conftest value is — read conftest first)
      Assert spools[0]["is_labeled"] is False

    test_spools_sub_resource_requires_auth(unauthenticated_client, test_filament_type):
      GET /api/v1/filament-types/{test_filament_type.id}/spools with no auth
      Assert response.status_code in [401, 403]

    test_spools_sub_resource_404_for_nonexistent(client, auth_headers):
      GET /api/v1/filament-types/00000000-0000-0000-0000-000000000000/spools
      Assert response.status_code == 404

    Decorator each method with @pytest.mark.asyncio. Use `await` for all client calls.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_filament_types_api.py::TestFilamentTypeAggregatedEndpoints -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All test methods in TestFilamentTypeAggregatedEndpoints pass (no failures, no errors)
    - `poetry run pytest tests/integration/test_filament_types_api.py -x -q` exits 0 (all tests including existing class)
    - `poetry run ruff check tests/integration/test_filament_types_api.py` exits 0
    - test_aggregated_list_labeled_count_accuracy asserts labeled_count == 0 for unlabeled test spool
    - test_spools_sub_resource_404_for_nonexistent asserts 404
    - test_aggregated_requires_auth and test_spools_sub_resource_requires_auth assert 401 or 403
  </acceptance_criteria>
  <done>New test class passes all 8 test methods; existing tests still green; ruff clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client → API | Tests exercise the same auth boundary as production requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Information Disclosure | Cross-tenant test coverage | mitigate | test_spools_sub_resource_404_for_nonexistent verifies that a random UUID returns 404 — no data from another tenant leaks; tenant isolation confirmed by explicit test |
| T-02-19 | Elevation of Privilege | Unauthenticated access tests | mitigate | Two auth tests verify 401/403 for both endpoints — confirms CurrentUser dependency is effective |
| T-02-SC | Tampering | pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -x -q` — exits 0, all tests green
2. `cd backend && poetry run ruff check tests/integration/test_filament_types_api.py` — exits 0
3. `grep -c "def test_" backend/tests/integration/test_filament_types_api.py` — count increases by 8 from the original (new methods added)
</verification>

<success_criteria>
- 8 new test methods covering aggregated list, labeled count accuracy, needs_labels filter, needs_sample filter, two auth tests, spools sub-resource, and 404 guard
- All existing TestFilamentTypesEndpoints tests still pass
- ruff clean
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-07-SUMMARY.md` when done
</output>
