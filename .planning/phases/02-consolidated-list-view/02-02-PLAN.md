---
phase: 02-consolidated-list-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/filament_type.py
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "FilamentTypeAggregatedResponse schema exists with id, brand, color, color_hex, material_type_name, material_type_code, has_sample, spool_count, labeled_count fields"
    - "FilamentTypeAggregatedListResponse schema exists with total, filament_types, page, page_size fields"
    - "SpoolInSheetResponse schema exists with id, spool_id, current_weight, initial_weight, is_labeled, is_active fields"
    - "All three new schemas are exported from the schemas module for use in the endpoint plan"
  artifacts:
    - path: "backend/app/schemas/filament_type.py"
      provides: "Three new Pydantic schemas for aggregated list view and spool sheet"
      contains: "FilamentTypeAggregatedResponse, FilamentTypeAggregatedListResponse, SpoolInSheetResponse"
  key_links:
    - from: "backend/app/schemas/filament_type.py"
      to: "backend/app/api/v1/filament_types.py"
      via: "imported in Plan 03 for new endpoint response_model annotations"
---

<objective>
Add three Pydantic schemas to the filament_type schema module: a slim aggregated response for the list view (with spool counts), a paginated wrapper, and a minimal spool record for the read-only sheet.

Purpose: These schemas are the contracts the backend aggregation endpoints (Plan 03) and the frontend types (Plan 04) are built against. Defining them first enables Plan 03 and Plan 04 to proceed in Wave 2 in parallel.
Output: backend/app/schemas/filament_type.py extended with three new classes; no other files touched.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-PATTERNS.md

<interfaces>
From backend/app/schemas/filament_type.py (existing schemas — read before editing):

Existing pattern (lines ~80-102):
  class FilamentTypeResponse(FilamentTypeBase):
      id: UUID
      tenant_id: UUID
      created_at: datetime
      updated_at: datetime
      material_type_code: str
      material_type_name: str
      model_config = ConfigDict(from_attributes=True)

  class FilamentTypeListResponse(BaseModel):
      total: int
      filament_types: list[FilamentTypeResponse]
      page: int
      page_size: int

Imports already present: UUID, Optional, BaseModel, ConfigDict, Field, datetime.

New schemas to add (append after FilamentTypeListResponse, before any module-level code):
  class FilamentTypeAggregatedResponse(BaseModel) — from_attributes=False (built from mappings())
  class FilamentTypeAggregatedListResponse(BaseModel) — paginated wrapper
  class SpoolInSheetResponse(BaseModel) — from_attributes=True (built from ORM Spool objects)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FilamentTypeAggregatedResponse, FilamentTypeAggregatedListResponse, and SpoolInSheetResponse schemas</name>
  <files>backend/app/schemas/filament_type.py</files>
  <read_first>
    - backend/app/schemas/filament_type.py (full file — understand current imports and existing class structure before appending)
  </read_first>
  <action>
    Read the full filament_type.py schema file. Append three new classes after the last existing class (FilamentTypeListResponse).

    FilamentTypeAggregatedResponse:
    - Inherits BaseModel (not FilamentTypeBase — spec fields excluded)
    - Fields: id (UUID), brand (str), color (str), color_hex (Optional[str] = None), material_type_name (str), material_type_code (str), has_sample (bool), spool_count (int), labeled_count (int)
    - model_config = ConfigDict(from_attributes=False) — because it is built from SQLAlchemy Row mappings, not ORM model instances
    - Docstring: "Slim aggregated response for list view — includes spool counts, excludes spec fields."

    FilamentTypeAggregatedListResponse:
    - Inherits BaseModel
    - Fields: total (int with Field description="Total number of filament types"), filament_types (list[FilamentTypeAggregatedResponse] with description="Aggregated list"), page (int = Field(1, ge=1, description="Current page number")), page_size (int = Field(20, ge=1, le=100, description="Items per page"))
    - No model_config needed
    - Docstring: "Paginated aggregated FilamentType list for the list view."

    SpoolInSheetResponse:
    - Inherits BaseModel
    - Fields: id (UUID), spool_id (str), current_weight (float), initial_weight (float), is_labeled (bool), is_active (bool)
    - model_config = ConfigDict(from_attributes=True) — built from ORM Spool instances via model_validate()
    - Docstring: "Minimal spool info for the read-only spool drill-down sheet."

    Do not change any existing classes or imports. If Optional is not already imported, add it to the existing import line (likely already present — check before adding).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run python -c "from app.schemas.filament_type import FilamentTypeAggregatedResponse, FilamentTypeAggregatedListResponse, SpoolInSheetResponse; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import statement above succeeds (prints "OK")
    - `poetry run ruff check app/schemas/filament_type.py` exits 0
    - FilamentTypeAggregatedResponse has ConfigDict(from_attributes=False)
    - SpoolInSheetResponse has ConfigDict(from_attributes=True)
    - FilamentTypeAggregatedListResponse has field filament_types typed as list[FilamentTypeAggregatedResponse]
    - All three class names appear in filament_type.py (grep confirms)
  </acceptance_criteria>
  <done>Three new Pydantic schemas added and importable; ruff clean; ready for Plan 03 endpoint annotations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Pydantic schema | Schema validates data shape before it reaches the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | FilamentTypeAggregatedResponse | mitigate | Schema excludes spec fields (temps, diameter, finish) and tenant_id — only public-facing fields included; no internal IDs beyond the primary key |
| T-02-04 | Information Disclosure | SpoolInSheetResponse | mitigate | Schema excludes storage_location, QR code raw data, and tenant_id — only operational status fields; full detail deferred to Phase 4 |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd backend && poetry run python -c "from app.schemas.filament_type import FilamentTypeAggregatedResponse, FilamentTypeAggregatedListResponse, SpoolInSheetResponse; print('OK')"` — prints OK
2. `cd backend && poetry run ruff check app/schemas/filament_type.py` — exits 0
3. `grep -c "FilamentTypeAggregatedResponse\|FilamentTypeAggregatedListResponse\|SpoolInSheetResponse" backend/app/schemas/filament_type.py` — returns 3 or more (class definitions)
</verification>

<success_criteria>
- All three schema classes importable from app.schemas.filament_type
- No ruff violations
- Existing tests still pass: `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -x -q`
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-02-SUMMARY.md` when done
</output>
