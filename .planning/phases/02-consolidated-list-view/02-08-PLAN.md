---
phase: 02-consolidated-list-view
plan: 08
type: execute
wave: 5
depends_on:
  - 02-05
  - 02-06
files_modified:
  - frontend/src/App.test.tsx
  - frontend/src/components/filaments/FilamentTypeCard.test.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - "App.test.tsx verifies that navigating to /inventory redirects to /filaments"
    - "FilamentTypeCard.test.tsx verifies that the card renders brand, color, spool count, labeled count, and sample badges correctly"
    - "FilamentTypeCard.test.tsx verifies that clicking the has_sample toggle calls onToggleSample and does NOT call onRowClick"
    - "All new frontend tests pass: pnpm test --run"
  artifacts:
    - path: "frontend/src/App.test.tsx"
      provides: "Route redirect verification for NAV-01/NAV-02"
    - path: "frontend/src/components/filaments/FilamentTypeCard.test.tsx"
      provides: "Unit tests for FilamentTypeCard props, rendering, and interaction"
  key_links:
    - from: "frontend/src/App.test.tsx"
      to: "frontend/src/App.tsx inventoryRoute"
      via: "RouterProvider test render — asserts /inventory renders Navigate to /filaments"
    - from: "frontend/src/components/filaments/FilamentTypeCard.test.tsx"
      to: "frontend/src/components/filaments/FilamentTypeCard.tsx"
      via: "component render tests"
---

<objective>
Create two frontend test files: App.test.tsx for route redirect verification and FilamentTypeCard.test.tsx for the mobile card component. These cover the NAV-01/NAV-02 redirect and the core rendering/interaction behaviors of the list view.

Purpose: Closes the automated test gaps identified in VALIDATION.md. The redirect test is the only automated proof that /inventory → /filaments works without a browser. The card test covers stopPropagation, badge variants, and color swatch rendering.
Output: Two new test files; no production files modified.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/02-consolidated-list-view/02-VALIDATION.md

<interfaces>
From frontend/src/pages/Settings.test.tsx or frontend/src/lib/api/modules.test.ts (test pattern reference — read to confirm test framework setup):
  Vitest test pattern: import { describe, it, expect, vi, beforeEach } from 'vitest'
  React Testing Library: import { render, screen, fireEvent } from '@testing-library/react'
  Mock pattern: vi.fn() for callback props

From frontend/src/App.tsx (route tree — needed to understand how to render the router in tests):
  The router is a TanStack Router v1.141 RouterProvider. Testing redirects requires either:
  - MemoryRouter equivalent from @tanstack/react-router
  - Or asserting that the component registered in inventoryRoute renders Navigate

From frontend/src/types/filament-type.ts (Plan 04):
  FilamentTypeListItem — used to construct test fixtures

From frontend/src/components/filaments/FilamentTypeCard.tsx (Plan 05):
  Props: { filamentType: FilamentTypeListItem; onRowClick: (id: string) => void; onToggleSample: (id: string, hasSample: boolean) => void; isTogglingId?: string | null }

Frontend test infrastructure:
  Framework: Vitest 4.x + @testing-library/react
  Config: frontend/vitest.config.ts
  Quick run: cd frontend && pnpm test --run src/components/filaments/FilamentTypeCard.test.tsx
  Full suite: cd frontend && pnpm test --run
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App.test.tsx for route redirect verification</name>
  <files>frontend/src/App.test.tsx</files>
  <read_first>
    - frontend/src/App.tsx (lines 1-30 and inventoryRoute definition — understand the Navigate component used)
    - frontend/src/pages/Settings.test.tsx (if exists — verify test setup pattern: QueryClientProvider, providers needed)
    - frontend/vitest.config.ts (confirm jsdom environment, global test flag, setup file if any)
    - frontend/src/lib/api/modules.test.ts (if exists — secondary test pattern reference)
  </read_first>
  <action>
    Create frontend/src/App.test.tsx.

    The simplest reliable approach for TanStack Router v1.141 redirect testing is to test the route's component directly (not via RouterProvider, which requires complex memory-history setup). The inventoryRoute component is `() => <Navigate to="/filaments" />` — this is a pure component that renders Navigate.

    Test 1 — Verify inventoryRoute component renders Navigate:
    Import the route-level component directly by extracting it or by checking the router config. If direct import is not feasible, import the Navigate component from '@tanstack/react-router' and verify the inventoryRoute's options.component renders it.

    Alternative approach (simpler, more reliable): Import the App module and check that inventoryRoute has component pointing to Navigate — test the route configuration object rather than rendering it. Use: import { inventoryRoute } from './App' if exported, or test via grep-based assertion in acceptance criteria instead.

    Pragmatic approach given TanStack Router v1's internal structure: Write two tests in App.test.tsx:
    1. A configuration test: verify that the inventoryRoute path is '/inventory' and its component renders a Navigate element. Render the inventoryRoute.options.component() and check the output contains a redirect or Navigate behavior.
    2. A grep/smoke test: verify no navigation-context '/inventory' links remain in AppLayout by importing fallbackNavItems (if exported) or asserting the string.

    Fallback if App module structure prevents clean unit testing of routes: Write a single describe block with a skip or a comment explaining the constraint, then add the grep-based test as a shell assertion in the verify block.

    Regardless of approach, the test file MUST exist and MUST pass `pnpm test --run src/App.test.tsx`.

    Read the existing test files in the project first to determine the working test pattern before writing the file.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm test --run src/App.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/App.test.tsx
    - `pnpm test --run src/App.test.tsx` exits 0 (all tests pass)
    - At least one test verifies the /inventory → /filaments redirect behavior (either via component render or route config assertion)
    - No test is marked `.skip` or `.todo` without a comment explaining the constraint
  </acceptance_criteria>
  <done>App.test.tsx passes with at least one redirect-coverage test; no flaky behavior</done>
</task>

<task type="auto">
  <name>Task 2: Create FilamentTypeCard.test.tsx</name>
  <files>frontend/src/components/filaments/FilamentTypeCard.test.tsx</files>
  <read_first>
    - frontend/src/components/filaments/FilamentTypeCard.tsx (full file — props interface, badge rendering conditions, stopPropagation location, color swatch conditional rendering)
    - frontend/src/pages/Settings.test.tsx or any existing component test (full — verify renderWithQueryClient or wrapper pattern, vi.fn() usage)
    - frontend/vitest.config.ts (confirm globals: true, jsdom environment)
  </read_first>
  <action>
    Create frontend/src/components/filaments/FilamentTypeCard.test.tsx.

    Construct a test filamentType fixture matching FilamentTypeListItem:
      { id: 'ft-1', brand: 'JAYO', color: 'Black', color_hex: 'FF000000', material_type_name: 'PETG', material_type_code: 'PETG', has_sample: false, spool_count: 3, labeled_count: 1 }

    Write these tests in a describe('FilamentTypeCard') block:

    Test 1 — renders brand and color:
      Render FilamentTypeCard with fixture, mock onRowClick and onToggleSample with vi.fn()
      Assert screen.getByText('JAYO') present
      Assert screen.getByText('Black') present

    Test 2 — renders material type name:
      Assert screen.getByText('PETG') present (material_type_name)

    Test 3 — renders spool count badge with correct text:
      Assert screen.getByText('3 spools') present

    Test 4 — renders labeled count badge showing partial label status:
      labeled_count=1, spool_count=3 → assert screen.getByText('1/3 labeled') present

    Test 5 — renders "No sample" badge when has_sample=false:
      Assert screen.getByText('No sample') present

    Test 6 — renders "Sample ✓" badge when has_sample=true:
      Re-render with has_sample=true
      Assert screen.getByText('Sample ✓') present

    Test 7 — clicking the card calls onRowClick with the filamentType id:
      fireEvent.click the card container (not the toggle button)
      Assert onRowClick mock called with 'ft-1'

    Test 8 — clicking the has_sample toggle calls onToggleSample but NOT onRowClick (stopPropagation):
      fireEvent.click the toggle button (find by aria-label containing "sample")
      Assert onToggleSample mock called
      Assert onRowClick mock NOT called (verifies stopPropagation works)

    Test 9 — renders color swatch div when color_hex is present:
      Query the swatch element (it has aria-hidden="true" and a style background-color)
      Assert it is in the document

    Test 10 — does NOT render color swatch when color_hex is absent:
      Re-render with color_hex: null
      Assert no swatch div with aria-hidden="true" and background-color style

    Use the same wrapper pattern as existing component tests (QueryClientProvider if hooks used — FilamentTypeCard does not use hooks directly, so raw render may work; check if any context is needed).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && pnpm test --run src/components/filaments/FilamentTypeCard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/components/filaments/FilamentTypeCard.test.tsx
    - `pnpm test --run src/components/filaments/FilamentTypeCard.test.tsx` exits 0
    - Test 8 (stopPropagation) passes — onRowClick NOT called when toggle clicked
    - Tests 5 and 6 both pass (badge text changes with has_sample value)
    - Test 9 passes (swatch present with color_hex)
    - Test 10 passes (no swatch without color_hex)
    - `pnpm test --run` (full suite) exits 0
  </acceptance_criteria>
  <done>FilamentTypeCard tests pass; stopPropagation, badge variants, and color swatch conditional all verified; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → component behavior | vi.fn() mocks verify call signatures but not real API auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | Test fixtures contain realistic-looking UUIDs | accept | Test fixtures are isolated from production; no production data used in tests |
| T-02-21 | Information Disclosure | Test file committed with hardcoded test data | accept | Test data is synthetic (no real credentials, tenant IDs are test values) |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed this plan |
</threat_model>

<verification>
1. `cd frontend && pnpm test --run src/components/filaments/FilamentTypeCard.test.tsx` — exits 0
2. `cd frontend && pnpm test --run src/App.test.tsx` — exits 0
3. `cd frontend && pnpm test --run` — full suite exits 0
</verification>

<success_criteria>
- App.test.tsx: at least one test covering /inventory redirect behavior, passes
- FilamentTypeCard.test.tsx: 10 tests all passing; stopPropagation test is the critical one
- Full frontend test suite green
</success_criteria>

<output>
Create `.planning/phases/02-consolidated-list-view/02-08-SUMMARY.md` when done
</output>
