---
phase: 01-data-model-migration
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-04
  - 01-05
files_modified:
  - backend/tests/conftest.py
  - backend/tests/unit/test_filament_type_schemas.py
  - backend/tests/unit/test_spool_schemas.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "test_filament_type fixture exists in conftest.py and creates a valid FilamentType record"
    - "test_spool fixture uses test_filament_type dependency (not test_material_type directly)"
    - "Unit tests for FilamentType schemas cover all D-01 fields, validators, and has_sample default"
    - "Unit tests for Spool schemas verify filament_type_id required, is_labeled default False, and removal of old type fields"
    - "All schema unit tests pass: cd backend && poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py -x"
  artifacts:
    - path: backend/tests/conftest.py
      provides: test_filament_type fixture + updated test_spool fixture
      contains: "async def test_filament_type"
    - path: backend/tests/unit/test_filament_type_schemas.py
      provides: FilamentType schema unit tests
      contains: "class TestFilamentTypeBase"
    - path: backend/tests/unit/test_spool_schemas.py
      provides: Updated Spool schema unit tests (no brand/color, has filament_type_id, is_labeled)
      contains: "filament_type_id"
  key_links:
    - from: backend/tests/conftest.py
      to: backend/app/models/filament_type.py
      via: "test_filament_type fixture creates FilamentType ORM object"
      pattern: "from app.models.filament_type import FilamentType"
    - from: backend/tests/unit/test_filament_type_schemas.py
      to: backend/app/schemas/filament_type.py
      via: "imports FilamentTypeBase, FilamentTypeCreate, FilamentTypeUpdate, FilamentTypeResponse"
      pattern: "from app.schemas.filament_type import"
---

<objective>
Update conftest.py to add the test_filament_type fixture and update test_spool to use it, then write unit tests for FilamentType schemas and update spool schema unit tests to reflect the new two-tier model.

Purpose: Establish test infrastructure so all subsequent integration tests and Phase gate checks have working fixtures and schema verification.
Output: Updated conftest.py, new test_filament_type_schemas.py, updated test_spool_schemas.py.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-PATTERNS.md

<interfaces>
<!-- Key patterns from existing test infrastructure -->

From backend/tests/conftest.py — test_spool fixture to UPDATE (lines 375-395):
  @pytest_asyncio.fixture(scope="function")
  async def test_spool(db_session: AsyncSession, test_tenant: Tenant, test_material_type: MaterialType) -> Spool:
      spool = Spool(id=uuid4(), tenant_id=test_tenant.id, material_type_id=test_material_type.id,
          spool_id="TEST-SPOOL-001", brand="Test Brand", color="Red", ...)
  After update: depends on test_filament_type instead of test_material_type directly; uses filament_type_id instead of material_type_id/brand/color.

New test_filament_type fixture to ADD (after test_material_type at line ~373):
  @pytest_asyncio.fixture(scope="function")
  async def test_filament_type(db_session: AsyncSession, test_tenant: Tenant, test_material_type: MaterialType) -> "FilamentType":
      from app.models.filament_type import FilamentType
      ft = FilamentType(id=uuid4(), tenant_id=test_tenant.id, material_type_id=test_material_type.id,
          brand="Test Brand", color="Red", diameter=1.75, has_sample=False, translucent=False, glow=False)
      db_session.add(ft); await db_session.commit(); await db_session.refresh(ft)
      return ft

From backend/tests/unit/test_spool_schemas.py — current valid_spool() helper includes brand, color, material_type_id:
  After update: valid_spool() uses filament_type_id instead; removes brand/color/material_type_id.
  Tests for purchased_quantity/spools_remaining validators must be removed.
  New tests: test_filament_type_id_required, test_is_labeled_defaults_false.

From backend/tests/unit/test_filament_type_schemas.py (new file — mirrors test_spool_schemas.py structure):
  valid_filament_type() helper with brand, color, material_type_id=uuid4(), diameter=1.75
  TestFilamentTypeBase: test_valid_minimal, test_brand_empty_raises, test_color_empty_raises,
    test_diameter_zero_raises, test_diameter_negative_raises, test_diameter_above_max_raises,
    test_extruder_temp_below_min_raises, test_extruder_temp_max_400, test_bed_temp_negative_raises,
    test_has_sample_defaults_false (DATA-04), test_density_zero_raises, test_density_above_max_raises
  TestFilamentTypeCreate: test_inherits_from_base
  TestFilamentTypeUpdate: test_all_optional
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update conftest.py — add test_filament_type, update test_spool fixture</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py — read from line 350 to end to see test_material_type, test_spool, and subsequent fixtures; know exact insertion point
    - backend/app/models/filament_type.py — verify FilamentType constructor accepts brand, color, diameter, has_sample, translucent, glow
    - backend/app/models/spool.py — verify Spool constructor now uses filament_type_id (not material_type_id/brand/color)
  </read_first>
  <behavior>
    - test_filament_type fixture: function-scoped, depends on db_session/test_tenant/test_material_type; creates FilamentType with brand="Test Brand", color="Red", diameter=1.75, has_sample=False
    - test_spool fixture: updated to depend on test_filament_type (not test_material_type directly); constructs Spool with filament_type_id=test_filament_type.id (no brand, color, material_type_id)
    - is_labeled=False added to test_spool construction
  </behavior>
  <action>
    EDIT backend/tests/conftest.py:

    ADD test_filament_type fixture immediately after the test_material_type fixture (around line 373). Follow the exact @pytest_asyncio.fixture(scope="function") pattern from test_spool. Import FilamentType inside the fixture body (lazy import, matching existing pattern). The fixture must return a FilamentType instance.

    UPDATE test_spool fixture:
    - Change signature: test_spool(db_session: AsyncSession, test_tenant: Tenant, test_filament_type: "FilamentType") -> Spool  (remove test_material_type parameter)
    - Remove: material_type_id=test_material_type.id, brand="Test Brand", color="Red" from Spool constructor
    - Add: filament_type_id=test_filament_type.id, is_labeled=False
    - Update docstring to: "Create a test spool linked to test_filament_type."

    Do NOT add a top-level import of FilamentType — use the lazy import inside the fixture body (this matches how test_spool currently imports Spool fields).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/conftest.py --collect-only -q 2>&1 | grep -E "test_filament_type|test_spool|error" | head -10</automated>
  </verify>
  <done>
    - conftest.py has test_filament_type fixture (function-scoped, async, returns FilamentType)
    - test_spool fixture depends on test_filament_type, not test_material_type
    - test_spool constructs Spool with filament_type_id=test_filament_type.id
    - is_labeled=False in test_spool constructor
    - conftest.py parses without error: python -c "import tests.conftest" succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write FilamentType schema unit tests + update Spool schema unit tests</name>
  <files>
    backend/tests/unit/test_filament_type_schemas.py
    backend/tests/unit/test_spool_schemas.py
  </files>
  <read_first>
    - backend/tests/unit/test_spool_schemas.py — read fully; this is the template for test_filament_type_schemas.py and must be updated to remove brand/color tests and add filament_type_id/is_labeled tests
    - backend/app/schemas/filament_type.py — verify validator limits: brand min_length=1, max_length=100; color min_length=1, max_length=50; diameter gt=0, le=5; extruder_temp ge=150, le=400; bed_temp ge=0, le=150; density gt=0, le=10
    - backend/app/schemas/spool.py — verify current SpoolBase fields (filament_type_id required, is_labeled=False default)
  </read_first>
  <behavior>
    test_filament_type_schemas.py assertions:
    - valid minimal: FilamentTypeCreate(material_type_id=uuid4(), brand="X", color="Y", diameter=1.75) succeeds; has_sample is False
    - brand empty string raises ValidationError
    - color empty string raises ValidationError
    - diameter=0 raises; diameter=-1 raises; diameter=5.1 raises; diameter=5.0 accepted
    - extruder_temp=149 raises; extruder_temp=150 accepted; extruder_temp=400 accepted; extruder_temp=401 raises
    - bed_temp=-1 raises; bed_temp=0 accepted; bed_temp=150 accepted; bed_temp=151 raises
    - has_sample defaults False (DATA-04)
    - density=0 raises; density=10.1 raises; density=10.0 accepted
    - FilamentTypeUpdate() instantiates with no args (all optional)
    - FilamentTypeUpdate with partial fields does not raise

    test_spool_schemas.py changes:
    - Remove valid_spool() helper's brand, color, material_type_id fields; ADD filament_type_id=uuid4()
    - Remove tests: test_brand_empty_raises, test_color_empty_raises (fields removed from Spool)
    - Remove tests: test_purchased_quantity_zero_raises, test_purchased_quantity_positive, test_spools_remaining_zero_raises (fields removed)
    - ADD: test_filament_type_id_required — SpoolCreate without filament_type_id raises ValidationError
    - ADD: test_is_labeled_defaults_false — SpoolBase(filament_type_id=uuid4(), spool_id="X", initial_weight=1000, current_weight=800).is_labeled is False (DATA-05)
    - Keep all weight/price/temp validator tests that still apply
  </behavior>
  <action>
    CREATE backend/tests/unit/test_filament_type_schemas.py:
    - Module docstring: "Tests for FilamentType Pydantic schemas."
    - Imports: uuid4 from uuid, pytest, ValidationError from pydantic, FilamentTypeBase/FilamentTypeCreate/FilamentTypeUpdate from app.schemas.filament_type
    - valid_filament_type(**kwargs) helper with defaults: material_type_id=uuid4(), brand="Bambu Lab", color="Black", diameter=1.75
    - TestFilamentTypeBase class with all test methods listed in behavior block above
    - TestFilamentTypeCreate class: test_inherits_from_base (creates via FilamentTypeCreate(**valid_filament_type()))
    - TestFilamentTypeUpdate class: test_all_optional (instantiates FilamentTypeUpdate()), test_partial_update (sets brand only)

    EDIT backend/tests/unit/test_spool_schemas.py:
    - Update valid_spool() helper: remove material_type_id, brand, color; add filament_type_id=uuid4()
    - Remove the 5 tests identified in behavior block above
    - Add test_filament_type_id_required and test_is_labeled_defaults_false
    - Update any other test that directly references brand, color, or material_type_id on spool schemas
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tests/unit/test_filament_type_schemas.py exists with at least 12 test cases
    - test_has_sample_defaults_false passes (DATA-04)
    - tests/unit/test_spool_schemas.py updated: brand/color/purchased_quantity tests removed; filament_type_id and is_labeled tests added
    - test_is_labeled_defaults_false passes (DATA-05)
    - All tests in both files pass: poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py -x exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures | test_filament_type creates data in test DB; test_spool depends on it via FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering | Test isolation | accept | Each test uses function-scoped fixtures; DB rolls back between tests via existing conftest transaction pattern |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py -v --tb=short 2>&1 | tail -20
```
</verification>

<success_criteria>
- test_filament_type fixture in conftest.py; test_spool updated to use it
- test_filament_type_schemas.py exists with 12+ tests covering all D-01 field validators and has_sample default (DATA-04)
- test_spool_schemas.py updated: no brand/color tests, no purchased_quantity tests; test_filament_type_id_required and test_is_labeled_defaults_false pass (DATA-05)
- poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py -x exits 0
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-06-SUMMARY.md when done
</output>
