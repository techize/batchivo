---
phase: 01-data-model-migration
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/app/api/v1/filament_types.py
  - backend/app/modules/threed_print/filament_types.py
  - backend/app/modules/threed_print/__init__.py
  - backend/app/main.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "GET /api/v1/filament-types returns 200 with pagination for authenticated tenant"
    - "POST /api/v1/filament-types returns 201 with new FilamentType record"
    - "GET /api/v1/filament-types/{id} returns 404 for unknown ID"
    - "All FilamentType endpoints use TenantDB (RLS-enabled) — not Depends(get_db)"
    - "FilamentTypesModule is registered in threed_print and the router is mounted at /api/v1/filament-types"
  artifacts:
    - path: backend/app/api/v1/filament_types.py
      provides: FilamentType CRUD endpoints
      exports:
        - router (APIRouter with POST, GET list, GET by ID, PUT, DELETE)
    - path: backend/app/modules/threed_print/filament_types.py
      provides: FilamentTypesModule (BaseModule subclass)
      contains: "class FilamentTypesModule"
    - path: backend/app/modules/threed_print/__init__.py
      provides: Updated module registry including FilamentTypesModule
      contains: "FilamentTypesModule"
    - path: backend/app/main.py
      provides: /api/v1/filament-types route mounted
      contains: "filament-types"
  key_links:
    - from: backend/app/api/v1/filament_types.py
      to: backend/app/auth/dependencies.py
      via: "TenantDB (not get_db)"
      pattern: "TenantDB"
    - from: backend/app/modules/threed_print/__init__.py
      to: backend/app/modules/threed_print/filament_types.py
      via: "FilamentTypesModule() in get_modules() list"
      pattern: "FilamentTypesModule()"
---

<objective>
Create the FilamentType CRUD API (POST, GET list, GET by ID, PUT, DELETE), wire it into the 3D print module system, and mount it at /api/v1/filament-types in main.py. All endpoints must use TenantDB (RLS-enabled) — not the legacy Depends(get_db) used in spools.py.

Purpose: Expose FilamentType data over HTTP so the frontend (Phase 2+) and tests can interact with it.
Output: filament_types.py controller, FilamentTypesModule, updated __init__.py, updated main.py.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-PATTERNS.md

<interfaces>
<!-- Key types and patterns the executor needs. -->

From backend/app/auth/dependencies.py:
  CurrentUser = Annotated[User, Depends(get_current_user)]
  CurrentTenant = Annotated[Tenant, Depends(get_current_tenant)]
  TenantDB = Annotated[AsyncSession, Depends(get_tenant_db)]

From backend/app/api/v1/spools.py (endpoint patterns to replicate for FilamentType):
  @router.post("", response_model=SpoolResponse, status_code=status.HTTP_201_CREATED)
  async def create_spool(spool_data: SpoolCreate, user: CurrentUser, tenant: CurrentTenant, db: TenantDB)
  @router.get("", response_model=SpoolListResponse)
  async def list_spools(user, tenant, db, page: int = Query(1, ge=1), page_size: int = Query(20, ge=1, le=100))
  @router.get("/{spool_id}", response_model=SpoolResponse)
  async def get_spool(spool_id: UUID, user, tenant, db)
  @router.put("/{spool_id}", response_model=SpoolResponse) — uses data.model_dump(exclude_unset=True)
  @router.delete("/{spool_id}", status_code=status.HTTP_204_NO_CONTENT)

From backend/app/modules/threed_print/consumables.py (FilamentTypesModule pattern):
  class ConsumablesModule(BaseModule):
      name = "consumables"
      tenant_types = [TenantType.THREE_D_PRINT, TenantType.GENERIC]
      def register_routes(self, router: APIRouter):
          from app.api.v1 import consumables
          for route in consumables.router.routes:
              router.routes.append(route)

From backend/app/main.py (router registration pattern):
  from app.api.v1 import spools
  app.include_router(spools.router, prefix=f"{settings.api_v1_prefix}/spools", tags=["spools"])
  — FilamentType router must follow the same include_router pattern with prefix="/filament-types"

Helper function pattern from spools.py (spool_to_response):
  def filament_type_to_response(ft: FilamentType) -> dict:
      return {
          **ft.__dict__,
          "material_type_code": ft.material_type.code if ft.material_type else "UNKNOWN",
          "material_type_name": ft.material_type.name if ft.material_type else "Unknown",
      }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FilamentType CRUD API endpoints</name>
  <files>backend/app/api/v1/filament_types.py</files>
  <read_first>
    - backend/app/api/v1/spools.py — exact endpoint patterns to replicate (imports, helper function, POST/GET/PUT/DELETE structure)
    - backend/app/schemas/filament_type.py — verify FilamentTypeCreate/Update/Response/ListResponse names
    - backend/app/models/filament_type.py — verify FilamentType class name
    - backend/app/auth/dependencies.py — confirm TenantDB is defined at line 296
  </read_first>
  <behavior>
    - POST "" → 201 + FilamentTypeResponse; catches IntegrityError → 400 "Invalid foreign key reference — check material_type_id exists."
    - GET "" → 200 FilamentTypeListResponse with page/page_size Query params; query scoped to tenant.id
    - GET "/{filament_type_id}" → 200 FilamentTypeResponse or 404 "FilamentType {id} not found"
    - PUT "/{filament_type_id}" → 200 FilamentTypeResponse using model_dump(exclude_unset=True); 404 if not found; 400 on IntegrityError
    - DELETE "/{filament_type_id}" → 204; 404 if not found
    - All endpoints: user: CurrentUser, tenant: CurrentTenant, db: TenantDB — never Depends(get_db)
    - filament_type_to_response helper: unpacks ft.__dict__, adds material_type_code/material_type_name from lazy-joined ft.material_type
    - module-level logger = logging.getLogger(__name__)
  </behavior>
  <action>
    Create backend/app/api/v1/filament_types.py:

    Imports: logging, Optional from typing, UUID from uuid, APIRouter/HTTPException/Query/status from fastapi, func/select from sqlalchemy, IntegrityError from sqlalchemy.exc, AsyncSession from sqlalchemy.ext.asyncio, CurrentTenant/CurrentUser/TenantDB from app.auth.dependencies, FilamentType from app.models.filament_type, FilamentTypeCreate/FilamentTypeListResponse/FilamentTypeResponse/FilamentTypeUpdate from app.schemas.filament_type.

    logger = logging.getLogger(__name__)
    router = APIRouter()

    Helper function filament_type_to_response(ft: FilamentType) -> dict — returns dict merging ft.__dict__ with material_type_code and material_type_name from ft.material_type (lazy="joined" relationship, always loaded).

    Five endpoint functions following the patterns from spools.py. For create: ft = FilamentType(tenant_id=tenant.id, **data.model_dump()), db.add(ft), await db.commit(), await db.refresh(ft). For list: select(FilamentType).where(FilamentType.tenant_id == tenant.id) with count subquery, offset+limit, order_by created_at.desc(). For update: model_dump(exclude_unset=True) then setattr loop.

    Do NOT add material-type sub-endpoints — those stay in spools.py.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.api.v1.filament_types import router
routes = {r.path: r.methods for r in router.routes}
assert '' in routes or '/' in routes, f'Missing root route in {routes}'
# Verify TenantDB usage (not get_db)
import pathlib
text = pathlib.Path('app/api/v1/filament_types.py').read_text()
assert 'TenantDB' in text, 'TenantDB not used'
assert 'get_db' not in text, 'get_db used (should be TenantDB)'
print('filament_types.py OK, routes:', list(routes.keys()))
"</automated>
  </verify>
  <done>
    - backend/app/api/v1/filament_types.py exists with router object
    - router has routes for POST "", GET "", GET "/{filament_type_id}", PUT "/{filament_type_id}", DELETE "/{filament_type_id}"
    - File contains "TenantDB" and does NOT contain "get_db"
    - All endpoints use user: CurrentUser, tenant: CurrentTenant, db: TenantDB parameter signature
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FilamentTypesModule and wire into app</name>
  <files>
    backend/app/modules/threed_print/filament_types.py
    backend/app/modules/threed_print/__init__.py
    backend/app/main.py
  </files>
  <read_first>
    - backend/app/modules/threed_print/consumables.py — exact FilamentTypesModule class template to replicate
    - backend/app/modules/threed_print/__init__.py — existing import and get_modules() pattern; know exactly where to add FilamentTypesModule
    - backend/app/main.py — find the spools.router include_router line (line ~241); add filament_types after it
  </read_first>
  <action>
    CREATE backend/app/modules/threed_print/filament_types.py:
      - Module docstring: "FilamentType module for 3D print tenants."
      - Imports: APIRouter from fastapi, BaseModule from app.modules.base, TenantType from app.schemas.tenant_settings
      - FilamentTypesModule(BaseModule):
          name = "filament_types"
          display_name = "Filament Types"
          icon = "layers"
          description = "Manage filament type definitions for 3D printing"
          tenant_types = [TenantType.THREE_D_PRINT, TenantType.GENERIC]
          def register_routes(self, router: APIRouter) -> None:
              from app.api.v1 import filament_types
              for route in filament_types.router.routes:
                  router.routes.append(route)

    EDIT backend/app/modules/threed_print/__init__.py:
      - Add import after SpoolsModule import: from app.modules.threed_print.filament_types import FilamentTypesModule
      - Add FilamentTypesModule() to get_modules() return list (place before SpoolsModule for logical ordering)
      - Add "FilamentTypesModule" to __all__ list

    EDIT backend/app/main.py:
      - Find the existing spools import: from app.api.v1 import ... spools ... (look at the import block around line 224-225)
      - Add filament_types to the import: from app.api.v1 import filament_types (after the spools import line)
      - Find app.include_router(spools.router, prefix=...) line (~241)
      - Add immediately after it:
        app.include_router(filament_types.router, prefix=f"{settings.api_v1_prefix}/filament-types", tags=["filament-types"])
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.modules.threed_print.filament_types import FilamentTypesModule
from app.modules.threed_print import get_modules
from app.schemas.tenant_settings import TenantType

# FilamentTypesModule exists and has correct tenant_types
ftm = FilamentTypesModule()
assert TenantType.THREE_D_PRINT in ftm.tenant_types
assert ftm.name == 'filament_types'

# get_modules() includes FilamentTypesModule
modules = get_modules()
module_names = [m.name for m in modules]
assert 'filament_types' in module_names, f'filament_types not in {module_names}'

# main.py has the filament-types route
import pathlib
main_text = pathlib.Path('app/main.py').read_text()
assert 'filament-types' in main_text, '/api/v1/filament-types not registered in main.py'
print('Module wiring OK')
"</automated>
  </verify>
  <done>
    - backend/app/modules/threed_print/filament_types.py exists with FilamentTypesModule
    - FilamentTypesModule.name == "filament_types", tenant_types includes THREE_D_PRINT and GENERIC
    - threed_print/__init__.py imports FilamentTypesModule and includes FilamentTypesModule() in get_modules()
    - backend/app/main.py has filament_types.router included at prefix /api/v1/filament-types
    - App starts without ImportError: python -c "from app.main import app" succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FilamentType API input | All payloads validated by Pydantic; material_type_id FK verified at commit time |
| Tenant isolation | TenantDB sets app.current_tenant_id session var; RLS policies enforce at PostgreSQL layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Elevation of Privilege | FilamentType endpoints | mitigate | All endpoints require CurrentUser + CurrentTenant via FastAPI Depends; unauthenticated requests receive 401 |
| T-01-09 | Information Disclosure | Cross-tenant FilamentType access | mitigate | TenantDB dependency (not get_db) sets app.current_tenant_id; RLS policies on filament_types table enforce isolation |
| T-01-10 | Tampering | Invalid material_type_id in FilamentTypeCreate | mitigate | IntegrityError caught at db.commit(); returns 400 with descriptive message |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
```bash
cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.main import app
routes = {r.path for r in app.routes}
filament_routes = [r for r in routes if 'filament-types' in r]
assert filament_routes, f'No filament-types routes found in {list(routes)[:10]}'
print('App loads OK. FilamentType routes:', filament_routes)
"
```
</verification>

<success_criteria>
- backend/app/api/v1/filament_types.py: 5 CRUD endpoints, all using TenantDB, no get_db usage
- FilamentTypesModule registered in 3D print module system; name="filament_types", correct tenant_types
- app.include_router for filament_types at /api/v1/filament-types in main.py
- python -c "from app.main import app" succeeds without errors
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-04-SUMMARY.md when done
</output>
