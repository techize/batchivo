---
phase: 01-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/filament_type.py
  - backend/app/schemas/filament_type.py
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "A FilamentType SQLAlchemy model exists with all D-01 fields: brand, color, color_hex, finish, pattern, spool_type, translucent, glow, diameter, density, extruder_temp, bed_temp, notes, has_sample"
    - "FilamentType carries tenant_id FK (CASCADE) and material_type_id FK — same pattern as all other tenant-scoped models"
    - "has_sample defaults to False"
    - "FilamentType Pydantic schemas exist: FilamentTypeBase, FilamentTypeCreate, FilamentTypeUpdate (all-optional), FilamentTypeResponse, FilamentTypeListResponse"
    - "FilamentTypeResponse nests material_type_code and material_type_name from the lazy=joined relationship"
  artifacts:
    - path: backend/app/models/filament_type.py
      provides: FilamentType ORM model
      contains: "class FilamentType(Base, UUIDMixin, TimestampMixin)"
    - path: backend/app/schemas/filament_type.py
      provides: Pydantic schema hierarchy
      exports:
        - FilamentTypeBase
        - FilamentTypeCreate
        - FilamentTypeUpdate
        - FilamentTypeResponse
        - FilamentTypeListResponse
  key_links:
    - from: backend/app/models/filament_type.py
      to: backend/app/models/base.py
      via: "class FilamentType(Base, UUIDMixin, TimestampMixin)"
      pattern: "UUIDMixin, TimestampMixin"
    - from: backend/app/schemas/filament_type.py
      to: backend/app/models/filament_type.py
      via: "ConfigDict(from_attributes=True) on FilamentTypeResponse"
      pattern: "from_attributes=True"
---

<objective>
Create the FilamentType SQLAlchemy model and its Pydantic schema hierarchy. These are the foundational types that all subsequent plans (migration, API, tests) depend on.

Purpose: Establish the data contracts for the new two-tier structure before any database changes or API code is written.
Output: backend/app/models/filament_type.py and backend/app/schemas/filament_type.py
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-RESEARCH.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-PATTERNS.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From backend/app/models/spool.py (exact field patterns to replicate):
  tenant_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("tenants.id", ondelete="CASCADE"), nullable=False, index=True, comment="...")
  material_type_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("material_types.id"), nullable=False, index=True, comment="...")
  diameter: Mapped[float] = mapped_column(Numeric(4, 2), nullable=False, default=1.75, server_default="1.75", comment="...")
  translucent: Mapped[bool] = mapped_column(default=False, nullable=False, comment="...")
  material_type: Mapped["MaterialType"] = relationship("MaterialType", lazy="joined")

From backend/app/models/base.py (mixins — must use exactly):
  class UUIDMixin — provides id: Mapped[uuid.UUID] (PK)
  class TimestampMixin — provides created_at, updated_at

From backend/app/auth/dependencies.py (aliases):
  CurrentUser = Annotated[User, Depends(get_current_user)]
  CurrentTenant = Annotated[Tenant, Depends(get_current_tenant)]
  TenantDB = Annotated[AsyncSession, Depends(get_tenant_db)]

From backend/app/schemas/spool.py (schema patterns to replicate):
  class SpoolBase(BaseModel) — uses Field(..., min_length=1, ...)
  class SpoolUpdate(BaseModel) — all fields Optional with same validators
  class SpoolResponse(SpoolBase) — has model_config = ConfigDict(from_attributes=True)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FilamentType SQLAlchemy model</name>
  <files>backend/app/models/filament_type.py</files>
  <read_first>
    - backend/app/models/spool.py — exact field patterns, relationship patterns, comment convention
    - backend/app/models/base.py — UUIDMixin and TimestampMixin definitions
    - backend/app/models/material.py — MaterialType model (to verify FK target table name is material_types)
  </read_first>
  <behavior>
    - FilamentType(Base, UUIDMixin, TimestampMixin) with __tablename__ = "filament_types"
    - tenant_id FK to tenants.id ondelete=CASCADE, nullable=False, index=True
    - material_type_id FK to material_types.id, nullable=False, index=True
    - brand: String(100) NOT NULL
    - color: String(50) NOT NULL
    - color_hex: String(9) nullable
    - finish: String(50) nullable
    - diameter: Numeric(4,2) NOT NULL default=1.75 server_default="1.75"
    - density: Numeric(5,3) nullable
    - extruder_temp: Integer nullable
    - bed_temp: Integer nullable
    - translucent: bool NOT NULL default=False
    - glow: bool NOT NULL default=False
    - pattern: String(50) nullable
    - spool_type: String(50) nullable
    - notes: Text nullable
    - has_sample: bool NOT NULL default=False with comment "Whether a display benchy has been printed for this filament type"
    - material_type relationship: Mapped["MaterialType"] lazy="joined"
    - spools back-reference: Mapped[list["Spool"]] back_populates="filament_type" lazy="select"
    - Every mapped_column has a comment= kwarg (matches spool.py convention)
  </behavior>
  <action>
    Create backend/app/models/filament_type.py using the import pattern from spool.py:
    - Imports: uuid, Optional from typing, ForeignKey/Integer/Numeric/String/Text/Boolean from sqlalchemy, Mapped/mapped_column/relationship from sqlalchemy.orm, Base from app.database, TimestampMixin/UUIDMixin from app.models.base
    - Module docstring: "FilamentType shared definition model."
    - D-01 fields as described in behavior block above — per D-04, brand+color+material_type_id are the deduplication key so brand and color must be NOT NULL
    - has_sample field is per DATA-04
    - Do NOT add any import of Spool (forward reference "Spool" string is sufficient)
    - Do NOT import MaterialType at module level — use string "MaterialType" in relationship
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "from app.models.filament_type import FilamentType; ft = FilamentType.__table__; cols = {c.name for c in ft.columns}; required = {'tenant_id','material_type_id','brand','color','diameter','translucent','glow','has_sample'}; assert required.issubset(cols), f'Missing: {required - cols}'; print('OK:', cols)"</automated>
  </verify>
  <done>
    - backend/app/models/filament_type.py exists
    - Import succeeds: from app.models.filament_type import FilamentType
    - FilamentType.__table__.columns contains: tenant_id, material_type_id, brand, color, color_hex, finish, diameter, density, extruder_temp, bed_temp, translucent, glow, pattern, spool_type, notes, has_sample
    - FilamentType.__tablename__ == "filament_types"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FilamentType Pydantic schema hierarchy</name>
  <files>backend/app/schemas/filament_type.py</files>
  <read_first>
    - backend/app/schemas/spool.py — Base/Create/Update/Response pattern, Field validators, ConfigDict usage
    - backend/app/models/filament_type.py — just created; verify field names and types to match
  </read_first>
  <behavior>
    - FilamentTypeBase(BaseModel): all D-01 fields; brand Field(..., min_length=1, max_length=100); color Field(..., min_length=1, max_length=50); diameter Field(1.75, gt=0, le=5); extruder_temp Field(None, ge=150, le=400); bed_temp Field(None, ge=0, le=150); density Field(None, gt=0, le=10); translucent=False; glow=False; has_sample=False
    - FilamentTypeCreate(FilamentTypeBase): pass
    - FilamentTypeUpdate(BaseModel): all fields Optional with same validators as Base (not inheriting from Base)
    - FilamentTypeResponse(FilamentTypeBase): adds id: UUID, tenant_id: UUID, created_at: datetime, updated_at: datetime, material_type_code: str, material_type_name: str; model_config = ConfigDict(from_attributes=True)
    - FilamentTypeListResponse(BaseModel): total: int, filament_types: list[FilamentTypeResponse], page: int = Field(1, ge=1), page_size: int = Field(20, ge=1, le=100)
    - material_type_code and material_type_name in FilamentTypeResponse come from the lazy="joined" relationship accessed in the API helper, NOT as ORM validators — they are plain fields in the schema
  </behavior>
  <action>
    Create backend/app/schemas/filament_type.py. Module docstring: "Pydantic schemas for FilamentType API."
    - Imports: datetime from datetime, Optional from typing, UUID from uuid, BaseModel/ConfigDict/Field from pydantic
    - Five classes as described above
    - FilamentTypeUpdate must have material_type_id: Optional[UUID] = None as well (D-03 places it on FilamentType)
    - No imports from app.models — schemas must not depend on ORM models directly
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.schemas.filament_type import FilamentTypeCreate, FilamentTypeUpdate, FilamentTypeResponse, FilamentTypeListResponse
from uuid import uuid4
from datetime import datetime

# FilamentTypeCreate requires brand, color, material_type_id
ft = FilamentTypeCreate(material_type_id=uuid4(), brand='Bambu', color='Red', diameter=1.75)
assert ft.has_sample is False
assert ft.diameter == 1.75

# FilamentTypeUpdate all optional
upd = FilamentTypeUpdate()
assert upd.brand is None

# FilamentTypeResponse requires id etc
resp = FilamentTypeResponse(
    id=uuid4(), tenant_id=uuid4(), material_type_id=uuid4(),
    brand='Bambu', color='Red', diameter=1.75,
    translucent=False, glow=False, has_sample=False,
    created_at=datetime.now(), updated_at=datetime.now(),
    material_type_code='PLA', material_type_name='PLA',
)
assert resp.material_type_code == 'PLA'
print('OK')
"</automated>
  </verify>
  <done>
    - backend/app/schemas/filament_type.py exists
    - FilamentTypeCreate instantiates with material_type_id, brand, color (minimum)
    - FilamentTypeCreate.has_sample defaults to False (DATA-04)
    - FilamentTypeUpdate instantiates with no arguments (all optional)
    - FilamentTypeResponse has model_config = ConfigDict(from_attributes=True)
    - FilamentTypeListResponse has filament_types: list[FilamentTypeResponse]
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema input | Pydantic validates all FilamentType creation/update payloads at the API boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | FilamentType.tenant_id | mitigate | tenant_id FK with CASCADE enforced at DB layer; RLS applied in Wave 2 migration inline |
| T-01-02 | Tampering | FilamentTypeCreate brand/color validation | mitigate | Field(min_length=1) prevents empty-string brand/color which would corrupt deduplication key |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — all dependencies are existing project deps |
</threat_model>

<verification>
After both tasks:

```bash
cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.models.filament_type import FilamentType
from app.schemas.filament_type import FilamentTypeCreate, FilamentTypeResponse, FilamentTypeUpdate, FilamentTypeListResponse
print('All imports OK')

# Verify all D-01 fields present on model
cols = {c.name for c in FilamentType.__table__.columns}
d01_fields = {'brand','color','color_hex','finish','pattern','spool_type','translucent','glow','diameter','density','extruder_temp','bed_temp','notes','has_sample'}
missing = d01_fields - cols
assert not missing, f'D-01 fields missing from model: {missing}'
print('D-01 field coverage OK')
"
```
</verification>

<success_criteria>
- backend/app/models/filament_type.py: FilamentType model with all D-01 fields, tenant_id FK, material_type_id FK, has_sample boolean default False
- backend/app/schemas/filament_type.py: five schema classes; FilamentTypeCreate validates brand/color as non-empty; FilamentTypeResponse has from_attributes=True
- Python import of both files succeeds without errors
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-01-SUMMARY.md when done
</output>
