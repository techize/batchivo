---
phase: 01-data-model-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/spool.py
  - backend/app/schemas/spool.py
  - backend/alembic/env.py
  - backend/alembic/versions/MERGE_filament_type_heads.py
autonomous: true
requirements:
  - DATA-02
  - DATA-05

must_haves:
  truths:
    - "Spool model no longer has brand, color, color_hex, finish, pattern, spool_type, translucent, glow, diameter, density, extruder_temp, bed_temp, notes, material_type_id, purchased_quantity, spools_remaining"
    - "D-02: The following fields stay on Spool: spool_id, initial_weight, current_weight, empty_spool_weight, storage_location, qr_code_id, is_active, is_labeled, purchase_price, supplier, purchase_date"
    - "D-03: material_type_id FK moves to FilamentType; Spool references FilamentType via filament_type_id FK"
    - "Spool model has filament_type_id FK to filament_types.id (RESTRICT) and is_labeled boolean default False"
    - "Spool has filament_type relationship (lazy=joined) replacing material_type relationship"
    - "SpoolBase schema has filament_type_id and is_labeled; all old type fields removed"
    - "SpoolResponse nests FilamentTypeResponse instead of flat material_type fields"
    - "alembic/env.py imports FilamentType so autogenerate works"
    - "A merge migration exists that has all 12 current heads as down_revision"
  artifacts:
    - path: backend/app/models/spool.py
      provides: Updated Spool model
      contains: "filament_type_id"
    - path: backend/app/schemas/spool.py
      provides: Updated Spool schemas
      contains: "filament_type_id"
    - path: backend/alembic/env.py
      provides: Updated Alembic env with FilamentType import
      contains: "from app.models.filament_type import FilamentType"
    - path: backend/alembic/versions/MERGE_filament_type_heads.py
      provides: Merge migration that unifies 12 heads
      contains: "down_revision"
  key_links:
    - from: backend/app/models/spool.py
      to: backend/app/models/filament_type.py
      via: "filament_type_id FK + filament_type relationship"
      pattern: 'ForeignKey("filament_types.id", ondelete="RESTRICT")'
    - from: backend/app/schemas/spool.py
      to: backend/app/schemas/filament_type.py
      via: "SpoolResponse nests FilamentTypeResponse"
      pattern: "filament_type: FilamentTypeResponse"
---

<objective>
Update the Spool model and schemas to use the new two-tier structure: remove all type-level fields (D-01), add filament_type_id FK (D-03) and is_labeled (D-05). Also register FilamentType in alembic/env.py and create the merge migration that resolves the 12 existing heads before the data migration is added.

Purpose: The Spool model must reflect its new role (per-unit record) before the Alembic data migration in Plan 03 can be written against it.
Output: Updated spool.py model, updated spool.py schemas, updated alembic env.py, and merge migration file.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-RESEARCH.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-PATTERNS.md

<interfaces>
<!-- Key types the executor needs. -->

Current Spool model fields being REMOVED (from backend/app/models/spool.py):
  - material_type_id (FK to material_types.id) — moves to FilamentType per D-03
  - brand, color, color_hex, finish — D-01
  - diameter, density, extruder_temp, bed_temp — D-01
  - translucent, glow, pattern, spool_type — D-01
  - purchased_quantity, spools_remaining — D-07
  - notes — D-01
  - material_type: Mapped["MaterialType"] relationship — replaced by filament_type relationship

Fields STAYING on Spool (D-02):
  - spool_id, initial_weight, current_weight, empty_spool_weight
  - storage_location, qr_code_id, is_active
  - purchase_price, supplier, purchase_date
  - tenant_id (stays)

Fields ADDED to Spool (D-02, D-03, D-05):
  - filament_type_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("filament_types.id", ondelete="RESTRICT"), nullable=False, index=True, comment="FilamentType this spool belongs to")
  - is_labeled: Mapped[bool] = mapped_column(default=False, nullable=False, comment="Whether a physical label has been printed for this spool")
  - filament_type: Mapped["FilamentType"] = relationship("FilamentType", lazy="joined")

Current Alembic heads (12 heads confirmed from codebase scan — `alembic heads` or dependency graph analysis):
  - a0b1c2d3e4f5  (add_3d_printing_fields_to_product_variants)
  - k2l3m4n5o6p7  (add_index_production_run_original_run_id)
  - b1c2d3e4f5g6  (add_multichannel_fields_to_products)
  - r5s6t7u8v9w0  (add_orders_tables)
  - c6d7e8f9a0b1  (add_composite_indexes)
  - c6d7e8f9g0h1  (add_platform_settings)
  - c384baaa92e4  (add_tenant_currency_fields)
  - u8v9w0x1y2z3  (add_email_tracking_to_orders)
  - 8e9eb6466afa  (add_webhook_events_tables)
  - l9m0n1o2p3q4  (models_products_sales_channels)
  - j4k5l6m7n8o9  (add_model_files_table)
  - o2p3q4r5s6t7  (add_product_components)

**IMPORTANT:** The executor MUST verify these revision IDs by reading each migration file's `revision = "..."` line before creating the merge migration. Do not use these IDs verbatim without verification — confirm each from the actual file.

The merge migration down_revision must reference ALL TWELVE of these as a tuple.

From backend/app/schemas/spool.py (current SpoolBase fields being removed):
  material_type_id, brand, color, color_hex, finish, diameter, density, extruder_temp, bed_temp,
  translucent, glow, pattern, spool_type, purchased_quantity, spools_remaining, notes

SpoolResponse currently nests material_type_code/material_type_name as flat strings.
After this plan: SpoolResponse nests filament_type: FilamentTypeResponse (imported from app.schemas.filament_type).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Spool model — remove type fields, add filament_type_id and is_labeled</name>
  <files>backend/app/models/spool.py</files>
  <read_first>
    - backend/app/models/spool.py — read fully before editing; know exact lines being removed
    - backend/app/models/filament_type.py — verify __tablename__ = "filament_types" to confirm FK target
  </read_first>
  <action>
    Edit backend/app/models/spool.py with these surgical changes:

    REMOVE these imports no longer needed after field removals:
    - DateTime from sqlalchemy (still needed for purchase_date — keep it)
    - Integer from sqlalchemy — remove only if purchased_quantity and spools_remaining were the only Integer columns (verify: extruder_temp and bed_temp also use Integer — but those move to FilamentType; check if Integer is still needed for any remaining Spool field; if not, remove)

    REMOVE these mapped_column fields:
    - material_type_id column (lines ~33-38)
    - brand, color, color_hex, finish (lines ~48-70)
    - diameter, density, extruder_temp, bed_temp (lines ~73-98)
    - translucent, glow, pattern, spool_type (lines ~101-123)
    - purchased_quantity, spools_remaining (lines ~164-178)
    - notes (lines ~187-192)

    ADD these fields after the storage_location field:
    - filament_type_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("filament_types.id", ondelete="RESTRICT"), nullable=False, index=True, comment="FilamentType this spool belongs to")
    - is_labeled: Mapped[bool] = mapped_column(default=False, nullable=False, comment="Whether a physical label has been printed for this spool")

    REPLACE the material_type relationship with:
    - filament_type: Mapped["FilamentType"] = relationship("FilamentType", lazy="joined")

    UPDATE __repr__ to:
    - return f"<Spool(id={self.spool_id}, filament_type_id={self.filament_type_id})>"

    KEEP: production_run_materials relationship unchanged
    KEEP: remaining_weight, remaining_percentage, cost_per_gram properties unchanged
    REMOVE: is_low_stock property (it references remaining_percentage which still works, so keep it — only remove if it references removed fields; check its body: it calls self.remaining_percentage which is fine, keep it)
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.models.spool import Spool
cols = {c.name for c in Spool.__table__.columns}

# Must have new fields
assert 'filament_type_id' in cols, 'filament_type_id missing'
assert 'is_labeled' in cols, 'is_labeled missing'

# Must NOT have removed fields
for removed in ['brand', 'color', 'material_type_id', 'purchased_quantity', 'spools_remaining', 'notes', 'diameter']:
    assert removed not in cols, f'{removed} should have been removed'

print('Spool model OK:', cols)
"</automated>
  </verify>
  <done>
    - Spool.__table__.columns contains filament_type_id and is_labeled
    - Spool.__table__.columns does NOT contain brand, color, color_hex, finish, diameter, density, extruder_temp, bed_temp, translucent, glow, pattern, spool_type, notes, material_type_id, purchased_quantity, spools_remaining
    - Python import succeeds: from app.models.spool import Spool
    - Spool has filament_type relationship (lazy="joined")
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Spool schemas + register FilamentType in env.py + create merge migration</name>
  <files>
    backend/app/schemas/spool.py
    backend/alembic/env.py
    backend/alembic/versions/MERGE_filament_type_heads.py
  </files>
  <read_first>
    - backend/app/schemas/spool.py — read fully; identify exact fields to remove from SpoolBase and SpoolUpdate, and SpoolResponse nesting to replace
    - backend/alembic/env.py — find the model import block (around line 38-41) to add FilamentType import
    - backend/alembic/versions/k2l3m4n5o6p7_add_index_production_run_original_run_id.py — confirm its revision ID is k2l3m4n5o6p7
    - backend/alembic/versions/8e9eb6466afa_add_webhook_events_tables.py — confirm revision ID
    - backend/alembic/versions/c384baaa92e4_add_tenant_currency_fields.py — confirm revision ID
    - backend/alembic/versions/local_file_refs_01_add_local_file_reference_support.py — confirm revision ID (may differ from filename)
  </read_first>
  <action>
    EDIT backend/app/schemas/spool.py:

    In SpoolBase: REMOVE these fields — material_type_id, brand, color, color_hex, finish, diameter, density, extruder_temp, bed_temp, translucent, glow, pattern, spool_type, purchased_quantity, spools_remaining, notes.
    ADD to SpoolBase: filament_type_id: UUID = Field(..., description="FilamentType ID") and is_labeled: bool = Field(False, description="Whether a label has been printed for this spool")

    In SpoolUpdate: Remove all the same removed fields. Add filament_type_id: Optional[UUID] = None and is_labeled: Optional[bool] = None.

    In SpoolResponse: Remove material_type_code and material_type_name flat fields. Import FilamentTypeResponse from app.schemas.filament_type (use TYPE_CHECKING guard to avoid circular import if needed — check if circular; both schemas are pure Pydantic with no cross-imports so it should be safe to import directly). Add filament_type: "FilamentTypeResponse" as a field. Keep remaining_weight and remaining_percentage fields.

    In spool_to_response helper in backend/app/api/v1/spools.py — this plan does NOT touch spools.py API (Plan 05 does that). The schema changes here will cause spools.py to fail at runtime until Plan 05 fixes it — that is expected and acceptable since tests are run at Phase gate, not per-plan.

    EDIT backend/alembic/env.py:
    Find the block where other models are imported (search for "from app.models" imports). Add:
      from app.models.filament_type import FilamentType  # noqa: F401
    This must be added before the target_metadata line.

    CREATE backend/alembic/versions/MERGE_filament_type_heads.py:
    Use the confirmed revision IDs of all 12 current heads (listed in <interfaces> above). The merge migration body has empty upgrade() and downgrade() functions — it is a pure merge point. Set:
      revision = "merge_filament_type_heads"
      down_revision = (id_1, id_2, id_3, id_4, id_5, id_6, id_7, id_8, id_9, id_10, id_11, id_12)
    Read each of the 12 migration files from backend/alembic/versions/ to confirm the `revision = "..."` value before using it. Do not use IDs from memory.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.schemas.spool import SpoolBase, SpoolCreate, SpoolUpdate, SpoolResponse

# SpoolBase must have filament_type_id and is_labeled
import inspect
fields = SpoolCreate.model_fields
assert 'filament_type_id' in fields, 'filament_type_id missing from SpoolCreate'
assert 'is_labeled' in fields, 'is_labeled missing from SpoolCreate'
assert 'brand' not in fields, 'brand should be removed from SpoolCreate'
assert 'material_type_id' not in fields, 'material_type_id should be removed'
print('SpoolCreate fields OK')

# SpoolUpdate all optional
upd = SpoolUpdate()
assert upd.filament_type_id is None
print('SpoolUpdate OK')

# env.py has FilamentType import
import pathlib
env_text = pathlib.Path('alembic/env.py').read_text()
assert 'from app.models.filament_type import FilamentType' in env_text, 'FilamentType not in env.py'
print('env.py OK')

# Merge migration exists and has 12 heads in down_revision
import re
merge_path = list(pathlib.Path('alembic/versions').glob('*filament_type_heads*'))
assert merge_path, 'Merge migration not found'
merge_text = merge_path[0].read_text()
down_rev_line = [l for l in merge_text.splitlines() if 'down_revision' in l][0]
ids_in_tuple = re.findall(r'\"([a-f0-9_]+)\"', down_rev_line)
assert len(ids_in_tuple) == 12, f'Expected 12 heads in down_revision, found {len(ids_in_tuple)}: {ids_in_tuple}'
print(f'Merge migration OK — {len(ids_in_tuple)} heads in down_revision')
"</automated>
  </verify>
  <done>
    - SpoolCreate has filament_type_id and is_labeled; brand, color, material_type_id, purchased_quantity, spools_remaining are gone
    - SpoolUpdate is all-optional with filament_type_id: Optional[UUID] and is_labeled: Optional[bool]
    - SpoolResponse has filament_type: FilamentTypeResponse (not flat material_type_code string)
    - alembic/env.py contains: from app.models.filament_type import FilamentType  # noqa: F401
    - backend/alembic/versions/MERGE_filament_type_heads.py exists with down_revision tuple of all 12 current head revision IDs (executor must verify IDs from actual migration files)
    - is_labeled defaults to False in SpoolBase (DATA-05)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spool schema input | filament_type_id is now required in SpoolCreate; missing FK produces 422 from Pydantic |
| Migration merge | Merge migration must reference all active heads or alembic upgrade will fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | Spool.filament_type_id | mitigate | FK ondelete=RESTRICT prevents orphaned spools; filament_type_id is NOT NULL so a spool cannot exist without a type |
| T-01-04 | Tampering | alembic heads divergence | mitigate | Merge migration consolidates all 12 heads; the data migration (Plan 03) chains off this merge |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.models.spool import Spool
from app.schemas.spool import SpoolCreate, SpoolUpdate, SpoolResponse
from app.models.filament_type import FilamentType
from app.schemas.filament_type import FilamentTypeResponse

# Verify Spool.filament_type_id FK
fk_targets = {fk.target_fullname for fk in Spool.__table__.foreign_keys}
assert 'filament_types.id' in fk_targets, f'FK to filament_types missing: {fk_targets}'

# Verify is_labeled present
assert 'is_labeled' in {c.name for c in Spool.__table__.columns}

print('All checks passed')
"
```
</verification>

<success_criteria>
- Spool model has filament_type_id FK (RESTRICT) and is_labeled (default False); all D-01 type fields removed
- Spool schemas updated to match model; SpoolResponse nests FilamentTypeResponse
- alembic/env.py imports FilamentType for autogenerate support
- Merge migration exists with all 12 current head IDs in down_revision (verify command checks the count)
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-02-SUMMARY.md when done
</output>
