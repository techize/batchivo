---
phase: 01-data-model-migration
plan: 07
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - backend/tests/integration/test_filament_types_api.py
  - backend/tests/integration/test_spools_api.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Integration tests for FilamentType API cover: create (201), list (200), get by ID (200), get unknown (404), update (200), delete (204), auth required (401), validation failure (422)"
    - "Integration tests for Spool API updated: create uses filament_type_id instead of brand/color/material_type_id; response has nested filament_type object"
    - "RLS isolation test: spool from tenant A not visible to tenant B — this test passes"
    - "All integration tests pass: poetry run pytest tests/integration/test_filament_types_api.py tests/integration/test_spools_api.py -x"
  artifacts:
    - path: backend/tests/integration/test_filament_types_api.py
      provides: FilamentType API integration tests
      contains: "class TestFilamentTypesEndpoints"
    - path: backend/tests/integration/test_spools_api.py
      provides: Updated Spool API integration tests
      contains: "filament_type_id"
  key_links:
    - from: backend/tests/integration/test_filament_types_api.py
      to: backend/app/api/v1/filament_types.py
      via: "POST /api/v1/filament-types with auth_headers"
      pattern: "client.post('/api/v1/filament-types'"
    - from: backend/tests/integration/test_spools_api.py
      to: backend/app/api/v1/spools.py
      via: "POST /api/v1/spools with filament_type_id"
      pattern: "filament_type_id"
---

<objective>
Write integration tests for the new FilamentType API endpoints and update existing Spool API tests to work with the two-tier model. Tests must cover the testing policy's four required scenarios (200/201, 401, 422, 404) for every new endpoint.

Purpose: Prove the API layer works end-to-end against the refactored models and schemas.
Output: New test_filament_types_api.py and updated test_spools_api.py.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-PATTERNS.md

<interfaces>
<!-- Test infrastructure from existing integration tests -->

From backend/tests/integration/test_spools_api.py (existing pattern to replicate):
  class TestSpoolsEndpoints:
      @pytest.mark.asyncio
      async def test_create_spool(self, client: AsyncClient, auth_headers: dict, test_material_type: MaterialType):
          response = await client.post("/api/v1/spools", headers=auth_headers, json={...})
          assert response.status_code == 201
  
  Available fixtures: client, auth_headers, test_tenant, test_user, test_spool, test_filament_type (after Plan 06), test_material_type

From backend/tests/conftest.py (fixture names available):
  client: AsyncClient (httpx client with app)
  auth_headers: dict with Authorization and X-Tenant-ID
  test_tenant: Tenant
  test_user: User
  test_spool: Spool (after Plan 06: depends on test_filament_type)
  test_filament_type: FilamentType (new from Plan 06)
  test_material_type: MaterialType

Endpoint paths:
  FilamentType: POST/GET /api/v1/filament-types, GET/PUT/DELETE /api/v1/filament-types/{id}
  Spool: POST/GET /api/v1/spools, GET/PUT/DELETE /api/v1/spools/{id}, POST /api/v1/spools/{id}/duplicate

Minimum valid FilamentType JSON payload:
  {"material_type_id": str(test_material_type.id), "brand": "Bambu Lab", "color": "Jade White", "diameter": 1.75}

Minimum valid Spool JSON payload (after migration):
  {"filament_type_id": str(test_filament_type.id), "spool_id": f"TEST-{uuid4().hex[:8].upper()}", "initial_weight": 1000.0, "current_weight": 1000.0}

Testing policy (from CLAUDE.md):
  New endpoint tests MUST cover: Success (200/201), auth (401), validation (422), not found (404)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write FilamentType API integration tests</name>
  <files>backend/tests/integration/test_filament_types_api.py</files>
  <read_first>
    - backend/tests/integration/test_spools_api.py — exact class/method structure to replicate for FilamentType
    - backend/tests/conftest.py — verify test_filament_type and test_material_type fixture names and return types
    - backend/app/api/v1/filament_types.py — verify response field names (has_sample, material_type_code, etc.)
  </read_first>
  <behavior>
    TestFilamentTypesEndpoints tests:
    - test_create_filament_type: POST /api/v1/filament-types with valid payload → 201; response has brand, color, has_sample=False, material_type_code, id
    - test_create_requires_auth: POST without auth_headers → 401
    - test_create_validation_error: POST with brand="" → 422
    - test_list_filament_types: GET /api/v1/filament-types → 200; response has total and filament_types list
    - test_list_requires_auth: GET without auth → 401
    - test_get_filament_type_by_id: GET /api/v1/filament-types/{test_filament_type.id} → 200; response id matches
    - test_get_filament_type_not_found: GET /api/v1/filament-types/{uuid4()} → 404
    - test_update_filament_type: PUT /api/v1/filament-types/{id} with {"has_sample": true} → 200; response has_sample is True
    - test_delete_filament_type: DELETE /api/v1/filament-types/{id} → 204
    - test_delete_not_found: DELETE /api/v1/filament-types/{uuid4()} → 404
    - test_cannot_access_other_tenant_filament_type: create filament type with tenant A's auth; try GET with tenant B's auth → 404 (RLS isolation)
  </behavior>
  <action>
    CREATE backend/tests/integration/test_filament_types_api.py:
    - Module docstring: "Integration tests for FilamentType API endpoints."
    - Imports: pytest, AsyncClient from httpx, uuid4 from uuid, MaterialType from app.models.material, FilamentType from app.models.filament_type
    - @pytest.mark.asyncio on all test methods
    - TestFilamentTypesEndpoints class with all test methods from behavior block
    - For test_cannot_access_other_tenant_filament_type: look at test_cannot_access_other_tenant_spool in existing test_spools_api.py for the pattern of creating a second tenant and auth headers
    - For each POST test, use minimum valid payload: {"material_type_id": str(test_material_type.id), "brand": "Bambu Lab", "color": "Jade White", "diameter": 1.75}
    - For update test: PUT /api/v1/filament-types/{test_filament_type.id} with {"has_sample": True}; assert response.json()["has_sample"] is True
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_filament_types_api.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - test_filament_types_api.py exists with TestFilamentTypesEndpoints class
    - At minimum 10 test methods covering all required scenarios
    - test_create_requires_auth → 401
    - test_create_validation_error → 422
    - test_get_filament_type_not_found → 404
    - test_cannot_access_other_tenant_filament_type → 404
    - All tests pass: poetry run pytest tests/integration/test_filament_types_api.py -x exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Spool API integration tests for two-tier model</name>
  <files>backend/tests/integration/test_spools_api.py</files>
  <read_first>
    - backend/tests/integration/test_spools_api.py — read fully to understand which tests reference brand, color, material_type_id, or export/import endpoints
    - backend/tests/conftest.py — verify test_spool now depends on test_filament_type
  </read_first>
  <behavior>
    Changes required to test_spools_api.py:
    - test_create_spool: remove brand, color, material_type_id from JSON payload; add filament_type_id=str(test_filament_type.id); add test_filament_type fixture parameter
    - test_list_spools: response assertion no longer checks brand/color on spool; instead checks response has filament_type nested object with brand field
    - test_get_spool_by_id: response assertion updated — no direct brand field, instead response["filament_type"]["brand"] exists
    - test_update_spool: remove material_type_id from update payload; update targets is_labeled or storage_location instead
    - test_spool_usage_history: if it referenced brand/color, update to use filament_type path
    - test_calculate_spool_cost_per_gram: field name check — if it checks spool.brand, update
    - test_cannot_access_other_tenant_spool: should still work; just update create payload to use filament_type_id
    - test_list_only_shows_own_tenant_spools: update create payload similarly
    - Remove any tests specifically for export/import endpoints (test_export_*, test_import_*) if they exist
    - Remove test_low_stock_alert if it references brand/color directly
    - Add test_spool_response_has_nested_filament_type: create spool, GET it, assert response["filament_type"]["brand"] == "Test Brand" (from test_filament_type fixture)
  </behavior>
  <action>
    EDIT backend/tests/integration/test_spools_api.py:

    Update all test method signatures that used test_material_type for spool creation to use test_filament_type instead.

    Update all JSON payloads for POST /api/v1/spools: remove brand, color, material_type_id; add filament_type_id: str(test_filament_type.id).

    Update response assertions: where tests check response.json()["brand"] or response.json()["color"] or response.json()["material_type_code"], update to check response.json()["filament_type"]["brand"] or response.json()["filament_type"]["color"].

    Remove tests for /export and /import endpoints if present.

    Add test_spool_response_has_nested_filament_type at the end of the class.

    For tests that use test_spool fixture: since test_spool now depends on test_filament_type (Plan 06), any test that uses test_spool still works but fixtures are automatically chained — no test_material_type parameter needed in tests that only use test_spool.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_spools_api.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - test_spools_api.py has no JSON payloads with brand, color, or material_type_id for spool creation
    - Response assertions use filament_type.brand/color path not direct brand/color
    - test_spool_response_has_nested_filament_type added
    - All tests pass: poetry run pytest tests/integration/test_spools_api.py -x exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test client | Uses httpx AsyncClient against app; auth enforced via real JWT/auth_headers fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | Cross-tenant test coverage | mitigate | test_cannot_access_other_tenant_filament_type explicitly verifies RLS isolation at integration level |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Full suite for Phase 1:

```bash
cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py tests/integration/test_filament_types_api.py tests/integration/test_spools_api.py -v --tb=short 2>&1 | tail -30
```
</verification>

<success_criteria>
- test_filament_types_api.py: 10+ tests, all 4 CLAUDE.md required scenarios covered for FilamentType endpoints
- test_spools_api.py: all spool creation payloads use filament_type_id; response assertions use nested filament_type object; export/import tests removed
- poetry run pytest tests/unit/test_filament_type_schemas.py tests/unit/test_spool_schemas.py tests/integration/test_filament_types_api.py tests/integration/test_spools_api.py -x exits 0
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-07-SUMMARY.md when done
</output>
