---
phase: 01-data-model-migration
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - backend/app/api/v1/spools.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "POST /api/v1/spools accepts filament_type_id in request body (not brand/color)"
    - "GET /api/v1/spools returns SpoolResponse with nested filament_type object"
    - "All spool endpoints use TenantDB (RLS-enforced) — get_db is gone from this file"
    - "GET /export and POST /import endpoints are removed (stale field names — Pitfall 4 in research)"
    - "POST /{spool_id}/duplicate copies filament_type_id correctly (not the old inline type fields)"
    - "GET /material-types and POST /material-types endpoints are unchanged and still work"
  artifacts:
    - path: backend/app/api/v1/spools.py
      provides: Updated Spool CRUD endpoints
      contains: "TenantDB"
  key_links:
    - from: backend/app/api/v1/spools.py
      to: backend/app/auth/dependencies.py
      via: "TenantDB replaces Depends(get_db) on all spool endpoints"
      pattern: "TenantDB"
    - from: backend/app/api/v1/spools.py
      to: backend/app/schemas/spool.py
      via: "SpoolCreate now requires filament_type_id"
      pattern: "filament_type_id"
---

<objective>
Update the existing Spool API (spools.py) to work with the new two-tier model: replace Depends(get_db) with TenantDB on all endpoints (fixing the pre-existing RLS bypass), update spool_to_response to use the nested FilamentType relationship, fix the duplicate endpoint, and remove the stale export/import endpoints.

Purpose: The spool API must work correctly after the data migration; all endpoints must go through RLS.
Output: Updated backend/app/api/v1/spools.py
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jonathan/Repos/batchivo/.planning/ROADMAP.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-CONTEXT.md
@/Users/jonathan/Repos/batchivo/.planning/phases/01-data-model-migration/01-RESEARCH.md

<interfaces>
<!-- From backend/app/auth/dependencies.py -->
  TenantDB = Annotated[AsyncSession, Depends(get_tenant_db)]   # line 296
  CurrentUser = Annotated[User, Depends(get_current_user)]     # line 219
  CurrentTenant = Annotated[Tenant, Depends(get_current_tenant)] # line 220

<!-- From backend/app/api/v1/spools.py current state -->
  All 8 endpoint functions currently use: db: AsyncSession = Depends(get_db)
  They must ALL become: db: TenantDB
  The get_db import from app.database must be removed once no longer used.
  The TenantDB import must be added to the from app.auth.dependencies import line.

  spool_to_response currently returns:
    "material_type_code": spool.material_type.code if spool.material_type else "UNKNOWN",
    "material_type_name": spool.material_type.name if spool.material_type else "Unknown",
  This must change to use the new filament_type relationship:
    "filament_type": spool.filament_type  (the lazy=joined relationship, returns FilamentType ORM object)
  SpoolResponse.filament_type field expects a FilamentTypeResponse — Pydantic will serialize it via from_attributes=True.

  ensure_material_type_exists helper is used by create_spool and update_spool (when material_type_id is in update data).
  After the migration, SpoolCreate no longer has material_type_id — so this helper call in create_spool must be removed.
  In update_spool, the ensure_material_type_exists call for material_type_id update is also gone (material_type_id is no longer in SpoolUpdate).
  Instead, update_spool should validate filament_type_id if present in update_data using a similar helper: ensure_filament_type_exists(db, filament_type_id).

  duplicate_spool currently copies all inline fields including material_type_id, brand, color etc.
  After migration, it should only copy: tenant_id, filament_type_id, initial_weight, current_weight, empty_spool_weight, storage_location, purchase_price, supplier, purchase_date, is_active, is_labeled.
  A new auto-generated spool_id must be created (existing logic already generates one).

  list_spools currently filters on Spool.brand.ilike and Spool.color.ilike in the search filter.
  After migration these fields don't exist. The search filter should instead join through FilamentType:
  Use a join: select(Spool).join(Spool.filament_type).where(Spool.tenant_id == tenant.id)
  Search filter: or_(Spool.spool_id.ilike(f"%{search}%"), FilamentType.brand.ilike(f"%{search}%"), FilamentType.color.ilike(f"%{search}%"))
  material_type_id filter: change to FilamentType.material_type_id == material_type_id (join already included)

  Imports that change:
  REMOVE: from app.database import get_db
  ADD: TenantDB to the from app.auth.dependencies import line
  REMOVE: csv, io, json, yaml, File, UploadFile, StreamingResponse imports (used only by export/import)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update imports, spool_to_response helper, and list_spools search filter</name>
  <files>backend/app/api/v1/spools.py</files>
  <read_first>
    - backend/app/api/v1/spools.py — read the full file to understand current state before any edits
    - backend/app/models/filament_type.py — verify FilamentType is importable and has brand/color/material_type_id
    - backend/app/schemas/spool.py — verify SpoolResponse.filament_type field type
  </read_first>
  <action>
    EDIT backend/app/api/v1/spools.py:

    IMPORTS section changes:
    - Remove: import csv, import io, import json, import yaml
    - Remove: File, UploadFile from fastapi imports (no longer used)
    - Remove: StreamingResponse from fastapi.responses import (no longer used)
    - Remove: from app.database import get_db
    - Add to from app.auth.dependencies import: TenantDB
    - Add: from app.models.filament_type import FilamentType

    HELPER FUNCTION spool_to_response:
    - Remove material_type_code and material_type_name keys
    - The SpoolResponse now has filament_type: FilamentTypeResponse — Pydantic will serialize the ORM relationship object via from_attributes=True. The helper needs to pass the full spool dict. Since filament_type is a relationship attribute (not a column), it should be included in the return via filament_type=spool.filament_type:
      return {**spool.__dict__, "remaining_weight": spool.remaining_weight, "remaining_percentage": spool.remaining_percentage, "filament_type": spool.filament_type}
    - Or alternatively, SpoolResponse can be instantiated directly: SpoolResponse.model_validate(spool) using from_attributes=True — use model_validate(spool) if it works, otherwise use the dict approach

    HELPER FUNCTION ensure_filament_type_exists(db, filament_type_id):
    - Add this helper: queries FilamentType.id where FilamentType.id == filament_type_id; raises HTTPException 400 if not found

    list_spools function:
    - Change base query to: select(Spool).join(Spool.filament_type).where(Spool.tenant_id == tenant.id)
    - Change search filter to use FilamentType.brand.ilike and FilamentType.color.ilike instead of Spool.brand/color
    - Change material_type_id filter: Spool.material_type_id → FilamentType.material_type_id (since material_type_id moved to FilamentType)
    - Keep low_stock_only filter unchanged (uses spool.is_low_stock which still works)
    - Keep count_query, pagination, and response construction unchanged

    All endpoint function signatures: change db: AsyncSession = Depends(get_db) → db: TenantDB on every function (list_material_types, create_material_type, create_spool, list_spools, get_spool, update_spool, delete_spool, duplicate_spool). Do this for all 8 functions.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
import pathlib
text = pathlib.Path('app/api/v1/spools.py').read_text()
assert 'get_db' not in text, 'get_db still present — should be replaced by TenantDB'
assert 'TenantDB' in text, 'TenantDB not imported'
assert 'material_type_code' not in text, 'stale material_type_code still in spool_to_response'
assert 'filament_type' in text, 'filament_type not referenced in response'
from app.api.v1.spools import router
print('spools.py imports OK')
"</automated>
  </verify>
  <done>
    - spools.py imports TenantDB from app.auth.dependencies; get_db import is gone
    - spool_to_response no longer references material_type_code or material_type_name
    - list_spools joins through Spool.filament_type for search/filter
    - All 8 endpoint functions use db: TenantDB (no db: AsyncSession = Depends(get_db) remaining)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix create_spool, update_spool, duplicate_spool; remove export/import</name>
  <files>backend/app/api/v1/spools.py</files>
  <read_first>
    - backend/app/api/v1/spools.py — current state after Task 1 edits (read to see what still needs changing)
  </read_first>
  <action>
    EDIT backend/app/api/v1/spools.py:

    create_spool:
    - Remove the call to ensure_material_type_exists (material_type_id no longer in SpoolCreate)
    - Keep the IntegrityError catch — it will still fire if filament_type_id is invalid
    - The Spool constructor call changes: Spool(tenant_id=tenant.id, **spool_data.model_dump()) — this now includes filament_type_id from SpoolCreate

    update_spool:
    - Remove the "if material_type_id in update_data" block (no longer relevant)
    - Add: if "filament_type_id" in update_data: await ensure_filament_type_exists(db, update_data["filament_type_id"])
    - Keep the rest of the update logic unchanged

    duplicate_spool:
    - The source spool fields to copy: filament_type_id, initial_weight, current_weight, empty_spool_weight, storage_location, purchase_price, supplier, purchase_date, is_active
    - is_labeled should default to False on the duplicate (new physical spool needs a new label)
    - Remove the old copy logic that referenced brand, color, material_type_id etc
    - Keep the auto-generated spool_id logic (it generates a new unique ID — check existing implementation and preserve it)
    - New spool: Spool(tenant_id=tenant.id, filament_type_id=source_spool.filament_type_id, spool_id=new_id, initial_weight=source_spool.initial_weight, current_weight=source_spool.initial_weight, is_labeled=False, ...)

    REMOVE export_spools and import_spools functions entirely:
    - Delete the @router.get("/export", ...) and export_spools function
    - Delete the @router.post("/import", ...) and import_spools function
    - These use stale field names (diameter_mm, location, cost_per_kg) that don't exist (Pitfall 4 in RESEARCH.md)
    - Note in a comment above the delete endpoints: "# export/import removed in Phase 1 (stale field names from pre-migration model)"
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && python -c "
import pathlib
text = pathlib.Path('app/api/v1/spools.py').read_text()

# export/import removed
assert 'export_spools' not in text, 'export_spools not removed'
assert 'import_spools' not in text, 'import_spools not removed'
assert 'diameter_mm' not in text, 'stale field diameter_mm still present'

# ensure material_type check gone from create
import ast
tree = ast.parse(text)
print('spools.py parses OK')

# Verify no export/import routes
from app.api.v1.spools import router
paths = [r.path for r in router.routes]
assert not any('export' in p or 'import' in p for p in paths), f'export/import routes still registered: {paths}'
print('Routes after cleanup:', paths)
"</automated>
  </verify>
  <done>
    - create_spool no longer calls ensure_material_type_exists
    - update_spool calls ensure_filament_type_exists if filament_type_id in update_data
    - duplicate_spool copies filament_type_id (not inline type fields); new spool has is_labeled=False
    - export_spools and import_spools functions removed from file
    - router has no /export or /import routes
    - File parses without SyntaxError
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spool API → Database | Previously bypassed RLS via get_db; now all endpoints use TenantDB enforcing RLS |
| Spool create/update input | filament_type_id validated by ensure_filament_type_exists; invalid FK returns 400 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information Disclosure | Spool list cross-tenant | mitigate | TenantDB replaces get_db on list_spools; RLS policies on spools table now enforced (was bypassed before) |
| T-01-12 | Elevation of Privilege | Spool create with foreign tenant filament_type_id | mitigate | TenantDB sets current_tenant_id; RLS on filament_types blocks cross-tenant FK resolution; IntegrityError returned as 400 |
| T-01-13 | Tampering | Stale export endpoint referencing removed fields | mitigate | export/import endpoints removed entirely to prevent AttributeError and data exposure via stale mapping |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages — only removes unused imports (csv, io, json, yaml) |
</threat_model>

<verification>
```bash
cd /Users/jonathan/Repos/batchivo/backend && python -c "
from app.api.v1.spools import router
import pathlib
text = pathlib.Path('app/api/v1/spools.py').read_text()

# All critical checks
assert 'get_db' not in text
assert 'TenantDB' in text
assert 'export_spools' not in text
assert 'import_spools' not in text
assert 'material_type_code' not in text

routes = [r.path for r in router.routes]
print('Spool routes:', routes)
assert '/' in routes or '' in routes  # list route exists
print('All checks passed')
"
```
</verification>

<success_criteria>
- All spool endpoints use TenantDB; zero occurrences of Depends(get_db) in spools.py
- spool_to_response returns filament_type ORM object (not flat material_type_code strings)
- list_spools joins FilamentType for brand/color search; uses FilamentType.material_type_id for filter
- duplicate_spool copies filament_type_id; is_labeled defaults False on duplicate
- export and import endpoints removed; csv/io/json/yaml/File/UploadFile/StreamingResponse imports removed
</success_criteria>

<output>
Create .planning/phases/01-data-model-migration/01-05-SUMMARY.md when done
</output>
