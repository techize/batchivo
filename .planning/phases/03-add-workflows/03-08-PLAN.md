---
phase: 03-add-workflows
plan: 08
type: execute
wave: 5
depends_on:
  - "03-05"
files_modified:
  - frontend/src/pages/FilamentLibrary.tsx
  - frontend/src/components/filaments/AddFilamentDialog.tsx
autonomous: true
gap_closure: true
requirements:
  - ADD-01
  - ADD-03

must_haves:
  truths:
    - "FilamentLibrary page renders with the same nav, footer, and layout as other authenticated pages"
    - "Quantity field in AddFilamentDialog accepts typed numeric input; values outside 1-20 are clamped"
  artifacts:
    - path: "frontend/src/pages/FilamentLibrary.tsx"
      provides: "FilamentLibrary page wrapped in AppLayout"
      contains: "AppLayout"
    - path: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      provides: "Editable quantity input with clamp validation"
      contains: "type=\"number\""
  key_links:
    - from: "frontend/src/pages/FilamentLibrary.tsx"
      to: "frontend/src/components/layout/AppLayout.tsx"
      via: "import { AppLayout } from '@/components/layout/AppLayout'"
      pattern: "AppLayout"
---

<objective>
Close two frontend-only UAT gaps: missing AppLayout wrapper on the FilamentLibrary page, and the read-only quantity field in AddFilamentDialog.

Purpose: FilamentLibrary must look and behave like every other authenticated page (nav, footer, layout). The quantity field must accept direct typed input, not just button clicks.
Output: FilamentLibrary.tsx wrapped in AppLayout; quantity Input in AddFilamentDialog changed from readOnly to editable with clamp-on-change.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md

<interfaces>
<!-- AppLayout — used on every other authenticated page -->
import { AppLayout } from '@/components/layout/AppLayout'
// Wrap the entire page return: <AppLayout>{...page content...}</AppLayout>
// Reference usages: frontend/src/pages/Printers.tsx, frontend/src/pages/OrderDetailPage.tsx

<!-- Quantity field — current state in AddFilamentDialog.tsx lines 596-601 -->
// Currently:
<Input
  type="number"
  readOnly
  className="w-16 text-center"
  {...field}
/>
// The readOnly prop blocks all keyboard input. Remove it and add an onChange handler
// that clamps the value to [1, 20].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap FilamentLibrary in AppLayout</name>
  <files>frontend/src/pages/FilamentLibrary.tsx</files>
  <read_first>
    - frontend/src/pages/FilamentLibrary.tsx (read the current return statement — confirm it starts with bare `<div>` with no AppLayout wrapper)
    - frontend/src/pages/Printers.tsx (see the exact import line and how AppLayout is used as the outermost wrapper)
  </read_first>
  <action>
    1. Add import at the top of the imports block:
       `import { AppLayout } from '@/components/layout/AppLayout'`

    2. Wrap the entire return value in `<AppLayout>`. The current return is:
       `return ( <div className="space-y-6 p-4 sm:p-6"> ... </div> )`
       Change it to:
       `return ( <AppLayout> <div className="space-y-6 p-4 sm:p-6"> ... </div> </AppLayout> )`

    Do not change any other logic, state, or JSX inside the div.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "FilamentLibrary" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AppLayout" frontend/src/pages/FilamentLibrary.tsx` returns 2 (one import, one usage)
    - `grep "import.*AppLayout" frontend/src/pages/FilamentLibrary.tsx` matches `import { AppLayout } from '@/components/layout/AppLayout'`
    - TypeScript compile produces no errors for FilamentLibrary.tsx: `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "FilamentLibrary"` returns empty
    - The return JSX has `<AppLayout>` as the outermost element: `grep -c "<AppLayout>" frontend/src/pages/FilamentLibrary.tsx` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make quantity field editable with clamp validation</name>
  <files>frontend/src/components/filaments/AddFilamentDialog.tsx</files>
  <read_first>
    - frontend/src/components/filaments/AddFilamentDialog.tsx (read lines 577-617 — the quantity FormField and the handleQuantityChange function at lines 246-249)
  </read_first>
  <action>
    In the quantity `FormField` render block (around line 584), the `<Input>` currently has `readOnly` prop. Make two changes:

    1. Remove the `readOnly` attribute from the `<Input>`.

    2. Add an `onChange` handler that clamps the typed value to the [1, 20] range. Replace the spread `{...field}` with explicit props that include a custom onChange:
       ```
       value={field.value}
       onChange={(e) => {
         const raw = parseInt(e.target.value, 10)
         if (!isNaN(raw)) {
           field.onChange(Math.max(1, Math.min(20, raw)))
         }
       }}
       onBlur={field.onBlur}
       name={field.name}
       ref={field.ref}
       ```
       Keep `type="number"`, `className="w-16 text-center"`, `min={1}`, `max={20}`.
       Add `min={1}` and `max={20}` HTML attributes so the browser also enforces the range.

    The `handleQuantityChange` function (used by +/- buttons) does not need changes — it already clamps via `Math.max(1, Math.min(20, ...))`.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "AddFilamentDialog" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readOnly" frontend/src/components/filaments/AddFilamentDialog.tsx` returns 0 (readOnly removed from the quantity input; no other readOnly attributes exist in the file)
    - The quantity input now has `min={1}` and `max={20}`: `grep "min={1}" frontend/src/components/filaments/AddFilamentDialog.tsx` matches at least one line
    - The onChange handler calls `Math.max(1, Math.min(20,` for clamping: `grep "Math.max(1, Math.min(20" frontend/src/components/filaments/AddFilamentDialog.tsx` returns at least one match
    - TypeScript compile produces no errors for AddFilamentDialog.tsx: `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "AddFilamentDialog"` returns empty
    - Frontend test suite still passes: `cd frontend && npm run test 2>&1 | tail -5` shows no NEW failures (existing failures are acceptable)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → quantity input | User can now type arbitrary values into the quantity field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | AddFilamentDialog quantity input | mitigate | Client-side clamp to [1, 20] on change; Zod schema (`max(20)`) enforces server-bound constraint; backend validates quantity via Pydantic |
| T-03-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `grep -c "AppLayout" frontend/src/pages/FilamentLibrary.tsx` returns 2
- `grep -c "readOnly" frontend/src/components/filaments/AddFilamentDialog.tsx` returns 0
- `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "FilamentLibrary|AddFilamentDialog"` returns empty (no compile errors)
</verification>

<success_criteria>
- FilamentLibrary page is wrapped in AppLayout — navigation, footer, and layout chrome appear on the page
- Quantity field accepts typed input; typing 5 sets quantity to 5; typing 25 clamps to 20; typing 0 clamps to 1
- No TypeScript compile errors introduced
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-08-SUMMARY.md` when done
</output>
