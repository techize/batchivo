---
phase: 03-add-workflows
plan: 07
type: execute
wave: 4
depends_on:
  - "03-04"
  - "03-05"
files_modified:
  - frontend/src/components/filaments/AddFilamentDialog.test.tsx
autonomous: true
requirements:
  - ADD-01
  - ADD-03
  - ADD-04
  - ADD-05

must_haves:
  truths:
    - "Dialog opens when Add Filament button is triggered"
    - "Mode selector shows two workflow cards with correct copy"
    - "Clicking bulk card shows bulk form; clicking batch card shows batch form"
    - "Back button returns to mode selector from either form"
    - "Bulk form: submitting calls useBulkCreateFilamentType.mutateAsync with correct shape"
    - "Batch form: 'Submit all' disabled when rows empty; enabled after adding row"
    - "Batch form: 'Add color' appends row to table; 'Submit all' calls useBatchCreateFilamentTypes.mutateAsync"
    - "All AddFilamentDialog.test.tsx tests pass"
    - "D-04: Frontend test verifies that after bulk add succeeds Dialog closes (onOpenChange called with false)"
    - "D-05: Frontend test verifies that after batch submit succeeds Dialog stays open, table clears, form resets"
    - "D-13: Frontend test verifies 'Add color' appends a row to the accumulator table and 'Submit all' sends all accumulated entries"
    - "D-14: Frontend test verifies after 'Submit all' succeeds table clears and form resets to last-used values"
  artifacts:
    - path: "frontend/src/components/filaments/AddFilamentDialog.test.tsx"
      provides: "Passing component tests for Dialog state machine and mutation calls"
      contains: "describe('AddFilamentDialog'"
  key_links:
    - from: "frontend/src/components/filaments/AddFilamentDialog.test.tsx"
      to: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      via: "import { AddFilamentDialog } from './AddFilamentDialog'"
      pattern: "AddFilamentDialog"
---

<objective>
Flesh out the AddFilamentDialog test stubs created in plan 03-01 with real assertions now that the component exists (plan 03-04) and is wired into FilamentLibrary (plan 03-05).

Purpose: Automated validation that the Dialog state machine, form submissions, and mutation calls work correctly.
Output: AddFilamentDialog.test.tsx with passing tests for all Dialog behaviors.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md
@.planning/phases/03-add-workflows/03-VALIDATION.md

<interfaces>
<!-- AddFilamentDialog props -->
interface AddFilamentDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
}
export { AddFilamentDialog }  // named export from './AddFilamentDialog'

<!-- Mocks needed -->
vi.mock('@/hooks/useFilamentTypes', () => ({
  useBulkCreateFilamentType: () => ({ mutateAsync: vi.fn().mockResolvedValue({ filament_type_id: 'ft-1', spool_ids: ['FIL-001'] }), isPending: false, isError: false, error: null }),
  useBatchCreateFilamentTypes: () => ({ mutateAsync: vi.fn().mockResolvedValue({ results: [{ filament_type_id: 'ft-1', spool_id: 'FIL-001' }] }), isPending: false, isError: false, error: null }),
}))
vi.mock('@/lib/api/spools', () => ({
  materialTypesApi: { list: () => Promise.resolve([{ id: 'mat-1', name: 'PLA', code: 'PLA' }]) }
}))

<!-- Dialog state machine text markers (from 03-UI-SPEC.md Copywriting Contract) -->
Mode selector heading: "How would you like to add filament?"
Bulk card: "Batch of identical spools"
Batch card: "Multiple color variants"
Bulk Dialog title: "Add filament — batch"
Batch Dialog title: "Add filament — color variants"
Bulk submit: "Add spools"
Batch add-row: "Add color"
Batch submit: "Submit all"
Back button: aria-label="Back to mode selector"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AddFilamentDialog.test.tsx with real assertions</name>
  <files>frontend/src/components/filaments/AddFilamentDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/filaments/AddFilamentDialog.test.tsx (read current stub structure — see which tests are stubs/todos to flesh out)
    - frontend/src/components/filaments/AddFilamentDialog.tsx (read full component — understand rendered text, data-testid usage, form field structure, and how mutation hooks are called)
    - frontend/src/components/filaments/FilamentTypeFilterSheet.tsx (check if there is a co-located test file for reference on QueryClient wrapper pattern in filament component tests)
    - .planning/phases/03-add-workflows/03-UI-SPEC.md (Copywriting Contract — exact button/heading text)
  </read_first>
  <action>
    Replace the stub test file content with a complete, working test suite. Keep the same describe/it structure but replace placeholder bodies.

    SETUP:
    - Import: `import { describe, it, expect, vi, beforeEach } from 'vitest'`
    - Import: `import { render, screen, fireEvent, waitFor } from '@testing-library/react'`
    - Import: `import userEvent from '@testing-library/user-event'`
    - Import: `import { QueryClient, QueryClientProvider } from '@tanstack/react-query'`
    - Import: `import { AddFilamentDialog } from './AddFilamentDialog'`

    Create helper: `function renderDialog(props = { open: true, onOpenChange: vi.fn() }) { const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } }); return render(<QueryClientProvider client={qc}><AddFilamentDialog {...props} /></QueryClientProvider>) }`

    Mock declarations (at module level, before describe):
    - `const mockBulkMutateAsync = vi.fn().mockResolvedValue({ filament_type_id: 'ft-1', spool_ids: ['FIL-001', 'FIL-002'] })`
    - `const mockBatchMutateAsync = vi.fn().mockResolvedValue({ results: [{ filament_type_id: 'ft-1', spool_id: 'FIL-001' }] })`
    - vi.mock('@/hooks/useFilamentTypes', () => ({ useBulkCreateFilamentType: () => ({ mutateAsync: mockBulkMutateAsync, isPending: false, isError: false, error: null }), useBatchCreateFilamentTypes: () => ({ mutateAsync: mockBatchMutateAsync, isPending: false, isError: false, error: null }) }))
    - vi.mock('@/lib/api/spools', () => ({ materialTypesApi: { list: () => Promise.resolve([{ id: 'mat-1', name: 'PLA', code: 'PLA' }]) } }))
    - beforeEach: `vi.clearAllMocks()`

    TEST IMPLEMENTATIONS:

    "renders mode selector when open":
      renderDialog()
      expect(screen.getByText('How would you like to add filament?')).toBeInTheDocument()
      expect(screen.getByText('Batch of identical spools')).toBeInTheDocument()
      expect(screen.getByText('Multiple color variants')).toBeInTheDocument()

    "clicking bulk card shows bulk form":
      renderDialog()
      await userEvent.click(screen.getByText('Batch of identical spools'))
      expect(screen.getByText('Add filament — batch')).toBeInTheDocument()
      expect(screen.getByText('Add spools')).toBeInTheDocument()

    "clicking batch card shows batch form":
      renderDialog()
      await userEvent.click(screen.getByText('Multiple color variants'))
      expect(screen.getByText('Add filament — color variants')).toBeInTheDocument()
      expect(screen.getByText('Submit all')).toBeInTheDocument()

    "back button from bulk form returns to mode selector":
      renderDialog(); navigate to bulk mode
      const backBtn = screen.getByRole('button', { name: /back to mode selector/i })
      await userEvent.click(backBtn)
      expect(screen.getByText('How would you like to add filament?')).toBeInTheDocument()

    "back button from batch form returns to mode selector":
      Similar to above but navigate to batch mode first

    "Submit all is disabled when batch rows are empty":
      renderDialog()
      await userEvent.click(screen.getByText('Multiple color variants'))
      const submitBtn = screen.getByRole('button', { name: /submit all/i })
      expect(submitBtn).toBeDisabled()

    "Add color appends a row to the batch table":
      renderDialog(); navigate to batch mode
      Fill brand field with "JAYO"; fill color field with "Red"; select material type (if Select is testable)
      await userEvent.click(screen.getByRole('button', { name: /add color/i }))
      await waitFor(() => expect(screen.getByText('JAYO')).toBeInTheDocument())
      await waitFor(() => expect(screen.getByRole('button', { name: /submit all/i })).not.toBeDisabled())

    "Submit all calls batchCreate mutation with accumulated entries":
      Add a row to batch table as above
      await userEvent.click(screen.getByRole('button', { name: /submit all/i }))
      await waitFor(() => expect(mockBatchMutateAsync).toHaveBeenCalledOnce())
      const callArg = mockBatchMutateAsync.mock.calls[0][0]
      expect(callArg).toHaveProperty('entries')
      expect(callArg.entries.length).toBe(1)
      expect(callArg.entries[0].brand).toBe('JAYO')

    NOTE: For the Select component (material type), shadcn/ui Select uses Radix UI which may need `await userEvent.click(trigger); await userEvent.click(option)`. If the Select cannot be easily driven in jsdom, use fireEvent to directly call onChange or mock the form value. Document any such workaround inline.

    NOTE: Add necessary Form and react-hook-form resolver mocks if Zod schemas fail in test environment (usually they work, but if tests fail with zodResolver errors, use `mode: 'onSubmit'` explicitly in the test render). Do not mock react-hook-form itself.

    Run tests after each test implementation to confirm passing before moving to next.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npm run test -- --reporter=verbose AddFilamentDialog 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- AddFilamentDialog 2>&1 | grep -E "PASS|FAIL"` shows PASS (not FAIL)
    - At least 8 tests in describe('AddFilamentDialog') — `grep -c "^\s*it(" frontend/src/components/filaments/AddFilamentDialog.test.tsx` returns at least 8
    - No `it.todo` or `it.skip` remaining: `grep -c "it.todo\|it.skip" frontend/src/components/filaments/AddFilamentDialog.test.tsx` returns 0
    - Test for "Submit all disabled when rows empty" passes
    - Test for "batchCreate called with entries" passes and asserts on the mutation call argument shape
    - `cd frontend && npm run test 2>&1 | tail -5` — full frontend test suite passes (no regressions)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → component | Mutation mocks validate the component calls the right function with the right shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | Frontend form submission | mitigate | Tests verify mutation is called with correct payload shape (no spool_id in request body, quantity bounded) |
| T-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `cd frontend && npm run test -- AddFilamentDialog` — all tests PASS
- `cd frontend && npm run test` — full suite passes
- All 8+ tests implemented with real assertions (no it.todo/it.skip)
</verification>

<success_criteria>
- AddFilamentDialog.test.tsx has 8+ passing tests
- Mode selector, navigation, bulk submission, and batch accumulation all tested
- Full frontend test suite remains green
- Tests verify mutation call argument shapes (not just that mutations were called)
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-07-SUMMARY.md` when done
</output>
