---
phase: 03-add-workflows
plan: 04
type: execute
wave: 2
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - frontend/src/components/filaments/AddFilamentDialog.tsx
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - ADD-04
  - ADD-05

must_haves:
  truths:
    - "AddFilamentDialog renders a mode selector with two clickable cards when opened"
    - "Mode selector shows 'Batch of identical spools' and 'Multiple color variants' cards"
    - "Clicking bulk card navigates to bulk add form (title: 'Add filament — batch')"
    - "Clicking batch card navigates to rapid batch form (title: 'Add filament — color variants')"
    - "Back button returns to mode selector without resetting form state"
    - "Bulk add form has brand, color, material type (required), finish, notes (optional), 'More options' collapsible, weight field, quantity +/- buttons (1–20)"
    - "Rapid batch form has shared weight field, entry form, accumulator table, 'Add color' and 'Submit all' buttons"
    - "'Submit all' is disabled when accumulator table is empty"
    - "After bulk add succeeds: Dialog closes"
    - "After batch submit succeeds: Dialog stays open, table clears, form resets to last-used values"
    - "API errors displayed via Alert variant=destructive"
    - "D-02: AddFilamentDialog is a shadcn/ui Dialog (not Sheet), fixed max-w-lg with scrollable content"
    - "D-03: First screen inside the Dialog is a mode selector — user consciously selects 'batch of identical spools' or 'multiple color variants'"
    - "D-04: After bulk add succeeds Dialog closes and FilamentLibrary list refreshes"
    - "D-05: After rapid batch final submit succeeds Dialog stays open, table clears, form resets"
    - "D-06: Bulk add form is a single scrollable form — no wizard steps, all fields visible at once"
    - "D-07: Visible fields: brand, color, material type, finish, notes plus 'More options' collapsible containing color_hex, diameter, extruder_temp, bed_temp, density, pattern, translucent, glow, spool_type"
    - "D-08: Quantity field is a numeric input with +/- buttons, range 1–20; quantity=0 is disallowed"
    - "D-09: Weight per spool is a single numeric input field defaulting to 1000g, applied to all auto-generated spools"
    - "D-10: Rapid batch Dialog shows shared weight field at the top (default 1000g) before the entry form"
    - "D-11: Each color step shows all FilamentType fields pre-filled from the previous entry"
    - "D-12: Each step creates 1 spool — no per-step quantity field"
    - "D-13: Form accumulates entries — 'Add color' appends a row to a table; nothing sent to backend until 'Submit all'"
    - "D-14: After 'Submit all' succeeds Dialog stays open, table clears, form resets to last-used values"
    - "D-22: Both bulk add and rapid batch forms show a back button (arrow icon) that returns to the mode selector"
    - "D-23: Form state is preserved on back navigation — state held at Dialog level, not inside mode-specific sub-components"
  artifacts:
    - path: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      provides: "Complete add workflow Dialog component with three screens"
      contains: "AddFilamentDialog"
  key_links:
    - from: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      to: "frontend/src/hooks/useFilamentTypes.ts"
      via: "useBulkCreateFilamentType, useBatchCreateFilamentTypes"
      pattern: "useBulkCreateFilamentType"
    - from: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      to: "frontend/src/lib/api/spools.ts"
      via: "materialTypesApi.list()"
      pattern: "materialTypesApi"
---

<objective>
Create AddFilamentDialog.tsx — the complete add workflow Dialog component with three screens: mode selector, bulk add form, and rapid batch form. State is held at the Dialog level per D-23.

Purpose: Users can create FilamentType + Spool records through a focused Dialog without leaving the FilamentLibrary page.
Output: A single AddFilamentDialog.tsx component that the FilamentLibrary page will import (plan 03-05).
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md
@.planning/phases/03-add-workflows/03-UI-SPEC.md
@.planning/phases/03-add-workflows/03-PATTERNS.md

<interfaces>
<!-- From frontend/src/hooks/useFilamentTypes.ts — hooks to import (added in plan 03-03) -->
export function useBulkCreateFilamentType():
  UseMutationResult<BulkCreateResponse, Error, BulkCreateRequest>
export function useBatchCreateFilamentTypes():
  UseMutationResult<BatchCreateResponse, Error, BatchCreateRequest>

<!-- From frontend/src/types/filament-type.ts — types added in plan 03-03 -->
export interface BulkCreateRequest { material_type_id, brand, color, color_hex?, finish?, pattern?, spool_type?, diameter?, density?, extruder_temp?, bed_temp?, translucent?, glow?, notes?, quantity, initial_weight }
export interface BulkCreateResponse { filament_type_id: string; spool_ids: string[] }
export interface BatchEntryRequest { material_type_id, brand, color, color_hex?, finish?, ... }
export interface BatchCreateRequest { entries: BatchEntryRequest[]; initial_weight: number }
export interface BatchCreateResponse { results: Array<{filament_type_id: string; spool_id: string}> }

<!-- From frontend/src/components/production-runs/CreateRunWizard.tsx — reference architecture -->
Pattern: mode state at container level; useForm calls at container level; pass form+handlers down as props.
Accumulator: rows held in useState<BatchRowEntry[]>([]); reset form to last entry on "Add color".
Error: Alert variant="destructive" with AlertDescription.
isPending: Button disabled + Loader2 animate-spin.

<!-- From frontend/src/components/filaments/FilamentTypeFilterSheet.tsx — materialTypesApi pattern -->
const { data: materialTypes } = useQuery({ queryKey: ['material-types'], queryFn: () => materialTypesApi.list() })
import { materialTypesApi } from '@/lib/api/spools'

<!-- From 03-UI-SPEC.md — component copy and layout constraints -->
Dialog: max-w-lg, overflow-y-auto max-h-[90vh]
Mode selector: grid grid-cols-1 sm:grid-cols-2 gap-4
Bulk form field order: brand → color → material type → finish → [More options Collapsible] → divider → weight → quantity
Rapid batch: shared weight at top, entry form (brand/color/material/finish), table (Brand|Color|Material|Finish|remove)
Button copy: "Add Filament" (trigger, not in Dialog), "Add spools" (bulk submit), "Add color" (batch append), "Submit all" (batch submit)
Dialog titles: "Add filament — batch" | "Add filament — color variants"
Mode selector heading: "How would you like to add filament?"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AddFilamentDialog.tsx with mode selector, bulk add form, and rapid batch form</name>
  <files>frontend/src/components/filaments/AddFilamentDialog.tsx</files>
  <read_first>
    - frontend/src/components/production-runs/CreateRunWizard.tsx (read lines 1–100 for imports; lines 143–160 for state machine; lines 329–368 for accumulator pattern; lines 537–585 for submit button + error display)
    - frontend/src/components/filaments/FilamentTypeFilterSheet.tsx (lines 40–70 for materialTypesApi.list() usage and Select render pattern)
    - .planning/phases/03-add-workflows/03-UI-SPEC.md (Layout Contract, State Contract, Copywriting Contract, Form Validation Contract sections)
    - .planning/phases/03-add-workflows/03-CONTEXT.md (D-01 through D-23 — all locked decisions; especially D-22, D-23, D-04, D-05, D-14)
    - .planning/phases/03-add-workflows/03-PATTERNS.md (AddFilamentDialog section — all imports, state machine code, accumulator patterns)
  </read_first>
  <action>
    Create frontend/src/components/filaments/AddFilamentDialog.tsx as a new file.

    IMPORTS (top of file):
    - React: `import { useState } from 'react'`
    - TanStack Query: `import { useQuery, useQueryClient } from '@tanstack/react-query'`
    - react-hook-form + zod: `import { useForm } from 'react-hook-form'`; `import { zodResolver } from '@hookform/resolvers/zod'`; `import { z } from 'zod'`
    - Icons: `import { ArrowLeft, ChevronDown, ChevronUp, Loader2, Minus, Package, Layers, Plus, Trash2 } from 'lucide-react'`
    - shadcn/ui Dialog: `import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog'`
    - shadcn/ui Card: `import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'`
    - shadcn/ui Form: `import { Form, FormField, FormItem, FormLabel, FormControl, FormMessage } from '@/components/ui/form'`
    - shadcn/ui inputs: `import { Input } from '@/components/ui/input'`; `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'`; `import { Button } from '@/components/ui/button'`
    - shadcn/ui Collapsible: `import { Collapsible, CollapsibleContent, CollapsibleTrigger } from '@/components/ui/collapsible'`
    - shadcn/ui Alert: `import { Alert, AlertDescription } from '@/components/ui/alert'`
    - shadcn/ui Table: `import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'`
    - Hooks: `import { useBulkCreateFilamentType, useBatchCreateFilamentTypes } from '@/hooks/useFilamentTypes'`
    - API: `import { materialTypesApi } from '@/lib/api/spools'`
    - Types: `import type { BulkCreateRequest, BatchEntryRequest, BatchCreateRequest } from '@/types/filament-type'`

    TYPES (after imports):
    - `type DialogMode = 'selector' | 'bulk' | 'batch'`
    - BulkAddFormValues type derived from bulkSchema (z.infer<typeof bulkSchema>)
    - BatchEntryFormValues type derived from batchEntrySchema (z.infer<typeof batchEntrySchema>)
    - `type BatchRowEntry = BatchEntryRequest & { id: string }` (add local id for React key)

    ZOD SCHEMAS (after types):
    bulkSchema = z.object({
      brand: z.string().min(1, 'Brand is required'),
      color: z.string().min(1, 'Color is required'),
      material_type_id: z.string().min(1, 'Material type is required'),
      finish: z.string().optional(),
      notes: z.string().optional(),
      color_hex: z.string().regex(/^#[0-9a-fA-F]{6}$/, 'Must be a valid hex color').optional().or(z.literal('')),
      diameter: z.number().min(0.1).max(5).optional(),
      extruder_temp: z.number().int().min(150).max(320).optional(),
      bed_temp: z.number().int().min(0).max(120).optional(),
      density: z.number().min(0.5).max(2.5).optional(),
      pattern: z.string().optional(),
      translucent: z.boolean().optional(),
      glow: z.boolean().optional(),
      spool_type: z.string().optional(),
      quantity: z.number().int().min(1, 'Must add at least 1 spool').max(20, 'Maximum 20 spools per batch'),
      initial_weight: z.number().positive(),
    })

    batchEntrySchema = z.object({
      brand: z.string().min(1, 'Brand is required'),
      color: z.string().min(1, 'Color is required'),
      material_type_id: z.string().min(1, 'Material type is required'),
      finish: z.string().optional(),
      color_hex: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional().or(z.literal('')),
      pattern: z.string().optional(),
      spool_type: z.string().optional(),
      diameter: z.number().min(0.1).max(5).optional(),
      density: z.number().min(0.5).max(2.5).optional(),
      extruder_temp: z.number().int().min(150).max(320).optional(),
      bed_temp: z.number().int().min(0).max(120).optional(),
      translucent: z.boolean().optional(),
      glow: z.boolean().optional(),
      notes: z.string().optional(),
    })

    COMPONENT PROPS:
    interface AddFilamentDialogProps {
      open: boolean
      onOpenChange: (open: boolean) => void
    }

    COMPONENT BODY (AddFilamentDialog):
    State (all at Dialog level per D-23):
    - mode: DialogMode, initially 'selector'
    - moreOptionsOpen: boolean, initially false
    - sharedWeight: number, initially 1000
    - batchRows: BatchRowEntry[], initially []
    - bulkForm = useForm<BulkAddFormValues>({ resolver: zodResolver(bulkSchema), defaultValues: { quantity: 1, initial_weight: 1000 } })
    - batchForm = useForm<BatchEntryFormValues>({ resolver: zodResolver(batchEntrySchema), defaultValues: {} })
    - bulkMutation = useBulkCreateFilamentType()
    - batchMutation = useBatchCreateFilamentTypes()
    - materialTypes query = useQuery({ queryKey: ['material-types'], queryFn: () => materialTypesApi.list() })

    handleClose: setMode('selector'); props.onOpenChange(false) — also reset forms on close (bulkForm.reset(), batchForm.reset(), setBatchRows([])) per expected UX; do NOT reset on back navigation.
    handleBack: setMode('selector') — form state NOT reset (D-23).
    handleBulkSubmit (form values): build BulkCreateRequest from values (pass color_hex as-is — API client in plan 03-03 handles '#' stripping before sending to backend); call bulkMutation.mutateAsync(payload); on success: handleClose().
    handleAddColor (form values): append { ...values, id: crypto.randomUUID() } to batchRows; batchForm.reset({ ...values, color: '' }) per D-11.
    handleBatchSubmit: build BatchCreateRequest from batchRows (map to BatchEntryRequest, pass color_hex as-is — API client in plan 03-03 handles '#' stripping before sending to backend); call batchMutation.mutateAsync; on success: capture last entry, setBatchRows([]), batchForm.reset({ ...lastEntry, color: '' }) per D-14.
    handleQuantityChange(delta): update quantity field: Math.max(1, Math.min(20, current + delta)).

    RENDER structure:
    <Dialog open={props.open} onOpenChange={(open) => { if (!open) handleClose(); else props.onOpenChange(true) }}>
      <DialogContent className="max-w-lg overflow-y-auto max-h-[90vh]">
        <DialogHeader>
          {mode !== 'selector' && (
            <Button variant="ghost" size="icon" onClick={handleBack} aria-label="Back to mode selector">
              <ArrowLeft className="h-4 w-4" />
            </Button>
          )}
          <DialogTitle>
            {mode === 'selector' ? 'How would you like to add filament?' : mode === 'bulk' ? 'Add filament — batch' : 'Add filament — color variants'}
          </DialogTitle>
          {mode === 'selector' && <DialogDescription>Choose a workflow to continue</DialogDescription>}
        </DialogHeader>

        {/* MODE SELECTOR */}
        {mode === 'selector' && (
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 p-1">
            <Card className="cursor-pointer hover:border-primary" onClick={() => setMode('bulk')}>
              <CardHeader>
                <Package className="h-8 w-8 mb-2 text-muted-foreground" />
                <CardTitle className="text-base">Batch of identical spools</CardTitle>
                <CardDescription>Create one filament type and generate multiple spools at once</CardDescription>
              </CardHeader>
            </Card>
            <Card className="cursor-pointer hover:border-primary" onClick={() => setMode('batch')}>
              <CardHeader>
                <Layers className="h-8 w-8 mb-2 text-muted-foreground" />
                <CardTitle className="text-base">Multiple color variants</CardTitle>
                <CardDescription>Set a shared weight then add different colors in rapid succession</CardDescription>
              </CardHeader>
            </Card>
          </div>
        )}

        {/* BULK ADD FORM */}
        {mode === 'bulk' && (
          <Form {...bulkForm}>
            <form onSubmit={bulkForm.handleSubmit(handleBulkSubmit)} className="space-y-4">
              {/* brand, color, material_type_id (Select), finish, notes */}
              {/* Collapsible "More options": color_hex, diameter, extruder_temp, bed_temp, density, pattern, translucent, glow, spool_type */}
              {/* Collapsible toggle button uses ChevronDown/ChevronUp, default collapsed */}
              {/* Divider */}
              {/* weight per spool Input (label: "Weight per spool (g)", defaultValue 1000) */}
              {/* quantity row: Minus button | Input type number | Plus button (min 44px height each) */}
              {bulkMutation.isError && (
                <Alert variant="destructive">
                  <AlertDescription>
                    {(bulkMutation.error as Error)?.message === 'Request failed with status code 409'
                      ? 'A spool ID collision occurred. Please try again.'
                      : 'Something went wrong. Check your connection and try again.'}
                  </AlertDescription>
                </Alert>
              )}
              <DialogFooter>
                <Button type="submit" className="w-full" disabled={bulkMutation.isPending}>
                  {bulkMutation.isPending ? (<><Loader2 className="mr-2 h-4 w-4 animate-spin" />Adding...</>) : 'Add spools'}
                </Button>
              </DialogFooter>
            </form>
          </Form>
        )}

        {/* RAPID BATCH FORM */}
        {mode === 'batch' && (
          <div className="space-y-4">
            {/* Shared weight field: label "Weight per spool (g)", Input w-32, onChange updates sharedWeight state */}
            <Form {...batchForm}>
              <form onSubmit={batchForm.handleSubmit(handleAddColor)} className="space-y-4">
                {/* brand, color (required), material_type_id (Select), finish */}
                <Button type="submit" variant="outline" className="w-full">Add color</Button>
              </form>
            </Form>
            {batchRows.length > 0 && (
              <Table>
                <TableHeader>
                  <TableRow>
                    <TableHead>Brand</TableHead>
                    <TableHead>Color</TableHead>
                    <TableHead>Material</TableHead>
                    <TableHead>Finish</TableHead>
                    <TableHead></TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {batchRows.map((row, index) => (
                    <TableRow key={row.id}>
                      <TableCell>{row.brand}</TableCell>
                      <TableCell>
                        {row.color_hex && (
                          <span className="inline-block w-3 h-3 rounded-full border mr-2" style={{ backgroundColor: row.color_hex }} />
                        )}
                        {row.color}
                      </TableCell>
                      <TableCell>{/* material type name — look up from materialTypes by material_type_id */}</TableCell>
                      <TableCell>{row.finish ?? '—'}</TableCell>
                      <TableCell>
                        <Button variant="ghost" size="icon" aria-label="Remove this entry"
                          onClick={() => setBatchRows(prev => prev.filter((_, i) => i !== index))}>
                          <Trash2 className="h-4 w-4 text-destructive" />
                        </Button>
                      </TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            )}
            {batchRows.length === 0 && (
              <p className="text-sm text-muted-foreground text-center py-4">
                Add at least one color variant above to continue
              </p>
            )}
            {batchMutation.isError && (
              <Alert variant="destructive">
                <AlertDescription>
                  {(batchMutation.error as Error)?.message?.includes('409')
                    ? 'A spool ID collision occurred. Please try again.'
                    : 'Something went wrong. Check your connection and try again.'}
                </AlertDescription>
              </Alert>
            )}
            <DialogFooter>
              <Button className="w-full" disabled={batchRows.length === 0 || batchMutation.isPending}
                onClick={handleBatchSubmit}>
                {batchMutation.isPending ? (<><Loader2 className="mr-2 h-4 w-4 animate-spin" />Submitting...</>) : 'Submit all'}
              </Button>
            </DialogFooter>
          </div>
        )}
      </DialogContent>
    </Dialog>

    Export: `export { AddFilamentDialog }` (named export, not default).

    NOTE on material type name in table: look up `materialTypes?.find(m => m.id === row.material_type_id)?.name ?? row.material_type_id` for the Material column in the batch table.

    NOTE on Form fields: Each field must use FormField with Controller pattern from react-hook-form integration. Use shadcn Form components (FormItem, FormLabel, FormControl, FormMessage) for all inputs. The Select for material type must use Controller render prop to wire value/onChange to react-hook-form.

    NOTE on quantity +/- buttons: use Button variant="outline" size="icon" with onClick calling handleQuantityChange. Use `bulkForm.watch('quantity')` to display current value. Ensure button height meets 44px minimum for accessibility.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "AddFilamentDialog"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/components/filaments/AddFilamentDialog.tsx
    - `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "AddFilamentDialog"` produces no TypeScript errors
    - `grep -c "useBulkCreateFilamentType\|useBatchCreateFilamentTypes" frontend/src/components/filaments/AddFilamentDialog.tsx` returns 2 (both hooks used)
    - `grep "materialTypesApi" frontend/src/components/filaments/AddFilamentDialog.tsx` returns a match
    - `grep "setMode.*selector\|mode.*selector" frontend/src/components/filaments/AddFilamentDialog.tsx` returns at least 2 matches (back navigation + close)
    - `grep "handleClose\|onOpenChange.*false" frontend/src/components/filaments/AddFilamentDialog.tsx` returns at least 1 match (bulk success closes Dialog)
    - `grep "setBatchRows.*\[\]\|batchRows.*length.*0" frontend/src/components/filaments/AddFilamentDialog.tsx` returns a match (table cleared on success)
    - `grep "Submit all" frontend/src/components/filaments/AddFilamentDialog.tsx` returns a match
    - `grep "Add spools" frontend/src/components/filaments/AddFilamentDialog.tsx` returns a match
    - `grep "disabled.*batchRows.length === 0" frontend/src/components/filaments/AddFilamentDialog.tsx` returns a match (Submit all disabled when empty)
    - `export { AddFilamentDialog }` or `export function AddFilamentDialog` present (named export)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → API payload | User inputs brand/color/material/quantity cross the Dialog boundary into the mutation call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | BulkAddForm quantity input | mitigate | Zod schema enforces ge=1 le=20 on form submit; backend enforces independently via Pydantic |
| T-03-07 | Tampering | spool_id field | accept | No spool_id field in form; IDs are server-generated only |
| T-03-SC | Tampering | npm installs | accept | No new packages installed |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit --skipLibCheck` — no TS errors in AddFilamentDialog.tsx
- `cd frontend && npm run lint` — no lint errors in AddFilamentDialog.tsx
- Component exports AddFilamentDialog named export
- Dialog state machine: mode selector → bulk → back → selector works
- Bulk form: submit calls useBulkCreateFilamentType
- Batch form: "Submit all" disabled when rows=0; calls useBatchCreateFilamentTypes
</verification>

<success_criteria>
- AddFilamentDialog.tsx compiles without TypeScript errors
- Mode selector, bulk form, and rapid batch form all present as separate screens
- Form state preserved across back navigation (held at Dialog level)
- Success behaviors match D-04 and D-14
- Error display uses Alert variant="destructive"
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-04-SUMMARY.md` when done
</output>
