---
phase: 03-add-workflows
plan: 05
type: execute
wave: 3
depends_on:
  - "03-04"
files_modified:
  - frontend/src/pages/FilamentLibrary.tsx
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - ADD-04
  - ADD-05

must_haves:
  truths:
    - "FilamentLibrary header shows [Search] [Filters] [Add Filament] layout with Add Filament as primary variant button"
    - "Clicking 'Add Filament' opens the AddFilamentDialog"
    - "AddFilamentDialog is rendered alongside the existing sheet siblings at the bottom of the page"
    - "After bulk add succeeds, filament library list refreshes with newly created types showing 'Needs label' badges"
    - "D-01: The '+ Add' button lives in the FilamentLibrary page header beside the existing Filters button (layout: [Search] [Filters] [+ Add])"
  artifacts:
    - path: "frontend/src/pages/FilamentLibrary.tsx"
      provides: "Add Filament button wired to AddFilamentDialog"
      contains: "addDialogOpen"
  key_links:
    - from: "frontend/src/pages/FilamentLibrary.tsx"
      to: "frontend/src/components/filaments/AddFilamentDialog.tsx"
      via: "AddFilamentDialog open={addDialogOpen} onOpenChange={setAddDialogOpen}"
      pattern: "AddFilamentDialog"
---

<objective>
Wire AddFilamentDialog into the FilamentLibrary page. Add the '+ Add Filament' button to the header controls row, manage its open state, and render the Dialog alongside the existing sheet siblings.

Purpose: Users can access the add workflow from the FilamentLibrary page with a single click on the primary action button.
Output: FilamentLibrary.tsx modified with Dialog open state, Add Filament button, and AddFilamentDialog component rendered.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md
@.planning/phases/03-add-workflows/03-UI-SPEC.md

<interfaces>
<!-- From frontend/src/pages/FilamentLibrary.tsx — current header structure (read before modifying) -->
Existing state (lines 27–32):
  const [spoolSheetFilamentTypeId, setSpoolSheetFilamentTypeId] = useState<string | null>(null)
  const [spoolSheetFilamentTypeName, setSpoolSheetFilamentTypeName] = useState<...>(null)
  const [filterSheetOpen, setFilterSheetOpen] = useState(false)
  const [params, setParams] = useState<FilamentTypeListParams>({ page: 1, page_size: 20 })

Existing controls row (lines 82–105 approx):
  <div className="flex flex-col sm:flex-row gap-3">
    <div className="relative flex-1">...</div>  {/* search input */}
    <Button variant="outline" onClick={() => setFilterSheetOpen(true)}>Filters ...</Button>
  </div>

Existing sheet siblings (lines 238–251 approx):
  <FilamentTypeSpoolSheet ... />
  <FilamentTypeFilterSheet open={filterSheetOpen} ... />

<!-- From frontend/src/components/filaments/AddFilamentDialog.tsx (created in plan 03-04) -->
Props: { open: boolean; onOpenChange: (open: boolean) => void }
Export: named — import { AddFilamentDialog } from '@/components/filaments/AddFilamentDialog'

<!-- From 03-UI-SPEC.md Layout Contract -->
"Add Filament" button: variant="default" (filled/primary), icon Plus h-4 w-4, gap-2
Controls row layout: [Search input — flex-1] [Filters — outline] [Add Filament — default]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AddFilamentDialog into FilamentLibrary page</name>
  <files>frontend/src/pages/FilamentLibrary.tsx</files>
  <read_first>
    - frontend/src/pages/FilamentLibrary.tsx (read FULL file — identify exact lines for: import block, useState declarations, controls row JSX, sheet sibling render area at bottom)
    - frontend/src/components/filaments/AddFilamentDialog.tsx (confirm named export and props interface before importing)
    - .planning/phases/03-add-workflows/03-UI-SPEC.md (Layout Contract — "FilamentLibrary Header" section)
  </read_first>
  <action>
    Make four targeted changes to frontend/src/pages/FilamentLibrary.tsx:

    CHANGE 1 — Add import at top of file.
    Locate the existing filament component imports (FilamentTypeFilterSheet, FilamentTypeSpoolSheet, etc.). Add immediately after them:
    `import { AddFilamentDialog } from '@/components/filaments/AddFilamentDialog'`
    Also add `Plus` to the existing lucide-react import if not already present (check imports first).

    CHANGE 2 — Add Dialog open state.
    Locate the line `const [filterSheetOpen, setFilterSheetOpen] = useState(false)`.
    Add immediately after it:
    `const [addDialogOpen, setAddDialogOpen] = useState(false)`

    CHANGE 3 — Add "Add Filament" button to the controls row.
    Locate the Filters button (variant="outline"). Add the new button immediately after it, on the same line level:
    `<Button variant="default" onClick={() => setAddDialogOpen(true)}>
      <Plus className="h-4 w-4 mr-2" />
      Add Filament
    </Button>`
    The controls row must end up as: [Search input flex-1] [Filters outline] [Add Filament default]. Do NOT change the Filters button.

    CHANGE 4 — Render AddFilamentDialog alongside sheet siblings.
    Locate the FilamentTypeFilterSheet render (near end of JSX, after main list content).
    Add immediately after FilamentTypeFilterSheet:
    `<AddFilamentDialog open={addDialogOpen} onOpenChange={setAddDialogOpen} />`

    Do NOT modify any other existing code. The query invalidation in useBulkCreateFilamentType (plan 03-03) handles list refresh automatically — no additional callback needed here.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "FilamentLibrary"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "addDialogOpen" frontend/src/pages/FilamentLibrary.tsx` returns at least 2 matches (useState + button onClick + Dialog prop)
    - `grep "AddFilamentDialog" frontend/src/pages/FilamentLibrary.tsx` returns at least 2 matches (import + JSX render)
    - `grep "Add Filament" frontend/src/pages/FilamentLibrary.tsx` returns a match (button copy)
    - `grep "variant=\"default\"" frontend/src/pages/FilamentLibrary.tsx` returns a match (primary button)
    - `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep "FilamentLibrary"` produces no TypeScript errors
    - `grep "variant=\"outline\"" frontend/src/pages/FilamentLibrary.tsx` still returns the Filters button (not removed)
    - `cd frontend && npm run test -- --reporter=verbose FilamentLibrary 2>&1 | tail -10` — existing FilamentLibrary tests unaffected (if any exist)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page → Dialog | No trust boundary crossed; Dialog is a child component within the same authenticated page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of Privilege | FilamentLibrary page | accept | Page is behind ProtectedRoute; no new auth boundary introduced |
| T-03-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit --skipLibCheck` — no errors in FilamentLibrary.tsx
- Button "Add Filament" present in controls row with variant="default"
- AddFilamentDialog rendered with open={addDialogOpen} onOpenChange={setAddDialogOpen}
- Filters button still present with variant="outline"
</verification>

<success_criteria>
- FilamentLibrary.tsx compiles without TypeScript errors
- "Add Filament" button visible in header alongside Filters
- Dialog opens on button click (controlled by addDialogOpen state)
- List auto-refreshes after add via TanStack Query invalidation (no additional wiring needed)
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-05-SUMMARY.md` when done
</output>
