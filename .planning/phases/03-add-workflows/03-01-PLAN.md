---
phase: 03-add-workflows
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/integration/test_filament_types_api.py
  - frontend/src/components/filaments/AddFilamentDialog.test.tsx
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - ADD-04
  - ADD-05

must_haves:
  truths:
    - "TestBulkCreate class exists in test_filament_types_api.py with failing stubs for all required behaviors"
    - "TestBatchCreate class exists in test_filament_types_api.py with failing stubs for all required behaviors"
    - "AddFilamentDialog.test.tsx exists with failing stubs for Dialog state machine and mutation calls"
    - "All stub tests fail with NotImplementedError or xfail marker, not import errors"
  artifacts:
    - path: "backend/tests/integration/test_filament_types_api.py"
      provides: "TestBulkCreate and TestBatchCreate test classes"
      contains: "class TestBulkCreate"
    - path: "frontend/src/components/filaments/AddFilamentDialog.test.tsx"
      provides: "Dialog state machine and mutation test stubs"
      contains: "AddFilamentDialog"
  key_links:
    - from: "backend/tests/integration/test_filament_types_api.py"
      to: "POST /api/v1/filament-types/bulk-create"
      via: "client.post('/api/v1/filament-types/bulk-create', ...)"
      pattern: "bulk-create"
    - from: "frontend/src/components/filaments/AddFilamentDialog.test.tsx"
      to: "AddFilamentDialog component"
      via: "import AddFilamentDialog"
      pattern: "AddFilamentDialog"
---

<objective>
Create failing test stubs for the Phase 3 add workflows before any implementation begins. Wave 0 ensures every behavior has a test before the implementation plans run.

Purpose: Nyquist compliance — no implementation task is written without a pre-existing test.
Output: TestBulkCreate and TestBatchCreate classes in the existing integration test file; AddFilamentDialog.test.tsx stub file.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md
@.planning/phases/03-add-workflows/03-VALIDATION.md

<interfaces>
<!-- From backend/tests/integration/test_filament_types_api.py — existing structure the executor must follow -->
Existing test class pattern:
  class TestFilamentTypesEndpoints:
      """Integration tests for /api/v1/filament-types endpoints."""
      @pytest.mark.asyncio
      async def test_create_filament_type(self, client, auth_headers, test_material_type): ...

Available fixtures (from backend/tests/conftest.py, verified):
  client: AsyncClient
  auth_headers: dict
  unauthenticated_client: AsyncClient
  db_session: AsyncSession
  test_tenant: Tenant
  test_user: User
  test_material_type: MaterialType
  test_filament_type: FilamentType
  test_spool: Spool

Minimal valid payload helper (existing, lines 12–18):
  def _valid_payload(test_material_type) -> dict:
      return {"material_type_id": str(test_material_type.id), "brand": "Bambu Lab", "color": "Jade White", "diameter": 1.75}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestBulkCreate and TestBatchCreate stubs to integration test file</name>
  <files>backend/tests/integration/test_filament_types_api.py</files>
  <read_first>
    - backend/tests/integration/test_filament_types_api.py (read full file — see existing class structure, imports, fixture usage, section dividers)
    - backend/tests/conftest.py (verify fixture names: test_material_type, test_filament_type, test_spool, auth_headers, unauthenticated_client)
    - .planning/phases/03-add-workflows/03-VALIDATION.md (per-task verification map — all test IDs from 03-02-x and 03-03-x rows)
  </read_first>
  <action>
    Append two new test classes to the END of backend/tests/integration/test_filament_types_api.py, after the last existing class.

    Add required imports at the top if not already present: `import pytest` is already there; ensure `from httpx import AsyncClient` is present.

    Class 1 — TestBulkCreate:
    Section divider: `# ============================================` then `# POST /api/v1/filament-types/bulk-create` then divider again.

    Methods to add (all marked `@pytest.mark.asyncio`, all decorated `@pytest.mark.xfail(reason="not implemented yet", strict=False)`):
    - test_bulk_create_returns_201_with_spool_ids: POST to `/api/v1/filament-types/bulk-create` with payload {material_type_id, brand, color, quantity: 3, initial_weight: 1000.0}; assert status_code == 201; assert "spool_ids" in data; assert len(data["spool_ids"]) == 3
    - test_bulk_create_quantity_1_creates_one_spool: POST with quantity: 1; assert len(data["spool_ids"]) == 1
    - test_bulk_create_spool_id_format: POST with quantity: 2; assert all spool IDs match pattern `FIL-\d{3}` (import re at top of file if not present)
    - test_bulk_create_spools_are_unlabeled: POST with quantity: 2; check that created spools all have is_labeled=False by querying db_session
    - test_bulk_create_requires_auth: use unauthenticated_client; assert status in [401, 403]
    - test_bulk_create_quantity_zero_rejected: POST with quantity: 0; assert status_code == 422
    - test_bulk_create_quantity_over_limit_rejected: POST with quantity: 21; assert status_code == 422
    - test_bulk_create_finds_existing_filament_type: POST twice with same brand+color+material_type_id; assert both responses return same filament_type_id

    Class 2 — TestBatchCreate:
    Section divider: `# ============================================` then `# POST /api/v1/filament-types/batch-create` then divider again.

    Methods to add (all `@pytest.mark.asyncio`, `@pytest.mark.xfail(reason="not implemented yet", strict=False)`):
    - test_batch_create_returns_201_with_results: POST to `/api/v1/filament-types/batch-create` with payload {entries: [{material_type_id, brand, color}], initial_weight: 1000.0}; assert status_code == 201; assert "results" in data; assert len(data["results"]) == 1
    - test_batch_create_multiple_entries: POST with entries list of 3 items (different colors, same brand+material); assert len(data["results"]) == 3
    - test_batch_create_spools_are_unlabeled: assert all results have corresponding spools with is_labeled=False
    - test_batch_create_reuses_existing_filament_type: POST two entries with identical brand+color+material; assert both results share filament_type_id
    - test_batch_create_requires_auth: use unauthenticated_client; assert status in [401, 403]
    - test_batch_create_empty_entries_rejected: POST with entries: []; assert status_code == 422

    Bulk create payload helper — define at module level (after existing _valid_payload):
    `def _bulk_payload(test_material_type, quantity=3): return {"material_type_id": str(test_material_type.id), "brand": "Test Brand", "color": "Ocean Blue", "quantity": quantity, "initial_weight": 1000.0}`
    `def _batch_payload(test_material_type, entries=None): return {"entries": entries or [{"material_type_id": str(test_material_type.id), "brand": "Test Brand", "color": "Forest Green"}], "initial_weight": 1000.0}`
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_filament_types_api.py::TestBulkCreate tests/integration/test_filament_types_api.py::TestBatchCreate -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `class TestBulkCreate:` (grep -c "class TestBulkCreate" backend/tests/integration/test_filament_types_api.py returns 1)
    - File contains `class TestBatchCreate:` (grep -c "class TestBatchCreate" backend/tests/integration/test_filament_types_api.py returns 1)
    - pytest runs the new classes without ImportError or collection errors; tests are collected as xfail
    - `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -v --co 2>&1 | grep -E "TestBulkCreate|TestBatchCreate"` lists at least 8 test items total across the two classes
    - Existing TestFilamentTypesEndpoints and TestFilamentTypeAggregatedEndpoints tests are still collected and unaffected
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create AddFilamentDialog.test.tsx stub</name>
  <files>frontend/src/components/filaments/AddFilamentDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/filaments/FilamentTypeFilterSheet.tsx (see how existing filament component tests are structured — imports, renderWithQueryClient usage)
    - frontend/src/hooks/useFilamentTypes.ts (existing hook names to mock: useFilamentTypes, useBulkCreateFilamentType — will be added in plan 03)
    - frontend/src/components/production-runs/CreateRunWizard.tsx (if a test file exists alongside it, use that as the test structure reference)
    - .planning/phases/03-add-workflows/03-VALIDATION.md (task IDs 03-04-01 and 03-04-02 — behaviors to stub)
  </read_first>
  <action>
    Create frontend/src/components/filaments/AddFilamentDialog.test.tsx as a new file. The component it tests (AddFilamentDialog.tsx) does not exist yet; stubs must import it but the tests will fail on missing module until plan 03-05 ships.

    File structure:
    - Import vitest primitives: `import { describe, it, expect, vi, beforeEach } from 'vitest'`
    - Import testing-library: `import { render, screen, fireEvent, waitFor } from '@testing-library/react'`
    - Import: `import { AddFilamentDialog } from './AddFilamentDialog'` (does not exist yet — test will fail with module not found until the component is created)
    - Mock hooks module: `vi.mock('@/hooks/useFilamentTypes', () => ({ useBulkCreateFilamentType: () => ({ mutateAsync: vi.fn(), isPending: false, isError: false }), useBatchCreateFilamentTypes: () => ({ mutateAsync: vi.fn(), isPending: false, isError: false }) }))`
    - Mock materialTypesApi: `vi.mock('@/lib/api/spools', () => ({ materialTypesApi: { list: () => Promise.resolve([{ id: 'mat-1', name: 'PLA', code: 'PLA' }]) } }))`

    Test cases (all marked with `it.todo(...)` to be fleshed out when component exists, or use `it.skip` with body):

    describe("AddFilamentDialog"):
    - "opens dialog when Add Filament button is clicked": render component, click button with text "Add Filament", expect dialog to appear with text "How would you like to add filament?"
    - "mode selector shows two workflow cards": render open dialog, expect text "Batch of identical spools" and "Multiple color variants"
    - "bulk add mode: navigating back to selector from bulk form": click bulk card, expect form title "Add filament — batch", click back button (aria-label "Back to mode selector"), expect mode selector again
    - "bulk add mode: submitting calls bulkCreate mutation": render in bulk mode, fill brand/color/material fields, submit form, expect mutateAsync called with correct shape
    - "batch mode: add color appends to table": render in batch mode, fill entry form, click "Add color", expect table row to appear
    - "batch mode: submit all disabled when table is empty": render in batch mode, expect "Submit all" button to be disabled
    - "batch mode: submitting calls batchCreate mutation": add row, click "Submit all", expect batchMutation.mutateAsync called

    All test bodies can use `expect(true).toBe(true)` as placeholder — they will be fleshed out in plan 03-07. The important thing is the describe/it structure exists and the file compiles (minus the missing module).
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/frontend && npm run test -- --reporter=verbose AddFilamentDialog.test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/src/components/filaments/AddFilamentDialog.test.tsx
    - File contains `describe('AddFilamentDialog'` (grep confirms)
    - File contains at least 6 `it(` or `it.todo(` or `it.skip(` entries
    - Running the test file produces a "FAIL" result due to missing module (`AddFilamentDialog`) — NOT due to syntax errors or TypeScript compile errors
    - `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep AddFilamentDialog` returns no TS syntax errors (module-not-found is acceptable at this stage)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → API | Test clients hit the API layer with controlled fixtures — no production data at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan; test scaffolds only |
</threat_model>

<verification>
- `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -v --co 2>&1 | grep -E "TestBulkCreate|TestBatchCreate"` lists at least 8 items
- `cd backend && poetry run pytest tests/integration/test_filament_types_api.py::TestFilamentTypesEndpoints -x` still passes (no regression)
- AddFilamentDialog.test.tsx exists and contains the describe block
</verification>

<success_criteria>
- TestBulkCreate and TestBatchCreate classes collected by pytest (xfail, not error)
- AddFilamentDialog.test.tsx present with 6+ test stubs
- No existing test regressions
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-01-SUMMARY.md` when done
</output>
