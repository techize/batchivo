---
phase: 03-add-workflows
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - backend/app/schemas/filament_type.py
  - backend/app/api/v1/filament_types.py
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03

must_haves:
  truths:
    - "BulkCreateRequest, BulkCreateResponse, BatchEntryRequest, BatchCreateRequest, BatchCreateResponse classes exist in schemas"
    - "_next_spool_ids helper function exists in filament_types.py and returns FIL-NNN format IDs"
    - "_find_or_create_filament_type helper exists and uses db.flush() not db.commit()"
    - "bulk_create endpoint registered before /{filament_type_id} dynamic route"
    - "batch_create endpoint registered before /{filament_type_id} dynamic route"
    - "Both endpoints require CurrentUser + CurrentTenant + TenantDB dependencies"
    - "All created spools have is_labeled=False"
    - "Atomic transaction: all spools or none committed"
    - "D-15: POST /api/v1/filament-types/bulk-create and POST /api/v1/filament-types/batch-create endpoints exist with documented request/response shapes"
    - "D-16: Spool IDs reserved atomically using _next_spool_ids within the same transaction — no sequential round-trips"
    - "D-17: Spool ID format is FIL-NNN (zero-padded 3+ digits, e.g. FIL-001, FIL-042)"
    - "D-18: Collision handling: UNIQUE constraint on (tenant_id, spool_id); one retry on IntegrityError; HTTP 409 after second failure"
    - "D-19: Both endpoints check for existing FilamentType matching (tenant_id, brand, color, material_type_id) before inserting"
    - "D-20: Matching FilamentType is reused; optional null fields on existing record are filled by incoming data"
    - "D-21: Match-and-reuse behavior applies to both endpoints; each batch-create row is checked independently"
  artifacts:
    - path: "backend/app/schemas/filament_type.py"
      provides: "BulkCreateRequest, BulkCreateResponse, BatchEntryRequest, BatchCreateRequest, BatchCreateResponse"
      contains: "class BulkCreateRequest"
    - path: "backend/app/api/v1/filament_types.py"
      provides: "bulk_create, batch_create endpoints + _next_spool_ids helper"
      contains: "async def bulk_create"
  key_links:
    - from: "backend/app/api/v1/filament_types.py"
      to: "backend/app/schemas/filament_type.py"
      via: "BulkCreateRequest, BulkCreateResponse, BatchCreateRequest, BatchCreateResponse imports"
      pattern: "BulkCreateRequest"
    - from: "bulk_create"
      to: "prisma._next_spool_ids"
      via: "_next_spool_ids(db, tenant.id, data.quantity)"
      pattern: "_next_spool_ids"
---

<objective>
Add Pydantic schemas for the two add workflow endpoints plus the backend endpoint implementations: `POST /api/v1/filament-types/bulk-create` and `POST /api/v1/filament-types/batch-create`.

Purpose: Deliver the two API endpoints that the frontend Dialog will call. Both create FilamentType + Spools atomically with FIL-NNN sequential IDs.
Output: BulkCreate* and BatchCreate* Pydantic schemas; bulk_create + batch_create route handlers + two private helpers in filament_types.py.
</objective>

<execution_context>
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathan/Repos/batchivo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-add-workflows/03-CONTEXT.md
@.planning/phases/03-add-workflows/03-RESEARCH.md
@.planning/phases/03-add-workflows/03-PATTERNS.md

<interfaces>
<!-- From backend/app/schemas/filament_type.py — existing base class to inherit from -->
class FilamentTypeBase(BaseModel):
    material_type_id: UUID = Field(..., description="Material type ID")
    brand: str = Field(..., min_length=1, max_length=100)
    color: str = Field(..., min_length=1, max_length=50)
    color_hex: Optional[str] = Field(None, max_length=9)
    finish: Optional[str] = Field(None, max_length=50)
    pattern: Optional[str] = Field(None, max_length=50)
    spool_type: Optional[str] = Field(None, max_length=50)
    diameter: float = Field(1.75, gt=0, le=5)
    density: Optional[float] = Field(None, gt=0, le=10)
    extruder_temp: Optional[int] = Field(None, ge=150, le=400)
    bed_temp: Optional[int] = Field(None, ge=0, le=150)
    translucent: bool = Field(False)
    glow: bool = Field(False)
    has_sample: bool = Field(False)
    notes: Optional[str] = None

<!-- From backend/app/api/v1/filament_types.py — existing route order critical context -->
Existing route registration order (lines 39, 72, 123, 195, 228, 258, 305):
  POST ""             → create_filament_type      (line 39)
  GET  ""             → list_filament_types        (line 72)
  GET  "/aggregated"  → list_filament_types_aggregated (line 123) [static — before dynamic]
  GET  "/{id}/spools" → list_spools_for_filament_type (line 195) [static — before dynamic]
  GET  "/{id}"        → get_filament_type          (line 228) [DYNAMIC — new routes go BEFORE this]
  PUT  "/{id}"        → update_filament_type       (line 258)
  DELETE "/{id}"      → delete_filament_type       (line 305)

Existing IntegrityError handling (lines 57–65):
  try:
      await db.commit()
  except IntegrityError:
      await db.rollback()
      raise HTTPException(status_code=HTTP_400_BAD_REQUEST, detail="...")

Existing auth pattern (lines 40–44):
  async def endpoint(data: Schema, user: CurrentUser, tenant: CurrentTenant, db: TenantDB) -> Response:

<!-- From backend/app/api/v1/spools.py lines 403–419 — spool ID logic to extract -->
  max_query = select(func.max(Spool.spool_id)).where(
      Spool.tenant_id == tenant.id, Spool.spool_id.like("FIL-%"))
  max_spool_id = max_result.scalar_one_or_none()
  # Existing code: new_spool_id = f"FIL-{current_num + 1:03d}"
  # New helper returns list: [f"FIL-{current_num + i + 1:03d}" for i in range(count)]

<!-- Spool creation pattern (spools.py lines 422–436) -->
  new_spool = Spool(
      tenant_id=tenant.id, spool_id=spool_id, filament_type_id=ft.id,
      initial_weight=data.initial_weight, current_weight=data.initial_weight,
      is_active=True, is_labeled=False)
  db.add(new_spool)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BulkCreate and BatchCreate Pydantic schemas</name>
  <files>backend/app/schemas/filament_type.py</files>
  <read_first>
    - backend/app/schemas/filament_type.py (read full file — understand FilamentTypeBase fields, import block, ConfigDict usage pattern)
  </read_first>
  <behavior>
    - BulkCreateRequest inherits from FilamentTypeBase minus has_sample, adds quantity (ge=1, le=20) and initial_weight (default 1000.0, gt=0)
    - BulkCreateResponse has filament_type_id: UUID and spool_ids: list[str]
    - BatchEntryRequest has same fields as FilamentTypeBase minus has_sample (no quantity, no initial_weight)
    - BatchCreateRequest has entries: list[BatchEntryRequest] = Field(..., min_length=1) and initial_weight: float = Field(1000.0, gt=0)
    - BatchCreateResponse has results: list[dict]
    - Validation: POST bulk-create with quantity=0 returns 422; quantity=21 returns 422; quantity=1 accepted
  </behavior>
  <action>
    Append five new schema classes at the END of backend/app/schemas/filament_type.py, after the last existing class (SpoolInSheetResponse).

    BulkCreateRequest: Do NOT inherit FilamentTypeBase directly (it includes has_sample which is server-controlled). Instead declare all fields from FilamentTypeBase except has_sample, then add:
    - quantity: int = Field(..., ge=1, le=20, description="Number of spools to auto-generate")
    - initial_weight: float = Field(1000.0, gt=0, description="Initial weight per spool in grams")

    BulkCreateResponse:
    - filament_type_id: UUID
    - spool_ids: list[str]
    - model_config = ConfigDict(from_attributes=True)

    BatchEntryRequest: Same fields as FilamentTypeBase minus has_sample (no quantity, no initial_weight). Declare fields directly (do not inherit FilamentTypeBase).

    BatchCreateRequest:
    - entries: list[BatchEntryRequest] = Field(..., min_length=1, description="One entry per color variant; each creates 1 spool")
    - initial_weight: float = Field(1000.0, gt=0, description="Shared weight per spool in grams")

    BatchCreateResponse:
    - results: list[dict] — each dict has shape: {"filament_type_id": UUID, "spool_id": str}
    - model_config = ConfigDict(from_attributes=True)

    Add docstrings to each class. Do NOT modify any existing class.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run python -c "from app.schemas.filament_type import BulkCreateRequest, BulkCreateResponse, BatchEntryRequest, BatchCreateRequest, BatchCreateResponse; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.schemas.filament_type import BulkCreateRequest, BulkCreateResponse, BatchEntryRequest, BatchCreateRequest, BatchCreateResponse` succeeds without error
    - BulkCreateRequest(material_type_id="00000000-0000-0000-0000-000000000001", brand="B", color="C", quantity=0, initial_weight=1000) raises ValidationError (ge=1 constraint)
    - BulkCreateRequest(material_type_id=..., brand="B", color="C", quantity=21, initial_weight=1000) raises ValidationError (le=20 constraint)
    - BulkCreateRequest(material_type_id=..., brand="B", color="C", quantity=1, initial_weight=1000) succeeds
    - BatchCreateRequest(entries=[], initial_weight=1000) raises ValidationError (min_length=1)
    - Existing imports (FilamentTypeBase, FilamentTypeCreate, etc.) still work: `from app.schemas.filament_type import FilamentTypeCreate` succeeds
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _next_spool_ids helper, _find_or_create_filament_type helper, and bulk_create + batch_create endpoints</name>
  <files>backend/app/api/v1/filament_types.py</files>
  <read_first>
    - backend/app/api/v1/filament_types.py (read full file — note line numbers of static routes /aggregated and /{id}/spools, and the start of /{filament_type_id} GET at line 228)
    - backend/app/api/v1/spools.py (lines 370–441 — duplicate_spool function with existing FIL-NNN generation logic)
    - backend/app/models/spool.py (confirm field names: spool_id, filament_type_id, initial_weight, current_weight, is_active, is_labeled, tenant_id)
  </read_first>
  <behavior>
    - _next_spool_ids(db, tenant_id, count) returns list of count sequential FIL-NNN IDs; called within an open transaction
    - _find_or_create_filament_type(db, tenant_id, data) uses db.flush() not db.commit(); merges optional fields (null on existing filled by incoming); returns FilamentType
    - POST /bulk-create: requires auth (401 without token); creates FilamentType (find-or-create) + N spools atomically; all spools have is_labeled=False; spool IDs follow FIL-NNN format; returns 201 with filament_type_id + spool_ids list
    - POST /batch-create: requires auth; each entry independently find-or-creates FilamentType; creates one spool per entry; all spools is_labeled=False; returns 201 with results list [{filament_type_id, spool_id}]
    - Duplicate FilamentType: second POST with same brand+color+material_type_id reuses the existing record; does not insert duplicate
    - Spool ID collision: catch IntegrityError on commit, rollback, retry once with fresh _next_spool_ids; return HTTP 409 after second failure
  </behavior>
  <action>
    Modify backend/app/api/v1/filament_types.py. All additions go in two places:
    1. After the existing imports block — add new schema imports and define two private helpers.
    2. Between the existing `/{filament_type_id}/spools` GET route (line ~195) and the `/{filament_type_id}` GET route (line ~228) — insert the two new POST routes.

    Step A — Update imports. Add to the existing import from app.schemas.filament_type:
    BulkCreateRequest, BulkCreateResponse, BatchCreateRequest, BatchCreateResponse
    (keep all existing imports, just add to the list)

    Step B — Add _next_spool_ids helper function after the existing filament_type_to_response function.
    Signature: async def _next_spool_ids(db: AsyncSession, tenant_id: UUID, count: int) -> list[str]
    Implementation: query SELECT max(Spool.spool_id) WHERE tenant_id = tenant_id AND spool_id LIKE 'FIL-%'. Parse current_num from result (handle ValueError → 0, None result → 0). Return list comprehension: [f"FIL-{current_num + i + 1:03d}" for i in range(count)].
    Must be called within an open transaction block (caller's responsibility). Add docstring stating this.

    Step C — Add _find_or_create_filament_type helper after _next_spool_ids.
    Signature: async def _find_or_create_filament_type(db: AsyncSession, tenant_id: UUID, data) -> FilamentType
    Where data is any schema with brand, color, material_type_id, and optional fields (color_hex, finish, pattern, spool_type, diameter, density, extruder_temp, bed_temp, translucent, glow, notes).
    Implementation: SELECT FilamentType WHERE tenant_id=tenant_id AND brand=data.brand AND color=data.color AND material_type_id=data.material_type_id. If found: iterate optional_fields = ['color_hex', 'finish', 'pattern', 'spool_type', 'notes', 'density', 'extruder_temp', 'bed_temp']; for each field where getattr(existing, field) is None and incoming value is not None, setattr then db.add(existing). Return existing. If not found: create FilamentType(tenant_id=tenant_id, has_sample=False, **data.model_dump(exclude={'quantity', 'initial_weight'})), db.add(ft), await db.flush(), await db.refresh(ft), return ft.

    Step D — Add bulk_create POST route between line ~195 `/{filament_type_id}/spools` GET and the `/{filament_type_id}` GET.
    Route: @router.post("/bulk-create", response_model=BulkCreateResponse, status_code=status.HTTP_201_CREATED)
    Dependencies: data: BulkCreateRequest, user: CurrentUser, tenant: CurrentTenant, db: TenantDB
    Logic:
      - Call _find_or_create_filament_type(db, tenant.id, data) → ft
      - Call _next_spool_ids(db, tenant.id, data.quantity) → spool_ids
      - db.add_all([Spool(tenant_id=tenant.id, spool_id=sid, filament_type_id=ft.id, initial_weight=data.initial_weight, current_weight=data.initial_weight, is_active=True, is_labeled=False) for sid in spool_ids])
      - await db.commit()
      - Catch IntegrityError: await db.rollback(); retry once (call _next_spool_ids again, rebuild spools list, re-add_all, re-commit); if second IntegrityError: raise HTTPException(status_code=409, detail="Spool ID collision — please retry.")
      - Return BulkCreateResponse(filament_type_id=ft.id, spool_ids=spool_ids)
    logger.info("bulk_create: created {quantity} spools for FilamentType {ft.id} (tenant {tenant.id})")

    Step E — Add batch_create POST route immediately after bulk_create.
    Route: @router.post("/batch-create", response_model=BatchCreateResponse, status_code=status.HTTP_201_CREATED)
    Dependencies: data: BatchCreateRequest, user: CurrentUser, tenant: CurrentTenant, db: TenantDB
    Logic:
      - Reserve all spool IDs in one call: all_spool_ids = await _next_spool_ids(db, tenant.id, len(data.entries))
      - For each entry (enumerate): ft = await _find_or_create_filament_type(db, tenant.id, entry); db.add(Spool(tenant_id=tenant.id, spool_id=all_spool_ids[i], filament_type_id=ft.id, initial_weight=data.initial_weight, current_weight=data.initial_weight, is_active=True, is_labeled=False))
      - Collect results list of {"filament_type_id": str(ft.id), "spool_id": all_spool_ids[i]}
      - await db.commit()
      - Catch IntegrityError: rollback, retry once (same all-at-once approach); second failure raises HTTPException(status_code=409, detail="Spool ID collision — please retry.")
      - Return BatchCreateResponse(results=results)
    logger.info("batch_create: created {len} spools for tenant {tenant.id}")

    CRITICAL: Both new routes (/bulk-create, /batch-create) MUST appear BEFORE the `/{filament_type_id}` GET route (line ~228). Verify placement by searching for the /{filament_type_id} GET route in the file and inserting above it.
  </action>
  <verify>
    <automated>cd /Users/jonathan/Repos/batchivo/backend && poetry run pytest tests/integration/test_filament_types_api.py::TestBulkCreate tests/integration/test_filament_types_api.py::TestBatchCreate -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def bulk_create\|async def batch_create\|async def _next_spool_ids\|async def _find_or_create" backend/app/api/v1/filament_types.py` returns 4 matches
    - `grep -n "async def get_filament_type" backend/app/api/v1/filament_types.py` returns a line number GREATER than the line numbers of bulk_create and batch_create (static routes before dynamic)
    - `cd backend && poetry run python -c "from app.api.v1.filament_types import router; print('OK')"` succeeds
    - TestBulkCreate tests all pass (xfail removed or tests genuinely pass): `cd backend && poetry run pytest tests/integration/test_filament_types_api.py::TestBulkCreate -v` shows PASSED not XFAIL for the core tests
    - TestBatchCreate tests all pass
    - `cd backend && poetry run pytest tests/integration/test_filament_types_api.py::TestFilamentTypesEndpoints -x` still passes (no regression in existing endpoints)
    - POST /bulk-create with quantity=3 returns 201 with spool_ids containing 3 IDs all matching regex `^FIL-\d{3,}$`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client request → bulk_create | Untrusted quantity, brand, color, material_type_id cross the API boundary |
| Client request → batch_create | Untrusted entries array with unbounded length potential |
| Backend → Database | tenant_id must come from JWT, never from request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | bulk_create, batch_create | mitigate | tenant_id sourced exclusively from CurrentTenant (JWT-derived), never from request body; TenantDB activates RLS |
| T-03-02 | Denial of Service | bulk_create quantity field | mitigate | Pydantic Field(ge=1, le=20) enforced in BulkCreateRequest; validated before DB write; 422 returned for violation |
| T-03-03 | Denial of Service | batch_create entries list | mitigate | Pydantic Field(min_length=1) on entries list; batch size implicitly bounded by timeout/request limits |
| T-03-04 | Tampering | spool_id field | mitigate | spool_id is entirely server-generated via _next_spool_ids; no spool_id field in BulkCreateRequest or BatchEntryRequest |
| T-03-SC | Tampering | pip installs | accept | No new packages; existing stack only |
</threat_model>

<verification>
- `cd backend && poetry run pytest tests/integration/test_filament_types_api.py -x -v` — all tests pass
- `cd backend && poetry run ruff check app/api/v1/filament_types.py app/schemas/filament_type.py` — no lint errors
- POST /bulk-create: quantity=3 → 201 + 3 FIL-NNN spool_ids
- POST /batch-create: 2 entries → 201 + 2 results
- POST /bulk-create: no auth → 401
- POST /bulk-create: quantity=0 → 422
</verification>

<success_criteria>
- BulkCreateRequest, BulkCreateResponse, BatchEntryRequest, BatchCreateRequest, BatchCreateResponse importable from app.schemas.filament_type
- bulk_create and batch_create routes respond 201 with correct shapes
- Static routes registered before /{filament_type_id} dynamic route
- All spools created with is_labeled=False
- Retry on spool ID collision (HTTP 409 after two failures)
- Existing test suite unaffected
</success_criteria>

<output>
Create `.planning/phases/03-add-workflows/03-02-SUMMARY.md` when done
</output>
