---
phase: 3
slug: add-workflows
status: draft
shadcn_initialized: true
preset: none
created: 2026-05-20
---

# Phase 3 — UI Design Contract: Add Workflows

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (default style) |
| Preset | Not configured — base neutral, CSS variables enabled |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | Lucide React |
| Font | Inter, Plus Jakarta Sans, system-ui fallback |

Source: `frontend/components.json`, `frontend/tailwind.config.js`, `frontend/src/index.css`

---

## Spacing Scale

Declared values (multiples of 4 only). Batchivo uses Tailwind default spacing scale — these are the semantic tokens for Phase 3 component internals.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, color swatch to label gap |
| sm | 8px | Form field label-to-input gap, button internal padding |
| md | 16px | Form field spacing, Dialog section padding |
| lg | 24px | Dialog content padding (top/bottom), section breaks within form |
| xl | 32px | Mode selector card height padding |
| 2xl | 48px | Dialog max-height internal scroll boundary |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Touch targets on +/− quantity buttons: minimum 44px height (accessibility, mobile)
- Color swatch dot in rapid batch table: 12px × 12px (3px border-radius)

Source: default — aligns with existing FilamentLibrary.tsx spacing (`space-y-6`, `p-4 sm:p-6`, `gap-3`)

---

## Typography

All sizes use Inter/Plus Jakarta Sans at the weights below. Matches existing page typography in FilamentLibrary.tsx.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 14px | 500 (medium) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 24px | 700 (bold) | 1.2 |

Notes:
- Dialog title uses Heading (20px, semibold) — rendered by `DialogTitle` which inherits `text-lg font-semibold`
- Mode selector card titles use Heading (20px, semibold)
- Form field labels use Label (14px, medium) — Tailwind `text-sm font-medium`
- Table cell text uses Body (14px, regular)
- Helper/hint text under fields uses Body (14px, regular, `text-muted-foreground`)

Source: default — matches existing `text-2xl sm:text-3xl font-bold` on page headings and `text-sm` form fields project-wide

---

## Color

Batchivo brand palette defined in `frontend/src/index.css` CSS variables and `frontend/tailwind.config.js`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(var(--background))` — Cream #fffaf0 | Dialog backdrop, page background, form surfaces |
| Secondary (30%) | `hsl(var(--card))` — White #ffffff + `hsl(var(--secondary))` #edf2f7 | Mode selector cards, rapid batch accumulator table rows, form card containers |
| Accent (10%) | `hsl(var(--accent))` — Amber #d97706 | "+ Add" button (primary variant), focus rings, active filter badges |
| Destructive | `hsl(var(--destructive))` — Coral #f56565 | Row remove button in rapid batch table only |

Accent reserved for:
- The "+ Add" button in FilamentLibrary page header (primary Button variant — filled amber/navy)
- Focus ring on all form inputs and interactive elements (`ring` token)
- Active Badge on Filters button when filters are applied

Destructive reserved for:
- Remove row button (trash icon) in the rapid batch accumulator table

Source: `frontend/src/index.css` CSS custom properties, CONTEXT.md D-01

---

## Component Inventory

All components already installed in `frontend/src/components/ui/`. No new installs required for this phase.

| Component | Source | Usage in Phase 3 |
|-----------|--------|-----------------|
| `Dialog` | `@/components/ui/dialog` | Add workflow container (D-02) |
| `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter` | shadcn/ui Dialog parts | Dialog structure |
| `Button` | `@/components/ui/button` | "+ Add" (primary), "Filters" (outline), back arrow (ghost/icon), form submit |
| `Card`, `CardContent`, `CardHeader`, `CardTitle`, `CardDescription` | `@/components/ui/card` | Mode selector cards (D-03) |
| `Input` | `@/components/ui/input` | Brand, color, finish, notes, weight, quantity fields |
| `Select`, `SelectTrigger`, `SelectContent`, `SelectItem` | `@/components/ui/select` | Material type dropdown |
| `Badge` | `@/components/ui/badge` | "needs label" indicator (ADD-03), quantity count in mode cards |
| `Alert`, `AlertDescription` | `@/components/ui/alert` | API error display within Dialog (D-discretion) |
| `Table`, `TableBody`, `TableHead`, `TableHeader`, `TableRow`, `TableCell` | `@/components/ui/table` | Rapid batch accumulator table |
| `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` | `@/components/ui/collapsible` | "More options" section in bulk add form (D-07) |
| `Form`, `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormMessage` | `@/components/ui/form` | All form fields (react-hook-form integration) |

Source: `frontend/src/components/ui/` directory scan, CONTEXT.md canonical refs

---

## Layout Contract

### FilamentLibrary Header (modified)

```
[Search input — flex-1] [Filters — outline] [+ Add — primary]
```

- Controls row: `flex flex-col sm:flex-row gap-3` (existing pattern, extend with third button)
- "+ Add" button: `variant="default"` (filled navy/amber) to distinguish from outline Filters button (D-01, CONTEXT specifics)
- "+ Add" icon: `Plus` from lucide-react, `h-4 w-4` with `gap-2`

### Add Workflow Dialog

- Container: `DialogContent` with `max-w-lg` class, `overflow-y-auto max-h-[90vh]`
- Header: `DialogHeader` containing `DialogTitle` + `DialogDescription`; back arrow (ArrowLeft icon, ghost icon Button) is shown on form screens but not the mode selector screen
- Footer: `DialogFooter` containing primary action button (right-aligned)
- No size transition between mode selector and forms (D-02)

### Mode Selector Screen

- Two `Card` components side by side (`grid grid-cols-1 sm:grid-cols-2 gap-4`)
- Each card is fully clickable (`cursor-pointer`, `hover:border-primary` ring on hover)
- Card structure: icon at top → title (Heading) → description (Body, muted)
- Icon for bulk add: `Package` (lucide), Icon for rapid batch: `Layers` (lucide)
- No back button on this screen (it is the first screen)

### Bulk Add Form

- Single scrollable form, all fields visible (D-06)
- Field order: brand → color → material type → finish → [More options collapsible] → horizontal divider → weight per spool → quantity
- "More options" collapsible: `ChevronDown` icon rotates to `ChevronUp` when open; label "More options" in `text-sm text-muted-foreground`
- Quantity row: `Input type="number"` flanked by `−` and `+` icon Buttons (`variant="outline" size="icon"`, min height 44px)
- Form submit button: "Add spools" (`variant="default"`, full-width in `DialogFooter`)

### Rapid Batch Add Form

- Shared weight field at top of form (`w-32`, right-aligned label "Weight per spool (g)")
- Entry form below: brand → color (required) → material type → finish
- "Add color" button: `variant="outline"`, appends row to table
- Accumulator table below form: columns Brand | Color | Material | Finish | (remove)
  - Color column: 12×12px swatch dot (inline `bg-[color_hex]` + `border rounded-full`) if `color_hex` present, then color name text
  - Remove button per row: `variant="ghost" size="icon"`, `Trash2` icon, destructive color
- "Submit all" button: `variant="default"`, full-width in `DialogFooter`, disabled when table is empty

---

## State Contract

| State | Element | Visual Treatment |
|-------|---------|-----------------|
| Dialog closed | Add button | Normal primary Button |
| Mode selector | Dialog | Two mode cards, no back arrow |
| Bulk form | Dialog | Back arrow visible, form fields, submit button |
| Batch form | Dialog | Back arrow visible, weight field, entry form, table, submit button |
| Submitting | Submit button | Disabled + `Loader2` spin icon (`animate-spin`) |
| API error | Dialog (below form) | `Alert variant="destructive"` with `AlertDescription` |
| Submit success (bulk) | Dialog | Closes; list refreshes via query invalidation |
| Submit success (batch) | Dialog | Stays open; table clears; form resets to last values (D-14) |
| Back navigation | Mode selector | Form state preserved at Dialog level (D-23) |
| Empty batch table | Submit all button | Disabled state |
| More options (bulk) | Collapsible | Collapsed by default; icon rotates on toggle |

---

## Copywriting Contract

Source: CONTEXT.md decisions + requirement language

| Element | Copy |
|---------|------|
| Primary CTA (header button) | + Add |
| Mode selector heading | How would you like to add filament? |
| Mode selector subheading | Choose a workflow to continue |
| Bulk add card title | Batch of identical spools |
| Bulk add card description | Create one filament type and generate multiple spools at once |
| Batch add card title | Multiple color variants |
| Batch add card description | Set a shared weight then add different colors in rapid succession |
| Dialog title (bulk) | Add filament — batch |
| Dialog title (batch) | Add filament — color variants |
| Bulk submit button | Add spools |
| Batch add-row button | Add color |
| Batch submit button | Submit all |
| Quantity field label | Number of spools |
| Weight field label | Weight per spool (g) |
| More options toggle | More options |
| Needs label badge (list, after add) | Needs label |
| Empty batch table hint | Add at least one color variant above to continue |
| Error state (API) | Something went wrong. Check your connection and try again. |
| Error state (duplicate spool ID, 409) | A spool ID collision occurred. Please try again. |
| Back button aria-label | Back to mode selector |
| Remove row aria-label | Remove this entry |
| Quantity minimum validation | Must add at least 1 spool |
| Quantity maximum validation | Maximum 20 spools per batch |
| Required field validation | {Field name} is required |

Destructive actions in this phase:
- Remove row from rapid batch accumulator table: no confirmation dialog (low consequence — row is local state, not persisted until "Submit all"). Single trash icon click removes immediately. Provide undo via re-add (no toast undo in this phase).

---

## Form Validation Contract

Strategy: `react-hook-form` + Zod, validation fires on submit (not on blur). Source: CONTEXT.md Claude's Discretion.

### Bulk Add Schema

| Field | Rule |
|-------|------|
| brand | required, min 1 char |
| color | required, min 1 char |
| material_type_id | required (Select must have a value) |
| finish | optional string |
| notes | optional string |
| color_hex | optional, regex `^#[0-9a-fA-F]{6}$` if provided |
| diameter | optional number, 0.1–5.0 |
| extruder_temp | optional integer, 150–320 |
| bed_temp | optional integer, 0–120 |
| density | optional number, 0.5–2.5 |
| quantity | required integer, 1–20 |
| initial_weight | required number, default 1000 |

### Rapid Batch Entry Schema (per-step)

| Field | Rule |
|-------|------|
| brand | required, min 1 char |
| color | required, min 1 char |
| material_type_id | required |
| finish | optional string |
| initial_weight | required number (shared, top-level field) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Dialog, Button, Card, Input, Select, Badge, Alert, Table, Collapsible, Form | not required |

No third-party registries declared for this phase. All components confirmed installed in `frontend/src/components/ui/`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
